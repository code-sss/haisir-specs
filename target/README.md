# Target State

This directory defines the near-term target state for hAIsir, built
incrementally from `current/`. See `current/` for the current schema, API
endpoints, and UI flows.

The long-term vision (full 6-persona platform, hAITU, notifications, etc.)
is in `vision/requirements/`.
