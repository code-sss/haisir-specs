# PLAN — Phase 6 (scoping in progress)

> Phase 5.6 closed out 2026-07-21 and archived to
> `archive/PLAN_Phase5.6-SecretsElimination_2026-07-16.md` /
> `archive/TASKS_Phase5.6-SecretsElimination_2026-07-16.md`. Phase 5.5 was already archived
> (`archive/PLAN_Phase5.5-SecretsOpenBao_2026-07-15.md`). This file will be replaced with the
> full Phase 6 goal tree once scope is confirmed via `/plan`.

## Backlog candidates (carried forward from Phase 5, untouched by 5.5/5.6)

Per `phases.md`'s Phase 5 and 5.5 entries:

- Remaining role migration — `become-tutor`/`invite-role` flows, frontend role-switcher metadata,
  `/institution` + `/parent` route guards (`vision/requirements/11_role_migration.md`)
- RAG ops backlog — external HTTP reranker for the stubbed Stage 3, bundled inference service in
  deploy, hAITU Prometheus monitoring (still blocked on Chainguard licensing)
- Per-child audience scoping of parent-created content
- Parent-facing hAITU endpoints (`vision/requirements/00_overview.md` §3.5–3.7)

Next step: confirm which of these are in scope for Phase 6 (and in what order) with the user,
then run the full `/plan` decomposition (goal tree, challenger rounds, `TASKS.md` checkboxes).
