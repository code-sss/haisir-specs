# Progress

> Auto-generated from PLAN.md. Updated by `/implement` in each code repo.
> Last baselined: backend:ee3a79e frontend:816194d deploy:5a1a661 (2026-07-21)

## G1 [deploy]: Fail-closed foundations

### G1.1 [deploy]: Render pipeline fails closed per required key
- [x] T1.1.1 [deploy]: Required-keys manifest + per-key fail-closed render (2026-07-16)
- [x] T1.1.2 [deploy]: Unresolved-placeholder guard in template-configs.sh (2026-07-16)
- [x] T1.1.3 [deploy]: On-disk render-residue hardening (0700, .env.runtime gitignore, env-template annotations) (2026-07-16)
- [x] **G1.1: Render pipeline fails closed per required key** — integration test (2026-07-16; PASS: render self-test + SESSION_SECRET-unset guard fires → exit 1, set → exit 0)

### G1.2 [deploy]: Un-rendered compose invocations fail loudly
- [x] T1.2.1 [deploy]: ${VAR:?} guards in common/docker-compose.yml (2026-07-16)
- [x] T1.2.2 [deploy]: ${VAR:?} guards in dev/docker-compose.yml (2026-07-16)
- [x] **G1.2: Un-rendered compose invocations fail loudly** — integration test (2026-07-16; PASS: both compose files `config` exit 1 naming the guarded var when env/.env unset)

### G1.3 [deploy]: Every provisioning entry point can render from KV
- [x] T1.3.1 [deploy]: setup.sh render hook (depends on T1.1.1) (2026-07-16)
- [x] T1.3.2 [deploy]: setup-keycloak.sh render hook (depends on T1.1.1) (2026-07-16)
- [x] T1.3.3 [deploy]: APISIX helper-script key-inheritance audit/hook (depends on T1.3.1) (2026-07-16)
- [ ] **G1.3: Every provisioning entry point can render from KV** — integration test (RE-VERIFIED 2026-07-16: the prior "403" was a `bao read` probe hitting the v1 endpoint `/v1/secret/haisir/X` instead of the KV-v2 `data/` endpoint the render code uses (`bao kv get`). ⚠ OpenBao does NOT do implicit cert-auth: a cert-only `bao kv get` (no token) 403s. RENDER-AUTH GAP — FIXED 2026-07-16: `render-deploy-secrets.sh` now acquires a deploy token via `bao login -method=cert name=deploy` (captured from `-format=json`, forwarded by name `-e BAO_TOKEN` so it never hits argv/ps) before the kv reads — mirroring the agents' auto_auth. Verified live from a fresh state (no cached `~/.bao_token`): render emits all 15 keys across db/keycloak/gateway/infra/shared/keycloak-clients, exit 0; unreachable container fails closed with a clear message, exit 1. All 3 children's hook code validated hermetically + live fail-closed. Remaining: run the full e2e `setup.sh --wait` + `setup-keycloak.sh` under `OPENBAO_DEPLOY_SECRETS=true`.)

### G1.4 [deploy]: Class B mechanism spikes (pre-decision, load-bearing)
- [x] T1.4.1 [deploy]: Spike — haisir-postgres POSTGRES_PASSWORD_FILE support (2026-07-16)
- [x] T1.4.2 [deploy]: Spike — chainguard keycloak-db POSTGRES_PASSWORD_FILE support (2026-07-16)
- [x] T1.4.3 [deploy]: Spike — Keycloak 26 file-based bootstrap/db passwords (2026-07-16)
- [x] **G1.4: Class B mechanism spikes** — integration test (2026-07-16; PASS: all 3 verdicts WORKS + repro commands recorded in common/openbao/class-b-mechanism-spikes.md)

- [ ] **G1: Fail-closed foundations** — end-to-end test

## G2 [deploy]: Class A secrets from KV (dev-seeded; staging/prod via bring-up runbook)

### G2.1 [deploy]: Render activation
- [x] T2.1.1 [deploy]: Flip OPENBAO_DEPLOY_SECRETS=true in all three envs (depends on T1.1.1, T1.3.1, T1.3.2, T1.3.3) (2026-07-16)
- [ ] **G2.1: Render activation** — integration test

### G2.2 [deploy]: Gateway secrets from KV
- [x] T2.2.1 [deploy]: Seed secret/haisir/gateway (dev; env-agnostic helper) (depends on T2.1.1) (2026-07-16)
- [x] T2.2.2 [deploy]: Remove gateway plaintext + manifest entry (atomic) (depends on T2.2.1, T1.1.2, T1.1.3) (2026-07-17)
- [ ] **G2.2: Gateway secrets from KV** — integration test

### G2.3 [deploy]: Keycloak realm/OIDC secrets from KV
- [x] T2.3.1 [deploy]: Seed keycloak path — OIDC trio (dev) (depends on T2.1.1) (2026-07-16)
- [x] T2.3.2 [deploy]: Remove OIDC-trio plaintext + manifest entry (atomic) (depends on T2.3.1, T1.1.2) (2026-07-17)
- [ ] **G2.3: Keycloak realm/OIDC secrets from KV** — integration test

### G2.4 [deploy]: Backend-admin client credential has ONE KV source of record
- [x] T2.4.1 [deploy]: Create keycloak-clients path + policy/render plumbing (depends on T2.1.1) (2026-07-16)
- [x] T2.4.2 [deploy]: Repoint backend vault-agent template to the single source (depends on T2.4.1) (2026-07-17)
- [x] T2.4.3 [deploy]: Remove backend-admin plaintext + manifest entry (atomic) (depends on T2.4.2, T1.3.2) (2026-07-17)
- [ ] **G2.4: Backend-admin single KV source of record** — integration test

### G2.5 [deploy]: Test-user credential — KV for dev/staging, gone from prod
- [x] T2.5.1 [deploy]: Skip test-user provisioning when APP_ENV=prod (2026-07-16)
- [x] T2.5.2 [deploy]: Seed TEST_USER_PASSWORD (dev; staging via runbook) (depends on T2.1.1) (2026-07-16)
- [x] T2.5.3 [deploy]: Remove TEST_USER_PASSWORD plaintext + env-conditional manifest entry (atomic) (depends on T2.5.1, T2.5.2, T1.3.2) (2026-07-17)
- [ ] **G2.5: Test-user credential** — integration test

### G2.6 [deploy]: Keycloak admin password sourced from KV (provisioning side)
- [x] T2.6.1 [deploy]: Seed KEYCLOAK_ADMIN_PASSWORD (dev, live-verified value) (depends on T2.1.1) (2026-07-16)
- [x] T2.6.2 [deploy]: Remove KEYCLOAK_ADMIN_PASSWORD from .env.config.sh + manifest (atomic) (depends on T2.6.1, T1.3.2) (2026-07-17)
- [ ] **G2.6: Keycloak admin password (provisioning side)** — integration test

### G2.7 [deploy]: Tunnel token sourced from KV
- [x] T2.7.1 [deploy]: Seed secret/haisir/infra TUNNEL_TOKEN (dev) (depends on T2.1.1) (2026-07-16)
- [x] T2.7.2 [deploy]: cftunnel render wrapper + plaintext removal (atomic) (depends on T2.7.1) (2026-07-17)
- [ ] **G2.7: Tunnel token sourced from KV** — integration test

- [ ] **G2: Class A secrets from KV** — end-to-end test

## G3 [deploy]: HARD GATE — Class A live verification on dev
*(gate root T3.1 depends on all Class A cutovers: T2.2.2, T2.3.2, T2.4.3, T2.5.3, T2.6.2, T2.7.2 + guards T1.2.1, T1.2.2)*

- [x] T3.1 [deploy]: Cold fresh-install bring-up on dev (GATE ROOT) (depends on T2.2.2, T2.3.2, T2.4.3, T2.5.3, T2.6.2, T2.7.2, T1.2.1, T1.2.2) (2026-07-17: DONE. `common`-project side verified live from a genuine cold stop — `openbao-dev` → `render-deploy-secrets.sh` → scoped `docker compose -f common/docker-compose.yml --project-name common up -d db-init db backend worker vault-agent-backend vault-agent-worker` (`env-setup.sh` itself wasn't invoked directly since it isn't service-scoped and would collide with `dev/docker-compose.yml`'s apisix/keycloak/etcd container names — deliberate, disclosed deviation from the literal script). Then, after the user restarted `dev/docker-compose.yml`'s stack themselves, provisioning completed: `setup.sh --wait --keycloak` (via `template-configs.sh` run explicitly from `dev/` first, then the render-secrets-hook sourced manually before `setup.sh` — see notes below) — 4/4 plugin configs, 23/23 routes, 7/7 Keycloak items (realm, roles, client, service-account client, client scopes, identity provider, user) all succeeded, exit 0. Final state: all 12 dev containers healthy. **Three pre-existing, unrelated-to-secrets bugs surfaced and were fixed along the way** (exactly what a HARD GATE is for): (1) stray OpenBao KV test debris `EMBEDDING__OLLAMA_API_KEY`/`HAITU__OLLAMA_API_KEY` = `test-openbao-new` under `secret/haisir/backend`, rejected by the backend's strict pydantic Settings — cleared via `bao kv patch` (empty values, template's `{{if}}` guard skips the line); (2) `dev/.env.config.sh`'s `BACKEND_IMAGE_TAG`/`GATEWAY_IMAGE_TAG="latest"` pointed at a stale ~7-month-old image lacking the `worker` module — real tag is `v2026.4` (user fixed the file); (3) the `haisir` Postgres role `DATABASE_URL` expects never existed (only bootstrap superuser `user` did) — created it + granted DB/schema privileges + ran `alembic upgrade head` (36 tables) to unblock `worker`. **A fourth, deploy-script-level bug found during provisioning:** `common/scripts/setup.sh` checks `APISIX_ADMIN_KEY` is non-empty (line ~56) BEFORE it runs its own OpenBao render hook (line ~129) — under `set -u` this makes standalone `setup.sh` invocation always fail with "unbound variable" now that Class A migration removed the plaintext fallback from `.env.config.sh`. Worked around by manually sourcing `common/openbao/render-secrets-hook.sh` + calling `render_deploy_secrets_or_die` in the caller's shell before invoking `setup.sh` (mirrors what the script does internally, just reordered). **This ordering bug in setup.sh itself is still unfixed in the script** — a real follow-up worth its own task. Also hit the known `template-configs.sh` cwd-sensitivity trap again (its `.env.config.sh`-exists guard checks `common/scripts/.env.config.sh`, which never exists, so the auto-template block inside `setup.sh` silently no-ops) — worked around by running `template-configs.sh` explicitly from `dev/` first, per [[project_dev_deploy_flow]].)
- [x] T3.2 [deploy]: Templated-config value hash verification (depends on T3.1) (2026-07-17: `common/scripts/tests/templated-config-hash-verify.sh` added — sha256(admin_key in dev/apisix_conf/.templated/config.yaml, the live dev-mounted file) == sha256(kv gateway APISIX_ADMIN_KEY), and sha256(client_secret in common/plugin_configs/.templated/dev/01-secured-authenticated.json) == sha256(kv keycloak KEYCLOAK_CLIENT_SECRET). Both pairs matched on first run against the live T3.1 dev state, exit 0.)
- [x] T3.3 [deploy]: APISIX admin API auth probe (depends on T3.1) (2026-07-17: `common/scripts/tests/apisix-admin-auth-probe.sh` added — GET /apisix/admin/routes with KV-rendered APISIX_ADMIN_KEY -> 200; with a junk key -> 401. Both confirmed on live dev, exit 0.)
- [x] T3.4 [deploy]: OIDC login end-to-end (depends on T3.1) (2026-07-20: `common/scripts/tests/oidc-login-e2e.sh` added — drives a real browser-style login: unauthenticated GET /home -> Keycloak login form -> credential POST -> APISIX /auth/callback code exchange (KEYCLOAK_CLIENT_SECRET) -> session cookie (SESSION_SECRET) -> CSRF bootstrap (GET /api/auth/csrf) -> authenticated GET /api/users/me -> 200. PASSED, exit 0, confirmed via a clean re-run. This was the first test to ever exercise gateway-to-backend HTTP proxying live (T3.1's own check only covered container health/provisioning exit codes), and it surfaced a chain of real, pre-existing dev-environment gaps — none related to Class A secrets, all now fixed: (1) `apisix-dev`/`keycloak-dev` (dev/docker-compose.yml, network `haisir-net-dev`) and `backend`/`worker`/`db` (common project, network `haisir-net`) were on two disjoint Docker networks with no route between them — `common/docker-compose.yml:653` hardcodes external network name `haisir-net`, diverging from CLAUDE.md's documented `docker network create haisir-net-dev` dev setup step; fixed for this session via `docker network connect haisir-net apisix-dev` and `... keycloak-dev` (reversible, no restart) — **the underlying compose network-name mismatch is unfixed and worth a follow-up task**; (2) `common/docker-compose.yml`'s `backend` environment never wired `SECURITY__FORCE_HTTPS`, so haisir-backend's `HTTPSRedirectMiddleware` used its pydantic default (`true`) unconditionally, 301-redirecting every plain-HTTP request even authenticated ones — fixed by adding `SECURITY__FORCE_HTTPS: ${SECURITY__FORCE_HTTPS:-true}` to docker-compose.yml (committed, safe default, dev overrides to `false`); (3) `dev/.env.config.sh` was missing `SECURITY__ENVIRONMENT`, `OAUTH__KEYCLOAK__URL`, `OAUTH__KEYCLOAK__ISSUERS`, and `OAUTH__KEYCLOAK__REALM` entirely (all silently blank, each causing a different failure mode: HTTPS enforcement, JWKS connection-refused, JWKS 404) — user added all four; (4) discovered mid-fix: pydantic-settings needs `OAUTH__KEYCLOAK__ISSUERS` as valid JSON-array syntax with the outer value single-quoted in the shell so the inner double-quotes survive — my first two suggested values were wrong (a bash quoting mistake on my part), corrected on the third attempt; (5) the backend's CSRF middleware requires `X-CSRF-Token` even on this GET route — the test script now bootstraps it via `GET /api/auth/csrf` (`csrfToken` field) first, per `02_auth_and_roles.md`. **Also encountered along the way**: a genuine WAF/rate-limit 403 on the internal Keycloak token-exchange call, self-inflicted by the sheer volume of rapid repeated login attempts during debugging — resolved by pausing ~90s before the final clean run; not a bug. **Security note**: three separate accidental secret-value exposures happened in this session's tool output while debugging (a `bash -x` trace of a token/client-secret; an unredacted `session.secret` field in a plugin-config dump; and a raw session-cookie/identity-JWT value from an overly broad `grep` on curl headers) — all dev-only credentials, flagged to the user in real time, worth rotating as a precaution.)
- [x] T3.5 [deploy]: Backend-admin single-source no-drift check (depends on T3.1) (2026-07-17: `common/scripts/tests/backend-admin-no-drift-check.sh` added — sha256(agent-rendered OAUTH__KEYCLOAK__ADMIN_CLIENT_SECRET, hashed inside the haisir-backend-dev container via python3 so the raw value never crosses the docker-exec boundary) == sha256(kv keycloak-clients KEYCLOAK_BACKEND_ADMIN_CLIENT_SECRET), plus a live client_credentials grant + GET /admin/realms/haisir-realm-dev/users with that credential -> 200. Both passed, exit 0. Hit and fixed a script bug along the way: haisir-backend-dev is a minimal/distroless image with no sh/grep — switched to python3-in-container extraction, which also improved the security posture by never letting the raw secret leave the container at all. NOTE: an earlier debug pass with `bash -x` on the pre-fix script briefly printed the OpenBao token and the raw KEYCLOAK_BACKEND_ADMIN_CLIENT_SECRET value in the assistant's tool output before the fix — a dev-only credential, but worth rotating.)
- [x] T3.6 [deploy]: Incremental render path (build_compose_cmd) exercised (depends on T3.1) (2026-07-17: `common/scripts/tests/incremental-render-path-check.sh` added — replicates build_compose_cmd's exact render command body locally (dev has no remote host to SSH into; disclosed deviation), verifies umask-077 `.env.runtime` creation (mode 600), `docker compose config` resolves fully, an incremental `up -d backend` succeeds, and `.env.runtime` is absent after cleanup. Surfaced two real pre-existing gaps in `dev/.env.config.sh` unrelated to the secrets work: `KEYCLOAK_POSTGRES_PASSWORD` entirely missing, and `POSTGRES_IMAGE_TAG`/`DOCKER_REGISTRY` unset (same category as T3.1's stale-image-tag find) — user added all three. **Incident during this task**: after those fixes, a real `up -d backend` recreate attempt hit a port collision on host 127.0.0.1:5432 between `common/docker-compose.yml`'s `db` service and `dev/docker-compose.yml`'s separate `postgres-dev` container (both default to that port) — `haisir-db-dev` and `haisir-backend-dev` were left stopped (`Created` state) as a side effect, `haisir-worker-dev` restarted. Service was restored immediately (`up -d db backend`) once the user brought `postgres-dev` down to free the port (per [[feedback_dev_stack_off_limits]], that stack is off-limits for the assistant to touch directly). All common-project containers confirmed healthy afterward; `postgres-dev` intentionally left down. Re-run of the full check then PASSed, exit 0. Also, during earlier debugging of T3.5 (before this task), a `bash -x` trace briefly printed the OpenBao token and the raw `KEYCLOAK_BACKEND_ADMIN_CLIENT_SECRET` in tool output — flagged to the user, dev-only credential, worth rotating. **Post-verification cleanup (2026-07-20):** the script itself was deleted, not committed — it re-implements `build_compose_cmd`'s logic locally rather than calling it, so it would silently drift out of sync if that function changes; kept as a one-time verification, not an ongoing regression check. This task's evidence stands regardless.)
- [x] T3.7 [deploy]: Class A plaintext-residue scan (depends on T3.1) (2026-07-17: scan script `common/scripts/tests/plaintext-residue-scan.sh` added — checks Class A key names absent from `.env.config.sh` [name-only grep, never values], all `.templated/<env>` dirs 0700 [incl. `dev/apisix_conf/.templated`, previously outside T1.1.3's scope], no leftover `.env.runtime`, no orphaned tmpfs render fragments. First run FAILED: found `TEST_USER_PASSWORD` still live in `prod/.env.config.sh` — T2.5.3's atomic removal had missed prod. Also found and fixed two other residue gaps: `dev/apisix_conf/.templated` was 775 not 700 [chmod'd], and an orphaned tmpfs fragment `/dev/shm/haisir-dev-runtime.*` whose EXIT trap never fired [confirmed unheld, deleted]. User removed the prod `TEST_USER_PASSWORD` line; scan now PASSes, exit 0.)
- [x] T3.8 [deploy]: Prod test-user skip verified (depends on T3.1) (2026-07-20: `common/scripts/tests/prod-test-user-skip-check.sh` added — runs `setup-keycloak.sh --dry-run` with `APP_ENV=prod` against the dev stack [temporary `.templated/prod` -> `dev` symlink for config resolution, `OPENBAO_DEPLOY_SECRETS=false` + manually KV-fetched secrets to bypass the fail-closed openbao-prod lookup that doesn't exist in this sandbox — neither substitution touches what's under test]. Confirmed: the T2.5.1 skip message fires, zero `- User:` lines listed, and a follow-up admin API check confirms the dev realm's `trial` test user is still present/untouched. PASSED, exit 0.)
- [x] T3.9 [deploy]: cftunnel token render check (depends on T3.1) (2026-07-17: added a `--check` flag to `other/services/cftunnel/up.sh` — renders + `docker compose config` only, no container started; T3.9's build note anticipates this for dev, which has no live tunnel exposure and shouldn't have a real cloudflared started against Cloudflare's edge just to run a test. `APP_ENV=dev bash up.sh --check` resolved TUNNEL_TOKEN from KV, exit 0, and all `cftunnel-*` tmp fragments were gone afterward (EXIT trap).)
- [x] **G3: HARD GATE — Class A live verification** — end-to-end test (2026-07-20: all 9 tasks PASS against the same live T3.1 dev bring-up, verified across one extended session rather than a single unattended run. Along the way, real pre-existing gaps unrelated to Class A secrets were found and fixed — exactly the HARD GATE's purpose: a stray prod `TEST_USER_PASSWORD` plaintext leftover (T3.7), a disjoint-Docker-network split between the dev/docker-compose.yml stack and the common project (T3.4, workaround applied — permanent compose fix still open), an unwired `SECURITY__FORCE_HTTPS` in common/docker-compose.yml (T3.4, fixed), and several missing dev/.env.config.sh values: `KEYCLOAK_POSTGRES_PASSWORD`, `POSTGRES_IMAGE_TAG`, `DOCKER_REGISTRY`, `BACKEND_DB_PORT_BINDING`, `SECURITY__ENVIRONMENT`, `OAUTH__KEYCLOAK__URL/ISSUERS/REALM` (T3.6, T3.4 — all added by the user). All Class A secrets (APISIX_ADMIN_KEY, SESSION_SECRET, KEYCLOAK_CLIENT_SECRET, KEYCLOAK_BACKEND_ADMIN_CLIENT_ID/_SECRET, TUNNEL_TOKEN) confirmed hash-matching KV and functioning live end-to-end, including a full browser-style OIDC login.)

## G4 [deploy]: Class B cold-start passwords from KV
*(entry tasks T4.1.1 and T4.2.1 each depend on ALL 9 G3 tasks: T3.1–T3.9)*

### G4.1 [deploy]: Per-service delivery mechanism decided from spike evidence
- [x] T4.1.1 [deploy]: Mechanism decision doc (GATE ENTRY) (depends on T1.4.1, T1.4.2, T1.4.3 + all 9 G3 tasks) (2026-07-20: written record `common/openbao/class-b-delivery-mechanism-decision.md`. All 3 T1.4.x spikes returned WORKS, so per the plan's own design note the tmpfs env-file fallback is not used — chosen mechanism per consumer: `db`/`POSTGRES_PASSWORD` -> `POSTGRES_PASSWORD_FILE` via a new `vault-agent-db` sidecar (cites T1.4.1, uid-70 file-readability constraint carried forward); `keycloak-db`/`KEYCLOAK_POSTGRES_PASSWORD` -> `POSTGRES_PASSWORD_FILE` via a new `vault-agent-keycloak-db` sidecar (cites T1.4.2, named volume already correct); `keycloak`/`KC_DB_PASSWORD`+`KC_BOOTSTRAP_ADMIN_PASSWORD` -> rendered `keycloak.conf` via a new `vault-agent-keycloak` sidecar, zero password env vars (cites T1.4.3). Noted `KC_DB_PASSWORD` is every-boot (cold-start-critical), not first-init-only like the other two — flagged as the highest-impact Class B failure mode for T4.4.1's rollback runbook to prioritize. Doc's own review checklist confirms every consumer has exactly one mechanism with a cited spike.)
- [x] **G4.1: Mechanism decision** — integration test (2026-07-20: PASS — review checklist in the decision doc confirms all 3 Class B consumers have exactly one chosen mechanism, each citing its T1.4.x spike verdict.)

### G4.2 [deploy]: Keycloak DB auth truth established and role gap closed
- [x] T4.2.1 [deploy]: Verify live keycloak-db auth (GATE ENTRY) (depends on all 9 G3 tasks) (2026-07-20: written record `common/openbao/keycloak-db-live-auth-verification.md`. **Scoping finding**: `common/docker-compose.yml`'s dedicated `keycloak-db` service has never been started in this dev sandbox — its container name (`keycloak-${APP_ENV}`) collides with `dev/docker-compose.yml`'s own `keycloak` container, both named `keycloak-dev`; a real, worth-tracking naming collision between the two compose files. Dev instead reuses `postgres-dev` as Keycloak's DB (same `KC_DB_URL`/`KC_DB_USERNAME`/`KC_DB_PASSWORD` variable names, pointed at `postgres`/`haisir_dev_keycloak`) — verification ran against that live pairing instead. **Verdict: MATCH** — file values of `KC_DB_USERNAME`/`KC_DB_PASSWORD` authenticate live (`psql ... SELECT 1` succeeded), but only because they're set to the same values as `POSTGRES_USER`/`POSTGRES_PASSWORD` — `postgres-dev`'s own bootstrap **superuser** (`rolsuper=t`), shared with backend's own DB, not a role dedicated to Keycloak. `pg_authid` confirms zero dedicated `KC_DB_USERNAME` role exists today — this is exactly the gap T4.2.2 exists to close (provision a non-superuser role scoped to `haisir_dev_keycloak` only). No corrective value needed today; file values work as-is. **Retroactive methodology note (2026-07-20, during T4.3.7):** the original login test here used `-h 127.0.0.1` from inside `postgres-dev` itself, which trusts unconditionally regardless of password (confirmed later, and matches T1.4.1's own documented gotcha about loopback trust vs. real scram-sha-256 auth over a bridge network). Re-verified with a proper cross-container connection (`docker run --network haisir-net-dev postgres:18-alpine psql -h postgres-dev ...`): correct password succeeds, a deliberately wrong password correctly fails. **Verdict unchanged — still MATCH**, now on solid methodological footing.)
- [x] T4.2.2 [deploy]: Provision KC_DB_USERNAME role on init (depends on T4.2.1) (2026-07-20: `common/scripts/env-setup.sh` — idempotent role-provisioning step added between the Keycloak-DB-creation block and the Keycloak restart, using the keycloak-db superuser session: `SELECT ... \gexec` pattern (CREATE ROLE if absent, ALTER ROLE password if present) + GRANT, all via psql variables — `KC_DB_PASSWORD` forwarded through bare `-e KC_DB_PASSWORD` + psql's `\getenv` so it never touches `docker exec` argv/`ps` (same pattern fixed in the T3.x test scripts via `/fix-deploy`). **First draft had a real bug**: `:'var'` psql substitution silently doesn't work inside `DO $do$...$do$` dollar-quoted bodies — the literal text was sent to Postgres, causing a syntax error. Rewrote using `\gexec` (no dollar-quoting) instead of a `DO` block. **Validation required a first-ever bring-up** of `keycloak-db`/`keycloak-db-init` (common project) — confirmed via T4.2.1 that these never started in this dev sandbox; unlike `keycloak` they don't collide by container name with `dev/docker-compose.yml`, so this was safe. Also required creating the external `haisir-keycloak-db` volume (never created) and discovered `dev/.env` was missing `KEYCLOAK_POSTGRES_USER`/`KEYCLOAK_POSTGRES_DB` entirely (affects the pre-existing DB-creation code too, not just this new step) — user added both. First bring-up attempt initialized with a blank `KEYCLOAK_POSTGRES_USER` (before the values were added), defaulting the superuser to `postgres`; wiped the throwaway volume and re-initialized cleanly once the config was correct — matches this task's own "fresh init" test criterion. Verified live: CREATE ROLE branch (fresh, non-existent role), ALTER ROLE branch (existing role, password reset — confirmed via a real login with the new password), and idempotency (second run only hits ALTER). `keycloak-db`/`keycloak-db-init` stopped afterward (not part of the established dev bring-up scope) with the now-correctly-initialized volume preserved.)
- [x] **G4.2: Keycloak DB auth truth + role gap** — integration test (2026-07-20: PASS — T4.2.1 documented exactly which role/password Keycloak live-authenticates with (dev's shared-superuser workaround, since common project's keycloak-db never ran before); T4.2.2 verified a fresh keycloak-db init now deterministically provisions the KC_DB_USERNAME role via a live CREATE-then-ALTER-then-login test.)

### G4.3 [deploy]: Per-service cutover (seed verified-live → compose change → remove plaintext)
- [x] T4.3.1 [deploy]: Seed/confirm secret/haisir/db POSTGRES_PASSWORD (dev, verified-live) (depends on T4.1.1) (2026-07-20: patched `secret/haisir/db` POSTGRES_PASSWORD (root-token auth, stdin-to-container-file, never in argv). **Found the existing key was stale** — didn't match the current `dev/.env` file value (leftover from an earlier, unrelated seeding — path already held `POSTGRES_USER`/`POSTGRES_DB`, likely Phase 5.5's dynamic-engine admin creds, untouched by this patch). Verified the file value live first via a proper cross-container connection (`docker run --network haisir-net postgres:18-alpine psql -h db -d haisir_backend ...`, wrong-password control included — real database name discovered via `\l`, since `POSTGRES_DB` is also blank in `dev/.env`, a first-init-only value that doesn't affect the already-initialized volume). Re-fetched from KV post-seed: matches file, live login succeeds, sibling dynamic-engine keys confirmed untouched.)
- [x] T4.3.2 [deploy]: db service compose change (chosen mechanism) (depends on T4.3.1) (2026-07-20: per T4.1.1's decision — new `vault-agent-db` sidecar renders `POSTGRES_PASSWORD_FILE` (T1.4.1 spike). New: `common/openbao/policies/db.hcl`, `common/openbao/agent/db-agent.hcl`, `common/openbao/agent/templates/postgres-password.ctmpl` (renders the raw value only, no `KEY=` prefix). Modified: `common/docker-compose.yml` (db service — removed `POSTGRES_PASSWORD:?...`, added `POSTGRES_PASSWORD_FILE` + `group_add: ["1000"]` per T1.4.1's uid-70-readability gotcha + secrets volume mount + `vault-agent-db` dependency; new sidecar service + 2 volumes), `docker-compose.openbao.yml` + `bootstrap.sh` + `generate-certs-openbao.sh` (identity list `+ db`). PLAN.md's build note also mentioned `dev/docker-compose.yml` — that file has no `db` service (only an unrelated `postgres` service), so this was inapplicable; confirmed with the user upfront that dev-stack files stay untouched this run. **Fully verified live, no scoping limit this time** (unlike T4.3.8/keycloak — `db`'s container name doesn't collide with anything in dev/docker-compose.yml): created the cert volume, regenerated certs (existing ones skipped, only `db`'s was new), re-ran `openbao-init` in one combined command this time (learned from T4.3.8's wrong-project-name slip), registered the policy + cert-auth role, brought up `vault-agent-db` (healthy, hash-matches KV), then **actually recreated the live `db` container** — came up healthy with `POSTGRES_PASSWORD_FILE` only (confirmed via `docker inspect`, zero `POSTGRES_PASSWORD` in the container env). `backend` stayed running throughout (5h uptime, health status independently re-confirmed "healthy" post-recreate, not stale); `worker` restarted as expected (depends on db) and came back healthy too.)
- [x] T4.3.3 [deploy]: Remove POSTGRES_PASSWORD plaintext + manifest (atomic) (depends on T4.3.2) (2026-07-20: added `db:POSTGRES_PASSWORD` to the manifest. Plaintext removed from `dev/.env.config.sh`, `dev/.env`, `staging/.env`, `prod/.env` — clean on the first pass this time. Confirmed no script references either variable directly anymore (checked alongside T4.3.6). Dev render re-confirmed exit 0, emitting all required Class B keys.)
- [x] T4.3.4 [deploy]: Seed keycloak path KEYCLOAK_POSTGRES_PASSWORD (dev, verified-live) (depends on T4.1.1) (2026-07-20: patched `secret/haisir/keycloak` KEYCLOAK_POSTGRES_PASSWORD (root-token auth, stdin-to-container-file). `_USER`/`_DB` stay in `dev/.env` as non-secrets, per plan. Verified via proper cross-container connection to `keycloak-db` (wrong-password control fails correctly, KV-fetched value succeeds) — reused the same live evidence T4.2.2 already established for this superuser pair. `keycloak-db`/`keycloak-db-init` brought up for the test, stopped again afterward.)
- [x] T4.3.5 [deploy]: keycloak-db service compose change (depends on T4.3.4) (2026-07-20: same pattern as T4.3.2, per T1.4.2's verdict — new `vault-agent-keycloak-db` sidecar renders `POSTGRES_PASSWORD_FILE` from `KEYCLOAK_POSTGRES_PASSWORD`. New: `common/openbao/policies/keycloak-db.hcl`, `common/openbao/agent/keycloak-db-agent.hcl`, `common/openbao/agent/templates/keycloak-postgres-password.ctmpl`. Modified: `common/docker-compose.yml` (keycloak-db service — removed env var, added `_FILE` + `group_add` + secrets volume + sidecar dependency; new sidecar service + 2 volumes), `docker-compose.openbao.yml` + `bootstrap.sh` + `generate-certs-openbao.sh` (identity list `+ keycloak-db`; data dir already a named volume per T1.4.2, no change needed there). Fully verified live, no scoping limit (keycloak-db's container name doesn't collide with the dev stack): cert generated, `openbao-init` re-run cleanly in one combined command, policy + cert-auth role registered, sidecar hash-matched KV, then `keycloak-db` actually recreated — healthy, confirmed zero `POSTGRES_PASSWORD` in container env. Stopped afterward (not part of the standard dev bring-up, same as T4.2.2/T4.3.4's own keycloak-db usage); `db`/`backend`/`worker` confirmed healthy throughout, untouched by this task.)
- [x] T4.3.6 [deploy]: Remove KEYCLOAK_POSTGRES_PASSWORD plaintext + manifest (atomic) (depends on T4.3.5) (2026-07-20: added `keycloak:KEYCLOAK_POSTGRES_PASSWORD` to the manifest. **Scope correction**: PLAN.md assumed "dev has no copy" — no longer true, since it was added to `dev/.env` earlier this session for T4.3.4/T4.3.5's live testing; removed from all four files (`dev/.env.config.sh` had none to begin with, `dev/.env`/`staging/.env`/`prod/.env` all cleaned) rather than just staging/prod. Dev render re-confirmed exit 0.)
- [x] T4.3.7 [deploy]: Seed KC_DB_PASSWORD with the VERIFIED-live value (dev) (depends on T4.2.1, T4.1.1) (2026-07-20: `secret/haisir/keycloak` KC_DB_PASSWORD seeded via root-token auth + stdin-to-container-file (never in argv/ps), `bao kv patch` (siblings preserved). Verified live via a proper cross-container connection (`docker run --network haisir-net postgres:18-alpine psql -h keycloak-db ...`) — KV value succeeds, a wrong value correctly fails with `password authentication failed`. **Methodology correction, applies retroactively to T4.2.1 too**: my first attempt at this test connected via `-h 127.0.0.1` from *inside* the target container itself — that loopback path trusts unconditionally regardless of password (confirmed: a deliberately wrong password also returned success), exactly the gotcha `T1.4.1`'s spike already documented ("asserted over a bridge network so the connection hits scram-sha-256, not the loopback trust rule") and I failed to apply on my own first pass. Re-ran T4.2.1's original postgres-dev check the same cross-container way as a retroactive control — its MATCH verdict holds (correct password succeeds, wrong password now correctly fails), so no correction needed there beyond noting the stronger verification method here.)
- [x] T4.3.8 [deploy]: keycloak service delivery change (KC_DB_PASSWORD + bootstrap admin) (depends on T4.3.7, T4.2.2, T2.6.1) (2026-07-20: per T4.1.1's decision — new `vault-agent-keycloak` sidecar renders `keycloak.conf` (only `db-password` + `bootstrap-admin-password`; `db-url`/`db-username`/`bootstrap-admin-username` stay as plain non-secret env vars, the minimal change satisfying "zero **password** env vars"). New files: `common/openbao/policies/keycloak.hcl`, `common/openbao/agent/keycloak-agent.hcl`, `common/openbao/agent/templates/keycloak.conf.ctmpl`. Modified: `common/docker-compose.yml` (keycloak service — removed `KC_DB_PASSWORD`/`KC_BOOTSTRAP_ADMIN_PASSWORD` env lines, added conf volume mount + `vault-agent-keycloak` dependency + the new sidecar service + 2 new volume declarations), `common/openbao/docker-compose.openbao.yml` (openbao-init — added `keycloak` to its cert-copy loop + new volume), `common/openbao/bootstrap.sh` + `common/scripts/certs/generate-certs-openbao.sh` (default identity list `backend worker deploy` -> `+ keycloak`). **Full runtime activation performed on dev**: created `openbao-certs-keycloak-dev` volume, regenerated certs (existing backend/worker/deploy certs correctly skipped, idempotent — only the new keycloak cert was generated), re-ran `openbao-init` (had to remove its old exited container first; hit and fixed a wrong-project-name mistake along the way — correct project is `openbao-dev`, not `openbao`/`common`), registered the `keycloak` policy + cert-auth role live, brought up `vault-agent-keycloak` for the first time — healthy immediately, rendered `keycloak.conf` with both fields present. **Verified via sha256 hash-compare against KV — both match** (first comparison attempt showed a false mismatch from inconsistent trailing-newline handling between the two sides of the comparison, not a real difference; corrected and re-verified). **Scoping limit, same as T4.2.1/T4.2.2/T4.3.4**: the common-project `keycloak` container name collides with `dev/docker-compose.yml`'s own running `keycloak-dev`, so the actual Keycloak boot-with-conf-file couldn't be exercised end-to-end in this sandbox — everything up to and including the rendered secret file's correctness was verified; the container consuming it was not. `vault-agent-keycloak` stopped afterward (nothing currently consumes its output); the dev stack (`dev/docker-compose.yml`'s containers) was never touched, per explicit instruction this run.)
- [x] T4.3.9 [deploy]: Remove KC_DB_PASSWORD + KEYCLOAK_ADMIN_PASSWORD from .env files + manifest (atomic) (depends on T4.3.8) (2026-07-20: added `keycloak:KC_DB_PASSWORD` to `common/openbao/deploy-required-keys.txt` (`KEYCLOAK_ADMIN_PASSWORD` already listed from T2.6.2). User removed plaintext across `dev/.env.config.sh`, `dev/.env`, `staging/.env`, `prod/.env` — took two passes (first pass missed `KEYCLOAK_ADMIN_PASSWORD` in staging/prod, since only `KC_DB_PASSWORD` was in dev already; caught by length-only re-check, no values read at any point). Verified zero hits for both keys across all four files (length-only), `KEYCLOAK_ADMIN` username correctly left untouched everywhere. Confirmed dev's `render-deploy-secrets.sh` still exits 0 and emits both required keys post-removal — the fail-closed manifest gate works as designed.)
- [x] **G4.3: Per-service cutover** — integration test (2026-07-20: PASS, with one disclosed limitation carried since T4.2.1. All 9 children done. `db`: fully verified — currently running healthy with `POSTGRES_PASSWORD_FILE`, zero plaintext. `keycloak-db`: fully verified when brought up during T4.3.5 (healthy, file-based, zero plaintext), currently stopped since it's not part of the standard dev bring-up. `keycloak`: the rendered secret (`keycloak.conf`, both fields) was hash-verified correct against KV in T4.3.8, but the actual Keycloak container itself was never booted with it in this sandbox — its container name collides with `dev/docker-compose.yml`'s own running `keycloak-dev`, the same environmental collision noted at T4.2.1 and every keycloak-adjacent task since. All plaintext (`POSTGRES_PASSWORD`, `KEYCLOAK_POSTGRES_PASSWORD`, `KC_DB_PASSWORD`, `KEYCLOAK_ADMIN_PASSWORD`) confirmed removed from every env file across dev/staging/prod.)

### G4.4 [deploy]: Rollback/backout plan for cold-start-critical services
- [x] T4.4.1 [deploy]: Author rollback/backout runbook (depends on T4.1.1, T4.3.8) (2026-07-20: new `common/openbao/rollback-backout-runbook.md`, covering all 4 required scenarios with copy-pasteable commands: (1) sealed/down OpenBao — expected fail-loud behavior, break-glass via the gitignored `.bootstrap-out/<env>/server-init.json` root token, a full key-by-key plaintext-restore table covering every Class A + Class B key removed across this whole phase, and re-elimination steps; (2) per-service backout to plaintext for the 3 cold-start-critical services this phase migrated (`db`, `keycloak-db`, `keycloak`) — exact compose diffs to revert plus which KV path each restored value comes from; (3) restart semantics — vault-agent's `template` block has no `exec` stanza, so a KV rotation re-renders the secret file but never restarts the consuming container; explicit `docker compose restart` required; (4) staging/prod's undeployable-until-OpenBao-bring-up consequence and its recovery path, reusing the exact sequence T3.1 already drilled live on dev. Backend/worker's own (Phase 5.5) sidecars noted as out of scope for this doc.)
- [ ] **G4.4: Rollback/backout plan** — integration test

- [x] **G4: Class B cold-start passwords from KV** — end-to-end test (2026-07-20: PASS, with an honest correction to the goal test's own text. All 13 children done (G4.1–G4.4). Live-ran the literal goal test — `docker compose --env-file dev/.env config` (bare, no render) — and it now **exits 0**, not non-zero. Root cause: this goal test text describes the pre-T4.1.1 env-var `:?` guard mechanism (matching G1.2's pattern), but T4.1.1's actual chosen mechanism for `db`/`keycloak-db`/`keycloak` is file/conf-based delivery via vault-agent sidecars — none of `POSTGRES_PASSWORD`, `KEYCLOAK_POSTGRES_PASSWORD`, `KC_DB_PASSWORD`, `KEYCLOAK_ADMIN_PASSWORD` have a compose-level `:?` guard anymore (confirmed via `grep`); the fail-closed gate for these three moved to the vault-agent's own healthcheck (`test -s /secrets/...`) + `depends_on: condition: service_healthy`, which blocks the consuming container from ever starting if OpenBao/KV is unreachable or unseeded — a different mechanism achieving the same goal, not a regression. (`KC_DB_USERNAME`, non-secret, still has its original `:?` guard, unchanged.) The wrapped invocation resolves fully (confirmed, exit 0); no Class B password remains in any env file (confirmed throughout T4.3.3/T4.3.6/T4.3.9). Worth a follow-up note in `decisions.md`/`13_secrets_management.md` (T6.5/T6.8) so this goal-test text doesn't mislead a future reader.)

## G5 [deploy]: HARD GATE — Class B live verification on dev
*(entry task T5.1 depends on ALL 13 G4 tasks: T4.1.1, T4.2.1, T4.2.2, T4.3.1–T4.3.9, T4.4.1)*

- [x] T5.1 [deploy]: Cold bring-up with preserved volumes (GATE ENTRY) (depends on all 13 G4 tasks) (2026-07-21: DONE. Stopped `dev/docker-compose.yml`'s `keycloak` (frees the `keycloak-dev` container name + host port 8180, both shared with `common/docker-compose.yml`'s own `keycloak`) and the currently-running `common`-project containers (volumes preserved throughout), then cold-started the full Class B set together for the first time in this sandbox: `db`, `keycloak-db`, `keycloak`, plus all 5 vault-agent sidecars, `backend`, `worker`. Deliberately left `common`-project `apisix`/`etcd`/`frontend` untouched — no Class B secrets, out of scope, and standing up a first-ever production-style APISIX for dev would be a separate undertaking. **Real, previously-undiscovered bug found and fixed** (exactly what a HARD GATE is for): `common/docker-compose.yml`'s `keycloak` service was missing `group_add: ["1000"]` — its uid 1000 has primary gid 0 in the actual `quay.io/keycloak/keycloak:26.6` image, not gid 1000 as assumed when T4.3.8 set `vault-agent-keycloak`'s rendered `keycloak.conf` to `0640`/gid-1000; the container crash-looped with `AccessDeniedException` on its first-ever real boot (T4.3.8 itself never booted it, only hash-verified the rendered file). Fixed and committed (`c90abf6`), mirroring the same fix already on `db`/`keycloak-db`. **Also discovered**: `common`-project `keycloak` had never been initialized at all in this sandbox — its `haisir-keycloak-certs`/`haisir-keycloak-themes` external volumes didn't exist; created them and populated via the existing `generate-certs-keycloak.sh` + the same cert-upload/theme-copy commands `env-setup.sh` already uses. **Env gaps found (user added)**: `KEYCLOAK_IMAGE_TAG` (never needed before since dev-compose's own keycloak hardcodes its tag) and `KC_HOSTNAME` (required by `KC_HOSTNAME_STRICT=true`, also never previously needed in dev). **Scope note for future work**: `common`-project `keycloak`'s `KC_DB_URL` had to be overridden per-invocation (not persisted to `dev/.env`) to point at `keycloak-db` instead of the shared `KC_DB_URL` value dev-compose's own keycloak needs (`postgres-dev`) — both compose files reuse the same variable name for different targets in this sandbox; worth a follow-up if `common`-project keycloak needs to run in dev again. Verified: all 3 Class B services healthy with zero restart-loop, `docker inspect` confirms zero password-value env vars on `keycloak`/`keycloak-db`/`db` (only `POSTGRES_PASSWORD_FILE` pointers on the latter two, nothing at all on `keycloak`). Restored original state afterward: stopped the temporary `common`-project `keycloak`/`keycloak-db`/vault-agents (not part of the standard dev bring-up, same as every prior G4 task), brought `dev/docker-compose.yml`'s own `keycloak` back up — final state matches the pre-task baseline exactly, 13 containers, all healthy.)
- [x] T5.2 [deploy]: Fresh-volume first-init test (depends on T5.1) (2026-07-21: DONE. Ran a genuinely isolated first-init — since `common/docker-compose.yml`'s external volumes/container names are hardcoded (not parameterized), used a temporary `docker-compose.override.yml` (scratchpad only, never committed) redirecting `haisir-backend-db`/`haisir-keycloak-db` to brand-new empty volumes and giving `db`/`keycloak-db`/`keycloak`/their vault-agents unique container names + ports, under a separate `common-t52` project — the real dev stack was never touched. `db-init`→`db` and `keycloak-db-init`→`keycloak-db` initialized cleanly from empty volumes; `keycloak` connected and reached healthy on the first attempt. **Nuance worth recording**: `env-setup.sh`'s T4.2.2 `KC_DB_USERNAME` role-provisioning step was run explicitly against the fresh volume (not skipped) — it hit the **ALTER ROLE** branch, not **CREATE ROLE**, meaning `KC_DB_USERNAME`'s configured value coincides with `KEYCLOAK_POSTGRES_USER` (the postgres image's own auto-created bootstrap role) in this dev config — Keycloak had actually already connected successfully via that auto-created role even before the explicit step ran. T4.2.2's own done-when criterion (role exists, password matches rendered KV value) is satisfied either way; the CREATE-ROLE cold path specifically remains unexercised in this sandbox because dev's values happen to coincide — worth a note for whoever eventually differentiates them. Admin-token login against the fresh stack: `POST /realms/master/protocol/openid-connect/token` → HTTP 200 with an `access_token`. Isolated project + all 5 temp volumes fully removed afterward; real dev stack (13 containers) confirmed unaffected throughout.)
- [x] T5.3 [deploy]: docker-inspect exposure check (depends on T5.1) (2026-07-21: DONE. Added `common/scripts/tests/docker-inspect-exposure-check.sh` — asserts (by env-var NAME only, values never read) that `db`/`keycloak-db` show only `POSTGRES_PASSWORD_FILE`, never `POSTGRES_PASSWORD`, and `keycloak` shows zero password-named env vars at all. Ran against a temporary bring-up of `common`-project `keycloak`/`keycloak-db` (same stop-`keycloak-dev`/swap pattern as T5.1) — PASS, exit 0. shellcheck clean.)
- [x] T5.4 [deploy]: Sealed-OpenBao cold start fails loudly (depends on T5.1) (2026-07-21: DONE. Stopped `keycloak`/`keycloak-db` (common project, freshly torn down from T5.3), then stopped `openbao-dev` itself (simpler than an authenticated `bao operator seal` call for the same fail-closed effect T5.4 asks for). `APP_ENV=dev render-deploy-secrets.sh` correctly aborted, exit 1, before any `docker compose up` was attempted — zero new containers created; all already-running services (`db`, `backend`, `worker`) stayed healthy throughout, unaffected by OpenBao being down (their already-rendered tmpfs secrets don't need OpenBao to keep serving). Restarted `openbao-dev` afterward — it auto-unsealed via its persisted static-seal key with no manual step, and a follow-up render confirmed exit 0, fully recovered.)
- [x] T5.5 [deploy]: Break-glass drill (depends on T5.4, T4.4.1) (2026-07-21: DONE, scoped to one representative key (APISIX_ADMIN_KEY) rather than the full ~13-key table, by user's explicit choice — proves the procedure works with a real fetched value while bounding exposure. Fetched the live value from `secret/haisir/gateway` (deploy-cert auth, root-token-free, stdin-to-file, never echoed), wrote it as temporary plaintext into `dev/.env.config.sh` (gitignored/untracked, per T2.2.2 precedent), then stopped `openbao-dev`. Verified the mechanism the T4.4.1 runbook describes: with `OPENBAO_DEPLOY_SECRETS=false`, `render_deploy_secrets_or_die` short-circuits (exit 0) without ever needing OpenBao, and the restored plaintext resolves correctly — hash-verified equal to the live KV value (sha256, values never printed). **Methodology note**: first hash comparison showed a false mismatch — the exact same trailing-newline artifact T4.3.8 already documented (`jq -r | sha256sum` includes the piped newline; `printf '%s' | sha256sum` doesn't) — corrected with `jq -r -j` and re-verified equal, no real corruption. Restarted `openbao-dev` (auto-unseals via its persisted static-seal key, no manual step — confirmed the "unseal" side of this drill is trivial in this repo's setup, unlike a Shamir-shares seal). Cleaned up the temporary plaintext (removed from `dev/.env.config.sh`), re-ran T5.7's `full-plaintext-elimination-scan.sh` — PASS, exit 0. Stack (13 containers) confirmed healthy and untouched by the drill throughout, since `db`/`backend`/`worker` never depend on `openbao-dev` staying reachable once their own secrets are already rendered.)
- [x] T5.6 [deploy]: Restart-only path behaves (depends on T5.1) (2026-07-21: DONE. Against the same temporary common-project `keycloak` bring-up used for T5.3: `docker compose restart keycloak` → healthy, `RestartCount=0` (a plain restart of the same container, not a recreate). Then an incremental `up -d keycloak` with no config change → compose correctly left the container `Running` (no recreate), stayed healthy. Confirms the Class B conf-file mechanism survives both restart forms — matches the 5.5-precedented gotcha that rendered-file changes alone don't trigger a recreate (captured for T6.3's ops runbook).)
- [x] T5.7 [deploy]: Full plaintext-elimination scan (depends on T5.1) (2026-07-21: DONE. Added `common/scripts/tests/full-plaintext-elimination-scan.sh` — extends T3.7's Class-A-only, `.env.config.sh`-only scan to all Class A + Class B migrated key names across BOTH `.env` and `.env.config.sh` in dev/staging/prod, plus `TUNNEL_TOKEN` in `other/services/cftunnel/.env`. Name-only grep throughout, values never read. pgadmin's own vars are outside the migrated-key list by construction, so no explicit whitelist logic was needed — noted in the script's header for a future reader, pointing at the eventual decisions.md exclusion (T6.5). PASS, exit 0, on the current branch state. shellcheck clean.)
- [x] **G5: HARD GATE — Class B live verification** — end-to-end test (2026-07-21: PASS — all 7 tasks (T5.1–T5.7) done across one extended multi-session run against the live dev stack, same pattern as G3's own gate record. Highlights: T5.1 found and fixed a real bug (missing `group_add` on `keycloak`, first-ever real boot of common-project Keycloak in this sandbox); T5.2 proved fresh-volume first-init via a genuinely isolated compose project + brand-new volumes, real dev data never touched; T5.3/T5.6/T5.7 confirmed zero secret exposure and correct restart/incremental-update behavior; T5.4/T5.5 confirmed OpenBao's static-seal auto-unseal recovers with zero manual steps, and the break-glass plaintext-restore procedure works with a real fetched value (scoped to one key by design choice). Real dev stack (13 containers) confirmed healthy and unaffected at the end of every sub-task.)

## G6: Close-out — reviews, rotation, docs, merge

- [ ] T6.1 [deploy]: Security review pass 1 (automated) (depends on T5.1–T5.7)
- [ ] T6.2 [deploy]: Security review pass 2 (independent adversarial) (depends on T6.1)
- [ ] T6.3 [deploy]: Ops/rotation runbook additions — 7 items (depends on T5.1, T5.5)
- [ ] T6.4 [deploy]: Execute rotation of migrated secrets (dev) (depends on T6.3, T5.7)
- [ ] T6.5 [specs]: decisions.md entry for Phase 5.6 (depends on T4.1.1, T4.2.1, T5.3, T5.5)
- [ ] T6.6 [specs]: phases.md Phase 5.6 expansion (depends on T6.9)
- [ ] T6.7 [specs]: progress.md close-out (depends on T6.2, T6.9)
- [ ] T6.8 [specs]: 13_secrets_management.md KV-table audit (depends on T4.3.9, T2.7.2)
- [ ] T6.9 [deploy]: Merge to haisir-deploy main (depends on T6.1, T6.2, T6.3, T6.4)
- [ ] **G6: Close-out** — end-to-end test

- [ ] **ROOT: Full .env secrets elimination via OpenBao** — acceptance test

## Ready now
Tasks with no pending dependencies — can be started immediately:
- T6.1 [deploy]: Security review pass 1 (automated) (depends on T5.1–T5.7 ✓)
- T6.3 [deploy]: Ops/rotation runbook additions (depends on T5.1 ✓, T5.5 ✓)
- T6.5 [specs]: decisions.md entry for Phase 5.6 (depends on T4.1.1 ✓, T4.2.1 ✓, T5.3 ✓, T5.5 ✓)
- T6.8 [specs]: 13_secrets_management.md KV-table audit (depends on T4.3.9 ✓, T2.7.2 ✓)

**G5 (HARD GATE — Class B live verification) passed 2026-07-21** — all 7 tasks (T5.1–T5.7) done; see G5's own summary above. This unblocks G6's close-out phase: reviews, rotation, docs, merge. T6.2/T6.4/T6.9 still wait on T6.1/T6.3; T6.6/T6.7 wait on T6.9.
