# Progress

> Auto-generated from PLAN.md. Updated by `/implement` in each code repo.
> **Last baselined: backend:`19ee5d5` frontend:`53f573b` deploy:`5f817be` (2026-08-11)** — read directly from each sibling repo's HEAD. backend:`19ee5d5` lands T5.1/T5.2 (poller idle-in-transaction rollback on empty poll) and T5.10/T5.11 (`question_id` on `ExamReviewChatRequest`, narrowed exam-review grounding). The deploy G2 changeset (T2.1–T2.9 Minimus image migration + T2.2 pgvector-build deletion + G2 exec-form healthchecks) is committed in `ba52d17`; G2's E2E (stack-goes-healthy) is deferred to the next staging deploy. T4.1's OpenBao image pin landed in `eb6516a`; T4.2–T4.9 and the G3 monitoring/Grafana changeset have since landed on top of it. T6.1.4's `rotate-secret.sh` stdin-bug fix landed in `1ec64b2`. T5.8's port-driver/version-pin doc changes landed in `7522757`, its slirp4netns-pin follow-up in `e4e3461`, its checksum-bug-fix + Compose/Buildx-pin follow-up in `a833ba1`, its Compose-version-typo fix in `1556e07`, its sandbox/detach-netns crash fix (verified live end-to-end on dev) in `3873876`. T5.6's certbot-hook drift check landed in `6fafb29`, T5.7's hook-mode assertion in `c8aee7c`. T3.3 + T3.5 + T3.6 (2026-08-12) landed in `5f817be`, together with their `/review-deploy` fixes and a gateway image-tag default bump to `v2026.7` — T3.3 carries a live defect fix (APISIX's metrics export server was bound to container-loopback, so Prometheus could never scrape it), and T3.6 closes G3's last child. **Deferred by owner call 2026-08-12**: T3.6's five `ALERT_*` SMTP keys are not seeded and their `deploy-required-keys.txt` entries are left commented out, so no unrelated staging/prod deploy is blocked; seeding them + uncommenting is the single outstanding action before G3 can close, and belongs in the `pre_checks:` of the release manifest that first ships the monitoring profile.
>
> **Phase 7.5 — Minimus Container Images + Phase 7 Deploy Backlog.** Scoped 2026-08-09 via `/plan`,
> two challenger rounds. 87 tasks across four repos. Goal tree, per-task Build/Done-when/Test and
> the five decisions taken at scoping are in `PLAN.md`.
>
> **Start with T6.1.1 and T1.1.** T6.1.1 unblocks all of G6 and is safe alone; T1.1 unblocks all of
> G1–G4. G6.2 (the Keycloak admin console answering 200 from the public internet) is reachable in
> three tasks and never queues behind image work.
>
> Bolded rows are goal/subgoal-level tests — they can only pass once every child task above them is
> checked, and several are satisfied by a named task rather than a separate run (G1→T1.7, G2→T2.8,
> G4→T4.11, G5→T5.12).


## G1: Application images build and boot from pinned Minimus bases

- [x] T1.1 [specs]: Pull and record the current Minimus migration workflow
- [x] T1.2 [backend]: Backend Dockerfile to Minimus, both stages (depends on T1.1) (2026-08-10)
- [x] T1.3 [backend]: Backend runtime container boots on the Minimus base (depends on T1.2) (2026-08-10)
- [x] T1.4 [frontend]: Frontend Dockerfile to Minimus, both stages (depends on T1.1) (2026-08-10)
- [x] T1.5 [frontend]: Frontend runtime container boots on the Minimus base (depends on T1.4) (2026-08-10)
- [x] T1.6 [deploy]: Reconcile the hardcoded 65532 UID against the Minimus images (depends on T1.3, T1.5) (2026-08-10)
- [ ] T1.7 [deploy]: Boot the full application stack on the migrated bases (depends on T1.6) — BLOCKED: no local environment runs `common/docker-compose.yml`'s backend/worker/frontend (needs OpenBao-rendered secrets + real DB); verify on the next staging deploy carrying T1.2-T1.6, via `docker compose -f common/docker-compose.yml up -d backend worker frontend && sleep 90 && [ "$(docker compose -f common/docker-compose.yml ps --format '{{.Name}} {{.Health}}' | grep -vc healthy)" = 0 ]`
- [ ] **G1: Application images build and boot from pinned Minimus bases** — E2E test

## G2: Infrastructure services run on pinned, hardened images

- [x] T2.1 [deploy]: App Postgres to the standalone Minimus pgvector image (depends on T1.1) (2026-08-10) — three `image:` lines (`common/docker-compose.yml` db-init/db, `dev/docker-compose.yml` postgres) set to `reg.mini.dev/pgvector:${POSTGRES_IMAGE_TAG}` (version held in the existing `POSTGRES_IMAGE_TAG` variable, same `*_IMAGE_TAG` pattern as backend/frontend/keycloak; the value the `deploy.sh` drift loop compares the running container against). **Operator must set `POSTGRES_IMAGE_TAG=0.8.6-pg18`** in `staging/.env` and `prod/.env` (change the existing built-image-tag value) and **add** it to `dev/.env` (dev had no such var — it used a hardcoded `pgvector/pgvector:0.8.2-pg18-trixie`). **Plan's prescribed `:18` tag does not exist** on Minimus; SELECT TAG re-verification (which the plan itself requires) found the real scheme is `0.8.x-pg18`, so the variable value is `0.8.6-pg18`. INSPECT confirmed pgvector 0.8.6 (≥ 0.8.4, no downgrade), shell + `pg_isready`/`psql` present, image default user `0` dropping to `postgres` uid **999**. UID/ownership reconciliation (Chainguard `70` → Minimus `999`) deliberately left to T2.3, so T2.1 alone must not reach staging/prod. T2.1's test is dev-only (dev has no `user:` override; both old and new use uid 999 → no volume wipe). Runtime test not run here — dev stack is off-limits to start; operator to verify after setting the env var: `docker compose -f dev/docker-compose.yml up -d postgres && docker compose -f dev/docker-compose.yml exec postgres psql -U postgres -tAc "SELECT extversion FROM pg_extension WHERE extname='vector'"` (expect `0.8.6`).
- [x] T2.2 [deploy]: Delete the from-source pgvector build (depends on T2.1) (2026-08-10) — `git rm -r postgres-docker/` (Dockerfile + Jenkinsfile + test.sh): the Wolfi builder stage, from-source pgvector compile, `POSTGRES_BASE_IMAGE=cgr.dev/chainguard/postgres:latest` default + its `checkov:skip`, and the **standalone `haisir-postgres` build-and-push Jenkins pipeline** — that pipeline IS "the Jenkins build for the postgres image"; all `POSTGRES_VERSION`/`PGVECTOR_VERSION`/`POSTGRES_BASE_IMAGE` build-arg threading lived only in `postgres-docker/Jenkinsfile`, deleted with the dir. The spec said "remove the `haisir-postgres` build-and-push stage from `Jenkinsfile`" but the root `Jenkinsfile` is a validation/lint pipeline with no such stage — the build was `postgres-docker/Jenkinsfile` (a standalone multibranch job), so no root-`Jenkinsfile`/`Jenkinsfile.deploy` edit was needed. Stale-reference sweep (the "everything dead must remove" pass): `common/docker-compose.yml` `db-init` comment — T2.1's "that build is deleted in T2.2" forward-ref — reworded to past tense; `.claude/skills/release-manifest/SKILL.md` Postgres-detection rewritten (was `git diff HEAD~1 -- postgres-docker/Dockerfile`, now a dead path) to point at `other/env_templates/.env.template` + the compose `db`/`db-init` image line; `other/env_templates/.env.template` `POSTGRES_IMAGE_TAG=18.4` (the dead built-image tag scheme) → `0.8.6-pg18` (T2.1 leftover). **KEPT**: `POSTGRES_IMAGE_TAG` in `{env}/.env`, `deploy.sh`'s drift loop (`postgres:POSTGRES_IMAGE_TAG:haisir-db-${ENV}`) + the `POSTGRES_IMAGE_TAG_OVERRIDE` manifest-override plumbing (T6.5.x owns that), `env-setup.sh`'s hard-requirement gate, and `common/openbao/*`'s `haisir-postgres` — the **OpenBao database secrets-engine config path/role name** (`database/config/haisir-postgres`, `database/rotate-root/haisir-postgres`, `db_name=haisir-postgres`), a live logical identifier, NOT the docker image (renaming would need a bootstrap re-run + root-cred rotation); `releases/v2026.4/manifest.yaml` (immutable historical record). Done-when test `! grep -rq 'haisir-postgres' Jenkinsfile Jenkinsfile.deploy common/scripts/` → exits 0; `yamllint` + `docker compose -f common/docker-compose.yml config --quiet` (with CI placeholders) pass. **Operator action (out of repo)**: deactivate/remove the `haisir-postgres` multibranch Jenkins job in the Jenkins UI — deleting the Jenkinsfile removes the pipeline definition from the repo, but the job registration lives in Jenkins config. No runtime test — pure deletion, no image to boot.
- [x] T2.3 [deploy]: Re-verify the Postgres data-directory ownership workaround (depends on T2.1) (2026-08-10) — INSPECT of `reg.mini.dev/pgvector:0.8.6-pg18` confirmed Minimus `postgres` uid = **999** (Chainguard was 70; uid 70 absent in the image), standard `docker-entrypoint.sh` that chowns as root only when run as root then `gosu`-drops to 999. Reconciled `common/docker-compose.yml` app-db services: `db-init` entrypoint `chown -R 70:70` → `chown -R 999:999` (×2), `db` `user: "70"` → `user: "999"`, comments updated. No volume-type change needed — the named-volume + db-init pre-chown workaround still applies (db runs as 999, finds PGDATA already 999-owned); tmpfs `/var/run/postgresql` defaults to mode 1777 so the socket dir stays writable. Runtime test (boots `common/docker-compose.yml` db, needs OpenBao secrets) deferred to the next staging deploy.
- [x] T2.4 [deploy]: keycloak-db Postgres to Minimus (depends on T2.3) (2026-08-10) — INSPECT of `reg.mini.dev/postgres:18` confirmed it exists (unlike the pgvector `:18` tag), **no `-hardened` variant** (BR-INFRA-003 → plain base), `postgres` uid = **999**, same standard entrypoint, shell + `pg_isready` present. Swapped both `keycloak-db-init` and `keycloak-db` from `cgr.dev/chainguard/postgres:latest` (rolling tag) → `reg.mini.dev/postgres:${KEYCLOAK_POSTGRES_IMAGE_TAG}` (version in a new `.env` var, kept separate from the app DB's `POSTGRES_IMAGE_TAG` since it's a different image; chosen over a full `POSTGRES_IMAGE_TAG`→`PGVECTOR_IMAGE_TAG` rename to avoid touching `deploy.sh`'s drift loop and `env-setup.sh`'s hard-requirement gate, which T2.2/T6.5 own). Applied the same T2.3 UID reconciliation: `chown -R 70:70` → `999:999` (×2 in init), `user: "70"` → `user: "999"`. `group_add: "1000"` (openbao secrets) unchanged. **Operator must set `KEYCLOAK_POSTGRES_IMAGE_TAG=18`** in `staging/.env` and `prod/.env` (dev has no keycloak-db). Runtime test (boots keycloak-db, needs OpenBao secrets) deferred to the next staging deploy.
- [x] T2.5 [deploy]: APISIX runtime stage to Minimus (depends on T1.1) (2026-08-10) — `gateway-docker/Dockerfile` runtime `FROM apache/apisix@${APISIX_DIGEST}` → `FROM reg.mini.dev/apache-apisix:${APISIX_VERSION}` (pinned by the `APISIX_VERSION` tag ARG, not a literal — deleted `ARG APISIX_DIGEST`; kept a reworded `checkov:skip=CKV_DOCKER_7` since checkov can't resolve ARG-substituted FROM tags); updated `ARG APISIX_VERSION` `3.17.0-ubuntu` → `3.17.0` and the *_VERSION-record comment. `gateway-docker/Jenkinsfile`: deleted the `apisixDigest` grep, added an `apisixTag` grep of `ARG APISIX_VERSION=` (mirrors the `goDigest` pattern) so `'pull apisix'` pulls `reg.mini.dev/apache-apisix:${apisixTag}` in sync with the FROM (kept `'pull golang'` digest-pull — T2.9 owns `GO_BUILDER_DIGEST`). `gateway-docker/UPGRADE-RUNBOOK.md`: base-image paragraph now records only the Go builder as digest-pinned, APISIX as tag-pinned via the ARG. INSPECT confirmed apisix uid 636 + shell present (runtime `USER apisix`/`chown` unchanged). `hadolint` clean of new findings (3 pre-existing — DL3062 go-install, 2× DL3066 named-user — none from this change). **Runtime test deferred to the Jenkins WAF Functional Gate** (`waf-harness.sh` CVE-2026-21876 → rule 922110) — the multi-stage mage/tinygo/go build isn't reproduced locally.
- [x] T2.6 [deploy]: Keycloak to one pinned Minimus tag across dev and prod (depends on T1.1) (2026-08-10) — `common/docker-compose.yml` keycloak `quay.io/keycloak/keycloak:${KEYCLOAK_IMAGE_TAG}` → `reg.mini.dev/keycloak:${KEYCLOAK_IMAGE_TAG}`; `dev/docker-compose.yml` `keycloak/keycloak:26.6` → `reg.mini.dev/keycloak:${KEYCLOAK_IMAGE_TAG}` (dev switched from a hardcoded tag to the var). INSPECT confirmed `reg.mini.dev/keycloak:26.7.1` exists, default user uid **1000** (matches existing `user: "1000"`, no UID reconciliation), `keycloak.conf` mount path stable across 26.6→26.7.1. **Operator set `KEYCLOAK_IMAGE_TAG=26.7.1`** in dev/.env (add), staging/.env + prod/.env (change). ⚠ **Healthcheck `CMD-SHELL /dev/tcp` BREAKS on Minimus keycloak** — its shell is busybox (no `/dev/tcp`) and the image has no `curl`/`wget`/`nc` (busybox applets stripped, no `openssl`); only `java`+`kc.sh` are present, no health subcommand. Since `apisix` hard-depends on `keycloak: service_healthy` (common :599-600), a deploy carrying T2.6 without T2.8 leaves keycloak never-healthy → apisix won't start. **T2.8 (now Ready) must land before any staging/prod deploy carrying T2.6.** Runtime test deferred to the next staging deploy (dev stack off-limits to start).
- [x] T2.7 [deploy]: etcd to Minimus (depends on T1.1) (2026-08-10) — `common/docker-compose.yml` etcd `quay.io/coreos/etcd:${ETCD_IMAGE_TAG}` → `reg.mini.dev/etcd:${ETCD_IMAGE_TAG}`; `dev/docker-compose.yml` `quay.io/coreos/etcd:v3.6.11` → `reg.mini.dev/etcd:${ETCD_IMAGE_TAG}` (dev switched from hardcoded `v3.6.11` to the var — Minimus tags carry no `v` prefix). INSPECT confirmed `reg.mini.dev/etcd:3.7.1` exists, default user uid **1000** (matches existing `user: "1000"`), `etcdctl 3.7.1` present. 3.7.1 is an **upgrade** (new minor over dev's 3.6.11), within the plan's no-downgrade rule. etcd:3.7.1 ships **no shell**, but the healthcheck is exec-form `["CMD","etcdctl","endpoint","health"]` → survives unchanged. `etcd-init`'s `alpine:latest` left to T4.7. **Operator set `ETCD_IMAGE_TAG=3.7.1`** in dev/.env (add), staging/.env + prod/.env (change). Runtime test deferred to the next staging deploy.
- [x] T2.8 [deploy]: Audit shell-dependent healthchecks across the migrated services (depends on T2.4, T2.5, T2.6, T2.7) (2026-08-10) — INSPECT of the five Minimus G2 images: `reg.mini.dev/pgvector:0.8.6-pg18` and `reg.mini.dev/postgres:18` carry `/usr/bin/pg_isready` + a busybox shell; `reg.mini.dev/apache-apisix:3.17.0` carries `/bin/test` (coreutils) + busybox shell; etcd's healthcheck was already exec-form `etcdctl` (T2.7, no shell but exec-form survives). **Keycloak was the real breakage**: `reg.mini.dev/keycloak:26.7.1` is a **JRE-only** image (no `jdk.compiler`), busybox shell with **no `nc`/`wget`/`curl`/`openssl`**, no bash `/dev/tcp`, `kc.sh` has no health subcommand, `java --source` fails ("Module jdk.compiler not in boot Layer") and `jrunscript -e` fails (Nashorn removed in Java 21) — so the old `CMD-SHELL exec 3<>/dev/tcp/localhost/8443` never reported healthy and apisix (`depends_on: service_healthy`) never started (the breakage T2.6 flagged). Converted all five to exec form using only verified-present binaries: db/keycloak-db → `["CMD","pg_isready","-U","${...}","-d","${...}"]`; apisix → `["CMD","test","-S","/usr/local/apisix/logs/worker_events.sock"]`; **keycloak → `["CMD","grep","-qE",":20FB 0+:0000 0A","/proc/net/tcp","/proc/net/tcp6"]`** — 8443 = hex `20FB`, the pattern matches a LISTEN-state socket (rem_addr all-zeros + state `0A`) so lingering TIME-WAIT sockets can't false-healthy; **`/proc/net/tcp6` is required** because Java binds IPv4-mapped IPv6 (the socket appears in tcp6, not tcp). Same fix in `dev/docker-compose.yml` (keycloak HTTP port 8080 = hex `1F90`). etcd unchanged. **Isolated mechanism tests run** (full stack-goes-healthy test deferred to next staging deploy — common stack needs OpenBao-rendered secrets, dev stack off-limits to start, same pattern as T2.1–T2.7): pg_isready exec form → 3× exit 0 against a throwaway pgvector container (binary also confirmed in postgres:18); the keycloak `/proc` grep → detects a jwebserver listener on 8443 (matched in tcp6), rejects no-listener, works as `--user 1000`; `/bin/test -S` negatives correct (non-socket and missing path → exit 1; positive socket case relies on coreutils POSIX `test -S`, the same check already healthy in prod today). Vault-agent sidecar `CMD-SHELL` healthchecks (5, in common) deliberately left as-is — they run on the unmigrated `ghcr.io/openbao/openbao` image (T4.1 owns that migration) and are not G2 services.
- [x] T2.9 [deploy]: Bring the Go builder digest to BR-INFRA-004 parity (depends on T2.5) (2026-08-10) — **Verified parity holds; no file changes.** `docker run --rm reg.mini.dev/go@${GO_BUILDER_DIGEST} go version` resolves `sha256:8ebfe4dd…` to **go1.25.12**; `gateway-docker/coraza-proxy-wasm/go.mod` declares `go 1.25.0`; `ARG GO_VERSION=1.25`. All on the 1.25 line. The plan's exact test command passes (both sides extract `1.25`). Per BR-INFRA-004, bumping nothing is the correct outcome — the task forbids bumping the digest alone, and the digest, `GO_VERSION` and `go.mod` agree, so there is nothing to bump together.
- [ ] **G2: Infrastructure services run on pinned, hardened images** — E2E test — BLOCKED: all children T2.1–T2.9 now done, but the stack-goes-healthy E2E (T2.8's deferred runtime test) needs a staging deploy carrying the full G2 changeset with OpenBao-rendered secrets (dev stack off-limits to start, common stack needs OpenBao); verify on the next staging deploy via `sleep 120 && [ "$(docker compose -f common/docker-compose.yml ps --format '{{.Name}} {{.Health}}' | grep -E 'postgres|keycloak|etcd|apisix' | grep -vc healthy)" = 0 ]`

## G3: Monitoring is live and alerts fire

- [x] T3.1 [deploy]: Add the Prometheus + exporters compose services (depends on T1.1) (2026-08-10) — added `prometheus`, `alertmanager`, `node-exporter`, `postgres-exporter`, `nginx-prometheus-exporter` to `common/docker-compose.yml` behind a `monitoring` profile (not started by a plain `up`), plus two local (non-external) data volumes. **Plan's "3.11 line" guess was stale** — live registry query (anonymous bearer token against `auth.mini.dev`, `docker manifest inspect` for confirmation) found current stable tags: `prometheus:3.13.2`, `alertmanager:0.33.1`, `node-exporter:1.12.1`, `postgres-exporter:0.20.1`, `nginx-prometheus-exporter:1.5.1`. **Repo-name gotcha**: `reg.mini.dev/alertmanager`, `/node-exporter`, `/postgres-exporter` all resolve (200) but are empty — the real repos are prefixed `prometheus-alertmanager`, `prometheus-node-exporter`, `prometheus-postgres-exporter`; found by testing after the bare names returned zero tags rather than a 404. All five image:tag pairs pulled and `docker inspect`'d live (User/Entrypoint/ExposedPorts), not guessed. Mounts the existing `common/prometheus/prometheus.yml` read-only. `node-exporter` gets the standard host-visibility mounts (`/proc`, `/sys`, `/` ro) + matching `--path.*` flags — without them it only ever sees the container's own filesystem. **Deliberately left unwired**: `postgres-exporter`'s `DATA_SOURCE_NAME` is a bare required var (operator-supplied DSN) — no OpenBao dynamic-credential wiring, that's bigger than T3.1's stated scope; `alertmanager` has no config mount yet (`alertmanager.yml` is T3.6's job, runs on image-default config until then); `nginx-prometheus-exporter`'s `--nginx.scrape-uri` defaults to APISIX's status endpoint, but enabling that endpoint on APISIX itself is not part of T3.1. New `*_IMAGE_TAG` vars + `POSTGRES_EXPORTER_DSN` placeholder documented in `other/env_templates/.env.template`. Validated: `docker compose --env-file staging/.env config --quiet` clean (0 project-caused errors); `--profile monitoring config --services` lists all 18 services (13 existing + 5 new) vs 13 on the default profile; `yamllint` clean on new lines. Runtime test (`curl :9090/-/ready`) not run — no local environment boots `common/docker-compose.yml` (needs OpenBao-rendered secrets), same limitation as every G1/G2 task; deferred to the next staging deploy.
- [x] T3.2 [deploy]: Add the Grafana compose service (depends on T3.1) (2026-08-11) — added `grafana` service to `common/docker-compose.yml`'s `monitoring` profile at `reg.mini.dev/grafana:13.1.3` (owner-chosen over the initially-verified `13.0`; re-checked live — same uid 1000, shell present, `/opt/grafana/run.sh` entrypoint, `3000/tcp` exposed. `curl` to `auth.mini.dev`/`reg.mini.dev` failed from the assistant's sandbox, but `docker pull` worked, so no operator hand-off was needed for tag verification). Mounts the existing `grafana.ini` + `provisioning/` read-only, new named volume `haisir-grafana-data`, `127.0.0.1:3001:3000`, same hardening pattern (`security_opt`/`cap_drop`/`mem_limit`/etc.) as the other five monitoring services. `GF_SECURITY_ADMIN_PASSWORD` sourced from a new required `GRAFANA_ADMIN_PASSWORD` env var — plain `.env` var, not OpenBao-delivered; see backlog item **B8** in `phases.md` for why the proper Vault-Agent fix was scoped out as its own follow-up rather than folded in here or given a new task ID outside `/plan`. `GRAFANA_IMAGE_TAG`/`GRAFANA_ADMIN_PASSWORD` added to `other/env_templates/.env.template`. **Found + fixed while wiring this up**: `common/grafana/provisioning/datasources/datasources.yaml`'s Prometheus datasource pointed at `http://apisix-prometheus:9090`, a hostname that never matched the real compose service name (`prometheus`) — broken since the file was written, predating T3.1. Corrected to `http://prometheus:9090`. `docker compose --profile monitoring config --services` lists `grafana` alongside the rest of the monitoring stack with all vars stubbed; `yamllint` clean. Runtime test (`curl localhost:3001/login` = 200) not run — same limitation as every other monitoring-profile task, no local environment boots `common/docker-compose.yml` with OpenBao-rendered secrets; deferred to the next staging deploy.
- [x] T3.3 [deploy]: Turn the skipped Prometheus test into a live gate (depends on T3.2) (2026-08-12) — **the plan's premise was stale on three counts, and one was a live defect.** (1) `common/scripts/tests/config.sh` has **no `dev` environment** — `:29` hard-exits unless `ENV` is `staging`/`prod`/`harness`, so the plan's own test (`! ENV=dev bash 13-test-prometheus.sh | grep -q 'not configured'`) would have exited 0 **because the script dies before printing anything** — a false green, not a gate. (2) There is no dev Prometheus to point at: T3.1/T3.2 put the monitoring profile in `common/docker-compose.yml` (what staging/prod run), not `dev/docker-compose.yml`, and the dev stack is off-limits to start so a dev gate could never be verified. (3) **Real defect found and fixed**: `common/apisix_conf/config.yaml:126-129` bound the metrics export server to `127.0.0.1:9091` *inside APISIX's own network namespace*, and 9091 was published nowhere — so `common/prometheus/prometheus.yml`'s `apisix:9091` scrape job could **never** connect. That target was permanently DOWN, meaning T3.4's `TargetDown` alert would have fired forever and the pre-existing `apisix-overview.json` dashboard would have rendered empty; nothing in T3.1's or T3.4's own validation could see it, because `promtool check config` validates syntax, not reachability. Fixed `export_addr.ip` `127.0.0.1` → `0.0.0.0` in `common/apisix_conf/config.yaml` and `dev/apisix_conf/config.yaml` (plus the untracked, hand-maintained `dev/apisix_conf/.templated/config.yaml` that dev actually mounts, so dev doesn't silently diverge). Published `127.0.0.1:9091:9091` on apisix in `common/docker-compose.yml` (loopback-only, deliberately NOT env-configurable — the only host-side consumer is this test) and `9091:9091` in `dev/docker-compose.yml`. `config.sh` `:84`/`:115`: `DEFAULT_PROMETHEUS_URL`/`DEFAULT_APISIX_METRICS_URL` now resolve to `http://localhost:9090`/`:9091` **under the existing `LOCAL_TESTS=true` gate** — the same mechanism `DEFAULT_ADMIN_URL` already uses for host-only endpoints — for staging *and* prod, staying empty on a remote run and for `harness`. **Security calls, surfaced and approved before implementing**: `0.0.0.0` exposes APISIX metrics (route names, status codes, latencies — operational data, no secrets) to siblings on `haisir-net`, which is the only way Prometheus can scrape it; the host binding is loopback-only, same exposure class as the APISIX admin port. **Rejected**: adding a `dev` branch to `config.sh` + the monitoring profile to `dev/docker-compose.yml` — a whole new env surface (realm, client id, secrets, gateway URL) for an env the suite never supported, on a stack that can't be started to verify it. **Verified live**: the `LOCAL_TESTS` gate sets/clears both URLs correctly on staging and prod (4 permutations); with the gate on, the test no longer prints "not configured" and reports a real pass/fail count (`SUMMARY: Prometheus Metrics|1|2`, exit 1 off-host where nothing listens — that *is* the gate) while `LOCAL_TESTS=false` still skips cleanly (exit 0), so remote suite runs are unchanged; the adapted done-when (`! ENV=staging LOCAL_TESTS=true bash 13-test-prometheus.sh | grep -q 'not configured'`) exits 0. Both compose files `config --quiet` clean under a stub env and render `127.0.0.1:9091->9091` (common) / `9091->9091` (dev). `shellcheck` clean on `config.sh`; `yamllint` clean (remaining warnings pre-existing style). ⚠ **Behaviour change the deploy must account for**: `13-test-prometheus.sh:60-62` **hard-fails and exits 1** if the metrics endpoint is not 200, so the staging/prod deploy carrying this must also re-render the APISIX config and recreate the apisix container, or a `LOCAL_TESTS=true` suite run will fail the whole category. Full runtime verification (test green on a host) deferred to that deploy — no local environment boots `common/docker-compose.yml`, same limitation as every G1/G2/G3 task.
- [x] T3.4 [deploy]: Write the alert rules (depends on T3.1) (2026-08-11) — added `common/prometheus/rules/haisir.rules.yml` (`TargetDown`, `ApisixHighErrorRate`, `PostgresIdleInTransaction`, `DiskSpaceLow`, each carrying a `severity` label and `summary` annotation), referenced via a new `rule_files` entry in `prometheus.yml`. Also wired two necessary corollaries T3.1 had left unwired: an `alerting:` stanza pointing Prometheus at `alertmanager:9093` (alertmanager was running but received nothing before this) and scrape jobs for `node-exporter`/`postgres-exporter` (deployed by T3.1, never added as scrape targets — the disk/postgres rules would otherwise have no data). `prometheus`'s compose mount switched from the single `prometheus.yml` file to the whole `./prometheus:/etc/prometheus:ro` directory so the container can see `rules/`. `docker run --entrypoint promtool reg.mini.dev/prometheus:3.13.2 check rules` → `SUCCESS: 4 rules found`; `check config` validates both files. **Deferred, not delivered**: the plan's other four named failure modes (backend/worker/frontend/keycloak service-down, certificate-expiry-in-21-days) have no metric source anywhere in the stack today — none of those four expose `/metrics`, and no exporter produces cert-expiry data. Writing rules against metrics that will never populate would `promtool`-validate but never fire — recorded as backlog item **B7** in `phases.md` (needs a new `blackbox_exporter` service, a scope decision beyond a rules-file edit) plus a `decisions.md` entry (2026-08-11), rather than silently under-delivering or unilaterally adding a new exporter service. `yamllint` clean.
- [x] T3.5 [deploy]: Provision the dashboards (depends on T3.2) (2026-08-12) — three net-new dashboards in `common/grafana/provisioning/dashboards/json/`: `host-overview.json` (8 panels — CPU, memory, disk, load, filesystem, network, from node-exporter), `postgres-overview.json` (9 panels — idle-in-transaction, connections, cache hit ratio, locks, sessions by state, transaction/deadlock rate, autovacuum age, dead tuples) and `application-overview.json` (9 panels — request rate, 5xx ratio, latency percentiles, per-route and upstream latency, job-table rows). No provider change needed: the existing `dashboards.yaml` already points at that directory with `updateIntervalSeconds: 10`, so Grafana hot-reloads them from disk without a restart. Four dashboards total, satisfying the done-when. **Metric availability verified, not assumed**: ran `--help` live against `reg.mini.dev/prometheus-postgres-exporter:0.20.1` to confirm the `stat_activity`, `stat_database`, `stat_user_tables`, `locks` and `replication` collectors are all **default-enabled** — every Postgres panel uses a series from one of those, and the idle-in-transaction panel deliberately uses the *same* `pg_stat_activity_count{state="idle in transaction"}` series as T3.4's `PostgresIdleInTransaction` rule so alert and dashboard cannot disagree. Job table names (`extraction_jobs`, `essay_grading_jobs`) read from the backend's own migrations (V26, V29), not guessed. **Deliberately under-delivered against the plan's wording, and marked as such in the dashboards themselves**: (a) the plan asks for "backend/worker request rate, latency, error rate" — backend, worker and frontend expose **no `/metrics` endpoint** (already recorded as backlog **B7** by T3.4), so the application tier is observed from the **edge** via APISIX per-route series rather than from application instrumentation that does not exist; (b) "queue depth for the two poller job tables" is served by `pg_stat_user_tables_n_live_tup`, an **approximate whole-table row estimate that is not filtered by job status** — postgres-exporter 0.20 dropped the user-defined-query mechanism, so real status-filtered depth needs B7's backend exporter. Both panels carry an explicit `description` saying so in the Grafana UI, rather than looking authoritative and being wrong; (c) **no replication-lag panel** — this is a single-node deployment with no standby, so such a panel would be empty for want of a replica, not for want of a metric; vacuum lag (the other half of the plan's "replication/vacuum lag") is delivered. **Validated**: all four dashboards `jq`-parse with unique `uid`s; **all 43 PromQL expressions across all four dashboards** (including the 7 pre-existing `apisix-overview.json` ones) extracted into a synthetic rules file and run through `promtool check rules` on `reg.mini.dev/prometheus:3.13.2` → `SUCCESS: 43 rules found`, so no panel ships an expression Prometheus cannot parse. Runtime test (`curl -u admin:… localhost:3001/api/search?type=dash-db | jq length` ≥ 4) not run — needs the monitoring profile up with OpenBao-rendered Grafana credentials; deferred to the next staging deploy, same limitation as T3.1/T3.2/T3.4. **Operator note**: the `monitoring` profile is opt-in (`docker compose --profile monitoring up -d`) — nothing in `deploy.sh` starts it.
- [x] T3.6 [deploy]: Route alerts to the owner-supplied destination (depends on T3.4, T3.5) (2026-08-12) — **owner input taken at implementation time, as the plan requires: email/SMTP, one destination for both tiers, severity-split routing.** New `common/prometheus/alertmanager.yml` (a *template*, not the file Alertmanager reads) with a route tree keyed on the `severity` label T3.4 actually wrote — `critical` → `haisir-critical` (`repeat_interval: 1h`), `warning` → `haisir-warning` (12h), and the top-level `receiver:` deliberately set to `haisir-warning` rather than left as a dead end so a future rule whose severity matches neither sub-route still reaches a human. Five values (`ALERT_SMTP_HOST` as host:port, `ALERT_SMTP_FROM`, `ALERT_SMTP_USER`, `ALERT_SMTP_PASSWORD`, `ALERT_EMAIL_TO`) are `{{PLACEHOLDER}}`s resolved from `secret/haisir/infra` per BR-SEC-022. `common/scripts/template-configs.sh` gained a rendering block (reusing `replace_placeholders` verbatim — its CIDR/JSON branch is gated on `.json` so a `.yml` just gets substitution + `chmod 600`) writing to `common/prometheus/.templated/$APP_ENV/alertmanager.yml`, **and — the load-bearing part — that directory was added to the existing unresolved-secret-placeholder grep**, which is what makes an unseeded destination abort the render. All five keys are written into `common/openbao/deploy-required-keys.txt` as `infra:…:envs=staging,prod` (dev excluded: it does not run the monitoring profile) but **COMMENTED OUT — the fail-closed gate is deliberately NOT armed yet (owner call, 2026-08-12)**: the SMTP values are not available, and armed entries apply to *every* staging/prod deploy, so they would abort deploys that never touch monitoring (T5.12, T6.1.5 and T6.2.2 all need a live staging run) on an inapplicable secret — precisely the trap the existing T2.7.2/`TUNNEL_TOKEN` entry in that file already documents. **This is not a fail-open, and that was verified rather than assumed**: `amtool check-config` on the *unrendered* template fails outright — `FAILED: address {{ALERT_SMTP_HOST}}: missing port in address` — so Alertmanager crash-loops instead of starting with a route that silently drops every alert. Until the keys are seeded the only cost is that `template-configs.sh` emits a WARNING for the unresolved placeholders instead of aborting, and the (opt-in, never started by `deploy.sh`) monitoring profile cannot come up. Confirmed the disarm is clean: the active, non-comment entries in the manifest are byte-identical to HEAD, and `manifest_entries` returns 0 `ALERT_*` keys on all three envs. No `render-deploy-secrets.sh` change needed — `infra` is already in its path loop (`:196`), so the keys emit automatically once seeded. `common/docker-compose.yml`: a new profile-gated `alertmanager-init` (busybox, `user: root`) stages the rendered config into a new local volume `haisir-alertmanager-conf`, and alertmanager mounts that volume at `/etc/alertmanager` behind `condition: service_completed_successfully` (the image ships only `alertmanager.yml` there and its default CMD already points `--config.file` at it, so replacing the directory is safe and no command override is needed); the stale "No config mount yet — T3.6" comment is gone. **The first attempt bind-mounted the rendered file directly and was broken** — caught by `/review-deploy`, see the fix note below. **Security gap found and closed while wiring this up**: T3.4 had widened prometheus's mount to the whole `./prometheus:/etc/prometheus:ro` directory so it could see `rules/` — which would have put the newly-rendered `alertmanager.yml`, *including the SMTP password*, inside a container that has no business reading it. Narrowed to two explicit mounts (`prometheus.yml` + `rules/`), so `.templated/` is no longer inside anything prometheus mounts. **Verified live, not assumed**: `amtool check-config` on a fully-rendered copy → `SUCCESS`, 2 receivers, route parsed (`/bin/amtool` confirmed present in the otherwise fully-distroless `reg.mini.dev/prometheus-alertmanager:0.33.1`); `amtool config routes test` resolves `severity=critical` → `haisir-critical`, `severity=warning` → `haisir-warning`, and both `severity=info` and a bare `alertname=…` → `haisir-warning`, proving the default receiver is not a dead end; the substitution regex `{{[A-Z_]*}}` matches exactly the 5 keys and **cannot** touch Alertmanager's own Go-template expressions (`{{ .CommonLabels.alertname }}`, `{{ .Alerts.Firing | len }}`) since those start with a space and a dot; `manifest_entries` returns 5 ALERT_ keys on staging and prod, 0 on dev; and the fail-closed path was **simulated end to end** — an unrendered config makes the check abort naming all five keys (names only, never values, per the script's existing contract) while a rendered one proceeds. `shellcheck`/`bash -n` clean on `template-configs.sh`; `yamllint` clean; `docker compose --profile monitoring config --quiet` clean under a stub env, with both mounts resolving to the expected paths. ⚠ **OPERATOR ACTION, DEFERRED TO THE STAGING MONITORING ROLLOUT — two steps that must happen together**: (1) seed `ALERT_SMTP_HOST` (host:port), `ALERT_SMTP_FROM`, `ALERT_SMTP_USER`, `ALERT_SMTP_PASSWORD` and `ALERT_EMAIL_TO` into `secret/haisir/infra` on staging **and** prod (`rotate-secret.sh` under the `admin-ops` cert identity, same route as T6.1.3/T6.1.4); (2) **uncomment the five entries in `common/openbao/deploy-required-keys.txt`** in the same change, which arms the fail-closed gate. Doing (1) without (2) leaves the gate disarmed; doing (2) without (1) aborts every staging/prod deploy. The checklist is written at the top of that file's T3.6 block, and this belongs in the `pre_checks:` of whichever release manifest first ships the monitoring profile. **The plan's own test is not runnable as written**: it says stop the backend and assert a `BackendDown` alert — no such rule exists, because T3.4 correctly declined to write one (backend exposes no `/metrics`, so it is not a scrape target and stopping it produces no alert at all — backlog B7). The runnable substitute against a real target: stop `node-exporter`, wait past `TargetDown`'s `for: 2m`, then `curl -s localhost:9093/api/v2/alerts | jq '[.[] | select(.labels.alertname=="TargetDown")] | length'` ≥ 1, and confirm the mail actually arrives. Deferred to the next staging deploy — no local environment boots `common/docker-compose.yml`, same limitation as every G1/G2/G3 task.
  - **Post-implementation review (`/review-deploy`, 2026-08-12) found T3.6 did not work as first delivered, and it was fixed the same day.** (1) **CRITICAL — alertmanager could not read its own config.** The original bind mount of the rendered file failed with `permission denied`, verified live: under rootless Docker the host deploy user maps to **container uid 0**, so `template-configs.sh`'s `chmod 600` file inside a `chmod 700` directory is root-owned from the container's point of view, and alertmanager runs as **uid 1000** (host subuid 100999 per `/etc/subuid`) — blocked by both the file mode and the parent directory. `amtool check-config` had passed only because it was run against a copy the assistant had chmod'd itself, never through the container's actual view; that gap is what let a non-working task be marked done. Fixed with the mechanism `env-setup.sh:307` already uses for the templated APISIX config — a root helper (`alertmanager-init`) copies the file into a volume and `chown 1000:1000`s it, mode staying `0600`, only the owner changing. Verified end to end: init stages it, then alertmanager as uid 1000 reads it → `SUCCESS`, 2 receivers. (2) **HIGH — a missing render silently became a directory that bricked later deploys.** Docker materialises a missing bind-mount source as a **directory** (verified), so starting the monitoring profile before `template-configs.sh` had run for that `APP_ENV` created a directory at the output path; the next run's `echo > "$output_file"` then failed under `set -e` and aborted the whole deploy with an error pointing nowhere near the cause. Fixed twice over: `alertmanager-init` mounts the **source directory** rather than the file (so the file path is never a bind source) and exits 1 with a named error if the render is absent, and `template-configs.sh` clears an empty stray directory via `rmdir` while naming a non-empty one instead of writing into it. Both paths verified. (3) **MEDIUM — dev published 9091 on all interfaces**; changed to `127.0.0.1:9091:9091` to match `common`. (4) **LOW — `.templated/` was not rsync-excluded**, so a locally-rendered secret could be pushed to a host; added `--exclude='.templated/'` to both rsyncs in `deploy-lib.sh` (safe: `deploy.sh` Step 4 regenerates them remotely before any consumer reads them). Re-validated after the fixes: `shellcheck`/`bash -n`/`yamllint`/`jq` clean, both compose files `config --quiet` clean, `alertmanager-init` correctly absent from the default profile.
- [ ] **G3: Monitoring is live and alerts fire** — E2E test — BLOCKED: all six children T3.1–T3.6 are now done, but the goal test needs the monitoring profile actually running on a host with OpenBao-rendered secrets (no local environment boots `common/docker-compose.yml`; the dev stack is off-limits to start and does not carry the monitoring profile at all), and the five `ALERT_*` keys must be seeded first **and their `deploy-required-keys.txt` entries uncommented** — both deferred by owner call 2026-08-12 because the SMTP values were not available; see T3.6 for the two-step checklist. **This is the one outstanding action standing between G3 and done.** **The goal test also cannot be run verbatim**: it asserts a `BackendDown` alert, which does not exist — backend/worker/frontend/keycloak expose no `/metrics`, so they are not scrape targets and stopping one produces no alert (backlog B7, recorded by T3.4). Everything statically checkable has been checked green: `promtool check rules`/`check config` (T3.4), all 43 dashboard PromQL expressions (T3.5), `amtool check-config` + `config routes test` (T3.6), and a simulated fail-closed render abort (T3.6). Verify on the next staging deploy, substituting a real scrape target for the nonexistent `BackendDown`: `docker compose --profile monitoring up -d && sleep 30 && curl -s localhost:9090/api/v1/targets | jq '[.data.activeTargets[] | select(.health=="up")] | length'` equals the target count (**expect the `apisix` job to be the one that proves T3.3's export-server fix — it was unreachable before**), then `docker compose stop node-exporter; sleep 180; curl -s localhost:9093/api/v2/alerts | jq '[.[] | select(.labels.alertname=="TargetDown")] | length'` ≥ 1 **and confirm the alert email actually arrives** — a firing alert that reaches Alertmanager but not the inbox is exactly the fail-open this goal exists to close.

## G4: No unpinned image can reach a host

- [x] T4.1 [deploy]: Pin OpenBao to a Minimus image (depends on T1.1) (2026-08-10) — all 6 `${OPENBAO_IMAGE:-ghcr.io/openbao/openbao:2.6.0}` image lines (5× `common/docker-compose.yml` vault-agent sidecars + 1× `common/openbao/docker-compose.openbao.yml` server) → `reg.mini.dev/openbao:${OPENBAO_IMAGE_TAG:-2.6.1}` — registry+name hardcoded, only the version in a var, matching the `POSTGRES_IMAGE_TAG`/`KEYCLOAK_IMAGE_TAG`/`ETCD_IMAGE_TAG` convention rather than the old single full-string `OPENBAO_IMAGE` var (which was used nowhere else in the repo — no drift-loop or manifest-override reference to break). Added `OPENBAO_IMAGE_TAG=2.6.1` to `other/env_templates/.env.template`. **Deliberately NOT wired into `deploy.sh`'s drift-detection loop / `image_tags:` manifest override** (owner call): that mechanism only covers `common/docker-compose.yml` services in the `SERVICES`-driven redeploy model (backend/frontend/gateway/keycloak/etcd/postgres); the OpenBao stack (`common/openbao/docker-compose.openbao.yml`, 6 containers) is a separate operator-run lifecycle that `deploy.sh` only health-gates on (`env-setup.sh`'s `check_openbao_ready`), never brings up or redeploys — adding drift detection there would be a new mechanism, not a copy of an existing one. **Operator must add `OPENBAO_IMAGE_TAG=2.6.1`** to `staging/.env` and `prod/.env` (dev doesn't run the openbao stack via `.env`-templated vars the same way — confirm per `common/openbao/README.md`). INSPECT confirmed `reg.mini.dev/openbao:2.6.1` pulls anonymously, has a shell (`/bin/sh`, Alpine-style busybox/ash) so every existing `CMD-SHELL` healthcheck and the server's inline seal-key `sh -c` script survive unchanged — no exec-form rewrite needed (unlike Keycloak in T2.8). Runs as root by default, same as compose assumes today (no `user:` override on any of these services) — no UID reconciliation needed. No `-hardened` variant exists (`2.6.1-hardened`/`2.6-hardened` both 404) → plain tag per BR-INFRA-003. 2.6.1 satisfies the ≥2.6.0 floor for CVE-2025-54996 (BR-SEC-016). `yamllint` and `docker compose config --quiet` both pass on both compose files (the latter's only error, `KC_DB_USERNAME` missing, is a pre-existing unrelated hard-requirement gate, not caused by this change). Runtime test (`docker compose -f common/openbao/docker-compose.openbao.yml up -d && bao status | grep 'Sealed *false'`) deferred to the next staging deploy — local workstation has no mTLS client certs for the OpenBao stack (`common/openbao/certs/` doesn't exist locally; populated at deploy time from the host cert source), same limitation as T2.1/T2.3/T2.4/T2.6/T2.7's deferred runtime tests.
- [x] T4.2 [deploy]: Pin the serving-path `other/services` images (depends on T1.1) (2026-08-10) — `other/services/cftunnel/docker-compose.yml` `cloudflare/cloudflared:latest` → `reg.mini.dev/cloudflared:2026.7.3`; `other/services/embedding/docker-compose.yml`'s four `ollama/ollama:latest` service defs (`ollama`, `ollama-pull-embed`, `ollama-pull-chat`, `ollama-pull-ocr`) → `reg.mini.dev/ollama:0.32.7`, one tag. Both versions verified as the current stable line via live `reg.mini.dev` tag-list queries (not guessed). Runtime test (curl version check) not run — these are standalone-VM/CI-only composes with no local environment to boot them; hardcoded tags directly in each compose (no `_IMAGE_TAG` env var) since neither is part of `deploy.sh`'s `SERVICES`-driven redeploy model, same precedent T4.1 set for OpenBao.
- [x] T4.3 [deploy]: Pin the internal registry to `distribution-registry` (depends on T1.1) (2026-08-10) — `other/services/registry/docker-compose.yml` `registry:3` → `reg.mini.dev/distribution-registry:3.1.1` (same upstream `distribution/registry` project/API; `REGISTRY_AUTH*`/htpasswd config untouched, carries over unchanged). Runtime test (authenticated push) not run — CI-only service, no local registry host to verify against.
- [x] T4.4 [deploy]: Pin SonarQube and its Postgres (depends on T1.1) (2026-08-10) — `other/services/sonarqube/docker-compose.yml`: `sonarqube:community` → `reg.mini.dev/sonarqube:26.8.0.126808`; `postgres:18-alpine` → `reg.mini.dev/postgres:18` (same tag T2.4 already validated for keycloak-db — uid 999 default, no `user:` override needed, no init/chown step for this service). Runtime test not run — CI-only service, no local host.
- [x] T4.5 [deploy]: Pin nginx-proxy-manager (depends on T1.1) (2026-08-10) — `other/services/npm/docker-compose.yml` `jc21/nginx-proxy-manager:2.15.1` → `reg.mini.dev/nginx-proxy-manager:2.15.1`, registry move only, version unchanged (already at an explicit tag). Runtime test not run — no local proxy host to verify a proxy host still resolves.
- [x] T4.6 [deploy]: Rebuild the Jenkins Docker-in-Docker layer on Minimus (depends on T1.1) (2026-08-10) — `other/services/jenkins/Dockerfile` rebased `FROM jenkins/jenkins:lts` → `FROM reg.mini.dev/jenkins:2.568.2-dev` (the `-dev` tag, not the plain runtime tag: live INSPECT confirmed the plain `reg.mini.dev/jenkins:2.568.2` ships no package manager at all, while `-dev` has `apk` against Minimus's full ~132k-package Wolfi-based repo — this image is a CI toolbox that needs its tools live at runtime, not a served app, so unlike the Python/Node app migrations there's no discard-the-builder multi-stage split; the `-dev` tag *is* the final image here, consistent with how the original Debian `jenkins/jenkins:lts` (not `-slim`) was already a "big image with tools" by design). **No separate `jenkins-agent` image applies** — confirmed live against the actual compose file: `other/services/jenkins/docker-compose.yml` builds one monolithic controller that runs pipeline steps directly against the mounted Docker socket, there is no controller/agent split despite the image-inventory table listing both. RESOLVE PACKAGES done via live `apk search`/install against the real repo (not guessed): `apt-transport-https`/`gnupg`/`lsb-release` dropped entirely (only existed to add Docker's apt repo, which Wolfi doesn't need — `docker-cli`/`docker-compose` install straight from Minimus's own repo); `build-essential`→`build-base`; `python3`/`python3-pip`/`python3-venv`→`python-3.14`/`py3.14-pip` (Wolfi's `python-3.14` package bundles venv, no separate split); `git`/`tar` dropped (already present in the `-dev` base, confirmed live, not re-added). **Playwright Chromium system-deps step rewritten, not translated**: Playwright's own `install-deps` helper only knows Debian/Ubuntu package names and shells out to `apt-get`, which doesn't exist on Wolfi — confirmed live it fails immediately (`spawn apt-get ENOENT`) regardless of a hand-written apk list, so a 1:1 translation was never possible. Replaced with `apk add chromium` — installed purely for the transitive runtime shared-library closure apk resolves (nss, atk, cups, mesa/drm, pango, cairo, alsa, ~75 packages), the package binary itself never invoked; the pipeline still downloads and caches its own Playwright browser at runtime exactly as before. **Live-verified, not assumed**: downloaded Playwright 1.61.0's real Chromium Headless Shell inside the built image and launched it headless (`--no-sandbox`), rendered a real page (`<h1>ok</h1>`), read the DOM back — zero missing-library errors. **`luarocks install luacheck` needed a real fix, not just a base swap**: apk's `luarocks` package runs under LuaJIT (its own shebang, a Wolfi packaging choice) with a default `LUA_INCDIR` guess that doesn't match where Wolfi installs LuaJIT's headers, so the one rock needing to compile (`luafilesystem`) failed until `luarocks config variables.LUA_INCDIR /usr/include/luajit-2.1` was set explicitly (added `luajit-dev` package for the headers; dropped `lua5.4`/`lua5.4-dev` from the original list entirely — confirmed via repo grep that nothing invokes the `lua5.4` binary directly, only `luacheck` matters, and it's correct for luacheck to lint against LuaJIT semantics since APISIX/OpenResty — what `crowdsec-bouncer.lua` actually runs under — is itself LuaJIT-based). **Full `docker build --no-cache` verified green** after 3 transient sandbox network retries (unrelated to the Dockerfile — a registry timeout, a mid-package IO error, and an overlay-storage permission error, each different, each on a clean retry); final build succeeded end-to-end (16/16 steps) and every tool verified live inside the built container: `docker version`/`docker compose version` against the real mounted `/run/user/1000/docker.sock` (matching the compose file's actual bind mount, not a guessed path), `trivy`, `gitleaks`, `semgrep`, `schemathesis`, `pip-audit`, `uv`, `luacheck`, `hadolint`, `yamllint`, `checkov`, `yq`, `sonar-scanner`, `rsync`, `shellcheck`, `jq`, `git` all run and report correct versions. Task's own done-when test (`docker exec jenkins docker version --format '{{.Client.Version}}'` prints a version string) passed exactly as specified. **Follow-up same day**: bumped the five hand-pinned tool `ARG`s to current latest, fetched live from each project's own release feed (not guessed) — `TRIVY_VERSION` 0.69.3→0.73.0, `GITLEAKS_VERSION` 8.30.0→8.30.1, `HADOLINT_VERSION` 2.12.0→2.15.1, `YQ_VERSION` 4.53.2→4.53.3 (new `YQ_SHA256` downloaded and verified live), `SONAR_SCANNER_VERSION` 6.2.1.4610→8.1.0.6389 (9 releases behind, biggest jump — confirmed the zip's internal path layout is unchanged across the major-version bump so the `ln -sf` target still resolves). Rebuilt clean on the first try; re-verified all five report the new version live inside the built image. `reg.mini.dev/jenkins:2.568.2-dev` re-checked against the live registry — still the newest LTS-shaped tag, no bump available. apk-installed packages (`docker-cli`, `docker-compose`, `chromium`, `python-3.14`, etc.) and the `pip`/`luarocks`/`astral.sh`-installed tools (`semgrep`, `schemathesis`, `pip-audit`, `yamllint`, `checkov`, `uv`, `luacheck`) were already unpinned/latest-at-build-time — nothing to bump there.
- [x] T4.7 [deploy]: Opportunistic busybox swap for the init/util images (depends on T1.1) (2026-08-10) — `common/openbao/docker-compose.openbao.yml`'s `openbao-init` and `other/services/sonarqube/docker-compose.yml`'s `sonarqube-init` → `reg.mini.dev/busybox:1.38.0` as named. **Third target corrected against the live tree**: the plan named "the `openbao-init` init container at `common/docker-compose.yml:494`", but no `openbao-init` service exists in that file — only `db-init`, `keycloak-db-init` (both already Minimus, from T2.1/T2.4) and `etcd-init`, and only `etcd-init` was still on `alpine:latest` (T2.7's own note already flagged "etcd-init's alpine:latest left to T4.7"). Swapped `etcd-init` → `reg.mini.dev/busybox:1.38.0` instead of chasing the stale reference. All three init scripts (`chown`/`chmod`/`cp`/`mkdir`, `set -euo pipefail`) verified busybox-compatible via a live `docker run` against `reg.mini.dev/busybox:1.38.0` before the swap — pure registry swap, no script changes needed.
- [x] T4.8 [deploy]: Record the no-match components as digest-pinned exceptions (depends on T4.2) (2026-08-10) — added `@sha256:` + a dated BR-INFRA-006 comment to all three: `other/services/crowdsec/docker-compose.yml` (`crowdsecurity/crowdsec:v1.7.8@sha256:2f527c9b…`), `other/services/embedding/docker-compose.yml`'s reranker (`ghcr.io/huggingface/text-embeddings-inference:cpu-1.9@sha256:ad950d30…`), `other/services/dockhand/docker-compose.yml` (moved off the rolling `:latest` onto `fnsys/dockhand:v1.0.41@sha256:de6c7d4b…` — confirmed live that `v1.0.41` and `latest` currently resolve to the identical digest, so this is an accurate pin of what's running today, not a downgrade). All three digests pulled live, not guessed.
- [x] T4.9 [deploy]: Rename image references outside the Dockerfiles (depends on T2.2, T2.5, T4.1) (2026-08-10) — ran the task's own literal grep test against the live tree first: `gateway-docker/Jenkinsfile:104`'s comment carried the literal string `apache/apisix@<digest>` describing the pre-T2.5 state (tripped the test though functionally dead code) — reworded to "the old Docker Hub digest pin" to preserve the historical note without the stale pattern. Also found a real gap the literal test didn't catch: `common/scripts/tests/waf-harness.sh:54`'s throwaway etcd container was still `quay.io/coreos/etcd:v3.6.11` — T2.7 only touched the real `common/`/`dev/docker-compose.yml` services, not this standalone WAF-harness fixture — updated to `reg.mini.dev/etcd:3.7.1` (T2.7's chosen tag). No other `DOCKER_REGISTRY`/`*_IMAGE_TAG` threading found stale in `Jenkinsfile`, `Jenkinsfile.deploy`, `gateway-docker/Jenkinsfile`, or `common/scripts/`.
- [ ] T4.10 [deploy]: CI gate that fails on an unpinned image (depends on T1.7, T2.8, T2.9, T3.2, T4.3, T4.4, T4.5, T4.6, T4.7, T4.8, T4.9)
- [ ] T4.11 [deploy]: Deploy the migrated image set to staging and verify at runtime (depends on T4.10)
- [ ] **G4: No unpinned image can reach a host** — E2E test

## G5: The v2026.6 backlog failure modes are closed

- [x] T5.1 [backend]: Release the extraction poller's transaction on an empty poll (2026-08-11)
- [x] T5.2 [backend]: Release the essay-grading poller's transaction on an empty poll (2026-08-11)
- [x] T5.3 [deploy]: Backstop the dynamic DB roles with an idle-transaction timeout (depends on T5.1, T5.2) (2026-08-11) — appended `ALTER ROLE "{{name}}" SET idle_in_transaction_session_timeout = '60s'; ALTER ROLE "{{name}}" SET statement_timeout = '300s';` to the `creation_statements` string in `common/openbao/bootstrap.sh`'s `cmd_db_engine()`, for both the `haisir-backend` and `haisir-worker` dynamic roles. Defense in depth, not the fix — T5.1/T5.2 (already shipped in `haisir-backend`) release the pollers' transactions at the source; this bounds how long any *future* poller-shaped bug can hold one open and block a `REINDEX`/`ALTER TABLE` behind it, per B1. `shellcheck`/`bash -n` clean. **Not wired into `deploy.sh`'s manifest flow** — `bootstrap.sh` is a standalone operator-run script (`bootstrap.sh db-engine`), never referenced by `deploy.sh` or any release manifest (confirmed via grep, zero hits either direction) — no manifest flag applies. **BLOCKED — operator has not yet re-run `bootstrap.sh db-engine` against staging or prod OpenBao.** `svr write database/roles/haisir-${role}` is idempotent (safe to re-run), but until it runs on each environment the new timeout settings apply to **no** lease there, existing or future. T5.3's own live test (issue a `database/creds/haisir-worker` lease, `SHOW idle_in_transaction_session_timeout` = `1min`) and T5.12 (staging verification) cannot pass until this operator step happens on staging; prod needs it separately before its own leases pick up the setting.
- [x] T5.4 [deploy]: Surface the real cause of a deploy-secret render failure (2026-08-10) — `bao_deploy_token()` in `common/openbao/render-deploy-secrets.sh` redirected the `bao login` stderr to `/dev/null`, collapsing container-down/sealed-vault/missing-cert/unregistered-role/`docker: command not found`/wrong-socket into one generic message. Now captures stderr to a temp file and, only on empty-token failure, `cat`s it to the real stderr above the existing generic message — fail-closed `exit 1` behavior on the caller side (`render-deploy-secrets.sh:179`) unchanged, only the diagnostic improved. Verified against a nonexistent-container stand-in for openbao-down: the real Docker daemon error (`Error response from daemon: No such container: ...`) now surfaces before the generic message, script still exits 1 cleanly (confirmed the `set -euo pipefail` + command-substitution-inside-`if !` interaction doesn't swallow it). `shellcheck` clean.
- [x] T5.5 [deploy]: Bring `other/cert/` inside the deploy sync (2026-08-10) — added a third rsync block to `sync_files_to_remote()` in `common/scripts/deploy-lib.sh`, transferring `other/cert/` → `${REMOTE_DIR}/other/cert/` with `--chmod=F755` on that invocation only (file is mode 644 in git; certbot execs it directly, so a content-correct but non-executable copy is still a broken hook). Did not add `--chmod` to the existing `common/`/`${env_name}/` rsyncs (they cover far more than this one path, and `full-setup.sh:268` invokes `setup.sh` directly — stripping the exec bit there breaks the next deploy). Added in `deploy-lib.sh`, not `deploy-remote-common.sh` (T6.6.1 deletes that file). `shellcheck` clean.
- [x] T5.6 [deploy]: Assert the installed certbot hook matches the repo (depends on T5.5) (2026-08-11) — added `assert_certbot_hook_matches()` to `common/scripts/deploy-lib.sh`: sha256sums the repo's `other/cert/haisir-sync-certs.sh` against the installed copy at `/etc/letsencrypt/renewal-hooks/deploy/haisir-sync-certs.sh` on the remote host, `log_error`s a named message (both hashes + re-install command) and returns 1 on missing/mismatch. Wired into `common/scripts/deploy.sh` as a new Step 2b, called unconditionally after the sync step (runs even under `--skip-sync`, since drift at the installed path is independent of whether this run re-synced `${REMOTE_DIR}/other/cert/`). Under the script's existing `set -euo pipefail`, a non-zero return aborts the deploy immediately with the printed error. `shellcheck` clean (0 warnings, run from `common/scripts/`). Committed `haisir-deploy@6fafb29`. Runtime test not run — no SSH target available in this session; matches the pattern of every other deploy task whose test needs a live staging/prod host.
- [x] T5.7 [deploy]: Assert the installed certbot hook is executable (depends on T5.6) (2026-08-12) — extended T5.6's `assert_certbot_hook_matches()` in `common/scripts/deploy-lib.sh`: the single SSH round trip now fetches both the installed hook's sha256sum and its `stat -c '%a'` mode (`echo "$h $m"` remotely, split locally via `${remote_info%% *}`/`${remote_info#* }`), and after the existing hash-match check passes, a new check aborts with a named error (path, actual mode, expected 755, fix command) if the installed mode isn't `755` — closes the gap where a mode-644 copy passes T5.6's content-only hash check but certbot still can't exec it. No `deploy.sh` change needed — Step 2b already calls this function unconditionally. `shellcheck` clean (0 warnings, run from `common/scripts/`). Parsing logic (hash/mode split, including the empty-file edge case) verified with a standalone bash check before relying on it. **Not committed** — user corrected mid-T5.6 that skill runs must not auto-commit; changes are in the working tree for the user's own review/commit. Runtime test not run — no SSH target available in this session, same limitation as T5.6 and every other deploy task needing a live host.
- [x] T5.8 [deploy]: Pin the rootless container runtime across hosts (2026-08-11) — `docs/DOCKER_INSTALL_GUIDE.md` had `--port-driver=slirp4netns` filed as an optional NPM-troubleshooting fix, not required provisioning, which is exactly how staging and prod diverged on it and broke the v2026.6 prod deploy (B5). Promoted it to a required Install step 3 on **every** host (dev, staging, prod — not just staging/prod), applied right after the rootless installer; added Install step 4 telling operators to immediately converge to the pinned `VERSION` via the Upgrade section rather than landing on whatever `get.docker.com` currently resolves as "stable"; reworded the Upgrade section's `VERSION=29.1.3` from "adjust if you want" to the canonical pinned value shared by all three hosts (rootlesskit ships bundled inside the `docker-rootless-extras-$VERSION.tgz` tarball, so pinning `VERSION` pins rootlesskit too — no separate rootlesskit download exists in this guide); added a new Upgrade step 8 that re-verifies the port-driver override survived the upgrade (`ps aux | grep rootlesskit`), since nothing previously checked that an upgrade didn't silently regress it. `common/apisix_conf/config.yaml`'s `allow_admin` comment (both CIDR entries) reworded to match — **both entries deliberately kept**, not narrowed to one, as defense-in-depth against a future rootlesskit upgrade or an unpinned host regressing to the `builtin` driver's bridge-gateway address; no functional YAML change. Committed `haisir-deploy@7522757`. **Follow-up same day**: `slirp4netns` itself was still coming from `apt`, drifting per-host independently of the rest of this pin — live-verified current stable upstream is `v1.3.4` (this host was running `1.3.3`, confirmed via `slirp4netns --version`) and it is **not** bundled in Docker's own static tarballs (a separate upstream project, `rootless-containers/slirp4netns`), so it's pinned the same way: checksum-verified download into `~/bin`, replacing the `apt install`/`apt install --only-upgrade` steps in Install §1 and Upgrade §6. Confirmed live on this host that `~/bin` is first in `docker.service`'s rendered `PATH`, so the downloaded binary is what `dockerd-rootless.sh` actually resolves — an apt-installed copy, if present, is shadowed. Also documented, after reading `dockerd-rootless.sh`'s actual default-resolution logic (verified live and cross-checked against current `moby/moby` upstream source) and this host's own live `ps aux` output as evidence: `--net` and `--port-driver` are independent flags — recent Docker auto-selecting `--net=slirp4netns` when the binary is present does **not** imply `--port-driver=slirp4netns` (that still defaults to `builtin` unless set explicitly), correcting a plausible-sounding but wrong claim that newer Docker defaults the port driver to slirp4netns. Committed `haisir-deploy@e4e3461`. **Follow-up 2, applying the guide for real on dev (operator's own workstation)**: this surfaced a real bug in the same-day slirp4netns pin — the checksum step downloaded the binary as `slirp4netns` but `SHA256SUMS` keys the hash by `slirp4netns-x86_64`, so `sha256sum -c` couldn't find the file, printed `FAILED open or read`, and — because that ran inside a subshell under `set -euo pipefail` rather than a bare top-level command — the script kept going and installed the **unverified** binary anyway instead of aborting. Fixed by preserving the real filename through download → verify → install; re-ran on dev and got a clean `slirp4netns-x86_64: OK`. Also, live state on dev didn't match the doc's placeholders: Docker was already `29.7.1` (adopted as the new canonical pin, replacing `29.1.3`, rather than downgrading a working host); Compose (`v2.40.3`) and Buildx (`v0.29.1`) were behind the doc's already-pinned `v5.0.1`/`v0.30.1` — both exist live and are now checksum-verified in the guide the same way (previously unverified `curl` + `chmod`), and applied on dev (compose → v5.0.1, buildx → v0.30.1, both confirmed `OK`). Documented that `vpnkit` (present in `~/bin`, asked about mid-task) ships bundled inside the same `docker-rootless-extras-$VERSION.tgz` as `rootlesskit` — no separate pin needed, unlike slirp4netns/Compose/Buildx which are separate upstream projects with their own release cadence; also inert on this stack once slirp4netns is pinned, since rootlesskit only falls back to it when neither slirp4netns nor pasta is available. Committed `haisir-deploy@a833ba1`. **Follow-up 3**: the operator hand-typed `COMPOSE_VERSION=v5.40.0` while re-running this on dev — nonexistent tag, 404 on both the binary and `.sha256` (they're keyed off the same tag). Live-checked the real latest release: `v5.4.0` (an extra `0` had been typo'd in), and separately confirmed `BUILDX_VERSION=v0.36.1` (the operator's own value) resolves cleanly — both re-pinned in the guide and re-applied on dev, checksums `OK`. Committed `haisir-deploy@1556e07`. **Follow-up 4 — the operator ran the actual `systemctl --user restart docker` on dev, and it failed**: `journalctl` showed `[rootlesskit:parent] error: --slirp4netns-sandbox conflicts with --detach-netns`. This is a **real, pre-existing bug in the override.conf content** that the "Real Client IP" section had carried since before this task — `SLIRP4NETNS_SANDBOX=true` (hardcoded) is unconditionally rejected by rootlesskit whenever `--detach-netns` is active, which is the current `dockerd-rootless.sh`'s own default; it had never been exercised end-to-end before (exactly the "staging is correct by accident, never actually verified" gap T5.8 exists to close). Root-caused via rootlesskit's own source (an explicit `if detachedNetNSPath != "" && d.enableSandbox { error }` check, no auto-negotiation for a literal `true`) — the fix is `SANDBOX=auto` (upstream's own default), which negotiates around the conflict instead of crashing; `SLIRP4NETNS_SECCOMP=true` has no such conflict and stays forced. Fixed in both the override template and the reference table; added a troubleshooting note pointing at `journalctl --user -u docker.service` for the real error under the `Start request repeated too quickly` restart-loop noise that systemd itself shows. Committed `haisir-deploy@3873876`. **Verified live end-to-end on dev after the fix**: `systemctl --user restart docker` succeeded clean; `ps aux` confirms `--net=slirp4netns --port-driver=slirp4netns --disable-host-loopback --slirp4netns-sandbox=auto --slirp4netns-seccomp=true`; `docker --version` → `29.7.1`, `slirp4netns --version` → `1.3.4`, `docker compose version` → `v5.4.0`, `docker buildx version` → `v0.36.1` — all match the pinned values. All 5 dev-stack containers (`apisix-dev`, `keycloak-dev`, `etcd-dev`, `postgres-dev`, `pgadmin-dev`, `restart: unless-stopped`) auto-resumed healthy; the operator's VS Code devcontainers (`backend`/`frontend`, no auto-restart) stayed down as expected, left alone per standing instruction, need the operator to reopen them in VS Code. **T5.8 is genuinely done on dev — dev's leg of the live test now passes.** **Follow-up 5 — staging hit the identical `SANDBOX=true`/`--detach-netns` crash** (the operator had applied the override before follow-up 4's fix landed): fixed live over SSH with the same one-line `sed` correction, `daemon-reload`, `reset-failed`, `restart` — confirmed clean, all 13 staging services (`apisix-staging`, `keycloak-staging`, `etcd-staging`, `haisir-db-staging`, `haisir-backend-staging`, `haisir-worker-staging`, `haisir-frontend-staging`, 5× vault-agent sidecars, `keycloak-db-staging`) came back healthy. **Follow-up 6 — prod separately hit an unrelated dpkg snag**: `apt-get install -y socat` (itself harmless — socat was already up to date) also triggered dpkg's standard "finish configuring any pending packages" behavior, which tried and failed to reconfigure `openssh-server` (pre-existing "half-configured" state, unrelated to this task) — its postinst calls `systemctl` on `ssh.service`, which the operator has deliberately masked (Tailscale-only access), and `deb-systemd-invoke`'s masked-unit skip logic only covers the `start`/`restart` actions, not whatever action dpkg's trigger used here, so it died instead of skipping. Root-caused via reading `/usr/bin/deb-systemd-invoke` directly on the host (not guessed) down to the exact line and branch. Fixed via the standard, non-invasive Debian mechanism — a temporary `/usr/sbin/policy-rc.d` returning `101` (forbidden), which `deb-systemd-invoke` explicitly checks before ever calling `systemctl` — so `dpkg --configure -a` completed cleanly without the mask being touched at any point (confirmed `ssh.service` still `masked`/`inactive` immediately after). Prod's `override.conf` was then written correctly the first time (`SANDBOX=auto` from the start, no repeat of follow-up 4's bug) and the daemon restarted; all 13 prod services came back healthy. Committed `haisir-deploy@3873876` (no further deploy-repo commits needed for follow-ups 5/6 — pure live-host operations, no doc changes). **Verified live end-to-end on all three real hosts**: `docker 29.7.1`, `slirp4netns 1.3.4` (`rootlesskit 3.0.2` identically on dev/staging/prod), Compose `v5.4.0`, Buildx `v0.36.1`, `--net=slirp4netns --port-driver=slirp4netns --slirp4netns-sandbox=auto` all match on dev, staging, and prod. Ran `PLAN.md`'s own convergence test across staging+prod (`rootlesskit --version` + override.conf's port-driver setting, unique-line count) — **note for whoever revisits this test**: its literal `grep -o 'port-driver=[a-z0-9]*' override.conf` pattern doesn't match this file's actual content (`Environment="DOCKERD_ROOTLESS_ROOTLESSKIT_PORT_DRIVER=..."` uses underscores/uppercase, not the lowercase-hyphenated CLI-flag spelling the test assumes), so it silently finds nothing rather than erroring — verified the same convergence directly via `ps aux`'s live `--port-driver=slirp4netns` on both hosts instead, which is what the test is actually trying to prove. CI pinning is operator-reported complete; not independently verified (no SSH target for it in this session). **T5.8 is done — dev, staging, and prod all converged and live-verified; CI operator-confirmed.**
- [x] T5.9 [specs]: Close the review-coverage gap on `92a4da2` (2026-08-10) — verdict CLEAN recorded in `decisions.md`; `92a4da2` is `d6adec7`'s immediate child (outside both G8 passes' range), an ancestor of `705833d` (coverage gap, not merge question). Test-only commit (CSP enforcement e2e soak); the gate is wired into CI before registry push, the negative test injects via response-body rewrite (not `page.evaluate`/CDP, which bypasses CSP), and the two assertions (script did not execute + real `/csp-report` POST collected) close each other's pass-for-the-wrong-reason gap. See `decisions.md` 2026-08-10 entry.
- [x] T5.10 [backend]: Declare `question_id` on `ExamReviewChatRequest` (2026-08-11)
- [x] T5.11 [backend]: Narrow exam-review grounding to the named question (depends on T5.10) (2026-08-11) — filtered client-side in the existing route-file helper `_build_review_grounding` (no new service/repository method; `get_by_session_id` still called with only `attempt_id`, filtering happens post-fetch). A `question_id` matching no session question in the attempt resolves to empty grounding, not a fallback to the whole attempt.
- [ ] T5.12 [deploy]: Confirm the pollers hold no transaction on staging (depends on T5.3)
- [ ] **G5: The v2026.6 backlog failure modes are closed** — E2E test

## G6: Env config is version-controlled and fails closed

- [ ] **G6: Env config is version-controlled and fails closed** — E2E test

### G6.1: Host topology resolves from KV, fully derived

- [x] T6.1.1 [deploy]: `template-configs.sh` sources the render hook (2026-08-10)
- [x] T6.1.2 [deploy]: Delete `template-configs.sh`'s cross-environment config fallback (depends on T6.1.1) (2026-08-10)
- [x] T6.1.3 [deploy]: Seed `KC_HOSTNAME_ADMIN` into KV for staging and prod (depends on T6.1.1) (2026-08-10) — seeded on both envs (`secret/haisir/infra`), verified non-empty via `bao kv get` on each. `deploy-required-keys.txt` now arms `infra:KC_HOSTNAME_ADMIN:envs=staging,prod` so a future deploy fails closed if this key is ever missing.
  - **Found + fixed while seeding (both envs)**: `admin-ops` OpenBao identity couldn't log in on either staging or prod (`invalid certificate or no client certificate supplied`). Cert/key/CA chain were all fine (pubkey hash matches the container's trusted CA, `openssl verify` OK, correct `clientAuth` EKU, key not passphrase-protected) — actual cause was that `auth/cert/certs/admin-ops` was simply never registered in OpenBao on either env (`bao read` returned "No value found"). Fixed on both by registering it (`bao write auth/cert/certs/admin-ops display_name=admin-ops policies=admin certificate=@... allowed_common_names=openbao-client-admin-ops ttl=3600`) using each env's bootstrap root token; `admin-ops` now logs in successfully on both and was used to seed the key on prod. Separately noted but NOT the cause: `reissue-ca-keyusage.sh` (2026-08-07) only patched the `apisix-certs` volume and left `openbao-certs-*`/`etcd-certs` on the pre-keyUsage CA cert ("Reconcile in a planned window" — still outstanding, harmless per the script's own same-key invariant, confirmed live).
- [x] T6.1.4 [deploy]: Seed the other eight infra keys, fully derived (depends on T6.1.3) (2026-08-11) — all 8 keys (`KEYCLOAK_ADMIN_PORT_BINDING`, `BACKEND_DB_PORT_BINDING`, `TAILSCALE_ADMIN_CIDR`, `KEYCLOAK_ADMIN_ALLOWED_CIDR`, `APISIX_ADMIN_ALLOWED_CIDR`, `EXTRACTION__OLLAMA_BASE_URL`, `EMBEDDING__OLLAMA_BASE_URL`, `HAITU__RERANK_BASE_URL`) seeded into `secret/haisir/infra` on both staging and prod as final expanded values (no `${` templates), operator-executed via `admin-ops` cert login + `common/openbao/rotate-secret.sh`. No repo file changes for the seeding itself — pure OpenBao KV writes, admin-ops's cert files had to be redelivered into the container's `/tmp` (T4.1's same-day Minimus image pin recreated the container, wiping the non-persistent stdin-delivered cert from T6.1.3).
  - **Found + fixed while seeding**: `rotate-secret.sh`'s `KEY=@-` stdin shortcut is broken through `docker exec -i` (`bao` does a literal `open("-")`, not a stdin alias, and errors `no such file or directory`) — same limitation `bootstrap.sh`'s `cmd_seed_deploy_path` already worked around. Fixed in `common/openbao/rotate-secret.sh` (uncommitted in working tree as of this note) by adopting the same real-temp-file-via-`cat` pattern. `shellcheck` clean.
  - **Found while seeding prod**: `KEYCLOAK_ADMIN_ALLOWED_CIDR` is `""` in prod's current `.env.config.sh` (matches T6.1.9's own note) — today, an empty/unrendered `*_CIDR` makes the `ip-restriction` plugin get stripped from the route entirely rather than deny-by-default, so **prod's Keycloak admin console is currently reachable from the public internet** (the issue TASKS.md's header names as "the most urgent open security item on the system"). Seeded KV with the same empty value (preserves current behavior exactly, no regression from this task) rather than inventing a placeholder or a real CIDR — `rotate-secret.sh` rejects an empty value by design, so this one key was written via a direct `bao kv patch` instead. **This is not resolved by T6.1.4** — closing it is G6.2's job (`T6.2.2` next in "Ready now", then `T6.2.3`/`T6.2.5`/`T6.2.6` land the actual deny-all fix and apply it to prod). Do not treat prod's admin-console exposure as fixed until G6.2's prod rollout (`T6.2.6`) is checked off. A manual APISIX Admin-API CIDR edit is not a substitute — `template-configs.sh` re-renders the route from KV on every deploy and would clobber it.
- [x] T6.1.5 [deploy]: Arm the per-key fail-closed gate for all nine (depends on T6.1.4) (2026-08-12) — appended 8 `infra:<KEY>:envs=staging,prod` lines to `common/openbao/deploy-required-keys.txt` for the keys T6.1.4 seeded (`KEYCLOAK_ADMIN_PORT_BINDING`, `BACKEND_DB_PORT_BINDING`, `TAILSCALE_ADMIN_CIDR`, `KEYCLOAK_ADMIN_ALLOWED_CIDR`, `APISIX_ADMIN_ALLOWED_CIDR`, `EXTRACTION__OLLAMA_BASE_URL`, `EMBEDDING__OLLAMA_BASE_URL`, `HAITU__RERANK_BASE_URL`) — the 9th key, `KC_HOSTNAME_ADMIN`, was already armed by T6.1.3. Landed before T6.1.6/T6.1.7/T6.1.9 delete any plaintext, per the manifest's gate-first convention. Parse-verified by sourcing `common/openbao/manifest.sh` and running `manifest_entries` against the file: all 9 keys resolve for `APP_ENV=staging` and `APP_ENV=prod`, zero resolve for `APP_ENV=dev`. Live test (blank a KV key, confirm `render-deploy-secrets.sh` aborts naming it, then restore) not run — no OpenBao token/SSH target in this session, same limitation as T6.1.3/T6.1.4's own runtime tests. **Not committed** — per standing instruction, skill runs don't auto-commit; changes are in the working tree for the user's own review/commit.
- [x] T6.1.6 [deploy]: Delete the three decorative CIDR defaults from the common file (depends on T6.1.5) (2026-08-12) — deleted `common/.env.config.common.sh`'s three `export <NAME>_CIDR="${<NAME>_CIDR:-127.0.0.1/32}"` decorative-default lines (`TAILSCALE_ADMIN_CIDR`, `KEYCLOAK_ADMIN_ALLOWED_CIDR`, `APISIX_ADMIN_ALLOWED_CIDR`) — user applied the edit directly in their IDE, also removing the three explanatory comment blocks above each line (PLAN.md's "no other line touched" scope guard named only the export lines; dropping the now-dangling comments alongside them is a reasonable deviation, not a gap). Now that T6.1.5 arms the fail-closed gate for these keys, deleting the defaults is safe — a missing KV value aborts the render instead of silently reverting to `127.0.0.1/32` (the v2026.6 failure mode this plan closes). Verified: `bash -n` clean, done-when test `! grep -q '_CIDR=' common/.env.config.common.sh` passes. Live render-diff test (byte-identical staging render before/after) deferred — needs a live host, same limitation as every other G6.1 task.
- [x] T6.1.7 [deploy]: Remove the eight from staging's config files (depends on T6.1.6) (2026-08-12) — user removed the eight `KEYCLOAK_ADMIN_PORT_BINDING`/`BACKEND_DB_PORT_BINDING`/`TAILSCALE_ADMIN_CIDR`/`KEYCLOAK_ADMIN_ALLOWED_CIDR`/`APISIX_ADMIN_ALLOWED_CIDR`/`EXTRACTION__OLLAMA_BASE_URL`/`EMBEDDING__OLLAMA_BASE_URL`/`HAITU__RERANK_BASE_URL` assignments directly in `staging/.env.config.sh` and `staging/.env` (edited outside this session per the standing `.env*` no-read/no-edit-by-assistant rule; existence-only checked, not content-verified). **T6.1.7's own done-when test (byte-identical `template-configs.sh` render before/after the deletion, via a `.templated-before/` capture) was not run** — no OpenBao token/staging host in this session to render either side. All eight keys are seeded in KV (T6.1.4) and the fail-closed gate is armed (T6.1.5), so removal should be a no-op at render time; T6.1.8's live gate (next) is the first point this is actually exercised end to end.
- [x] T6.1.8 [deploy]: Positive control: the render aborts when KV is unreadable (depends on T6.1.7) (2026-08-12) — **live-verified on staging by the operator, plan's own test corrected first.** PLAN.md's literal test (`BAO_TOKEN=invalid bash common/scripts/template-configs.sh` exits non-zero) does not exercise the intended path: `render-deploy-secrets.sh:189` unconditionally overwrites `BAO_TOKEN` with a fresh cert-auth login result, so a pre-set value is always clobbered — confirmed by reading the script, not guessed. Code inspection first established no build was needed: `template-configs.sh:57` calls `render_deploy_secrets_or_die` before any `.templated/` write begins at `:213`, and `render-deploy-secrets.sh:189-192` already `exit 1`s if the deploy cert-auth login fails, so "KV unreadable" was already structurally fail-closed. Ran the real negative control instead — `docker stop openbao-staging` (stop only, no `down`/`rm`, state/volumes untouched), then `template-configs.sh` → **exit code 1**, sha256 snapshot of `.templated/staging/*` before vs. after the failed run → **byte-identical, zero new/modified files**. `docker start openbao-staging` restored it; Docker's own healthcheck confirmed `Up ... (healthy)` afterward (a direct `bao status` needs the client cert too, not just `BAO_CACERT` — that command's TLS error was a missing flag in the verification command itself, not a real fault; the container healthcheck is the authoritative signal here). No repo file changes — pure verification task.
- [x] T6.1.9 [deploy]: Remove the eight from prod's config files (depends on T6.1.8) (2026-08-12) — user removed the same eight assignments (`KEYCLOAK_ADMIN_PORT_BINDING`/`BACKEND_DB_PORT_BINDING`/`TAILSCALE_ADMIN_CIDR`/`KEYCLOAK_ADMIN_ALLOWED_CIDR`/`APISIX_ADMIN_ALLOWED_CIDR`/`EXTRACTION__OLLAMA_BASE_URL`/`EMBEDDING__OLLAMA_BASE_URL`/`HAITU__RERANK_BASE_URL`) directly in `prod/.env.config.sh` and `prod/.env` (edited outside this session per the `.env*` no-read/no-edit-by-assistant rule; existence-only checked). Deliberately held pending until T6.1.8's live gate passed, per the manifest's gate-first-delete-second convention and explicit user sequencing. **T6.1.9's own done-when test (byte-identical prod render before/after) was not run** — same gap as T6.1.7, no OpenBao render captured on either side of the deletion; the keys are seeded (T6.1.4) and the gate is armed (T6.1.5), so removal should be a no-op at render time, but this has not been positively confirmed on prod the way T6.1.8's negative path was confirmed on staging.
- [ ] **G6.1: Host topology resolves from KV, fully derived** — integration test — BLOCKED: all nine children T6.1.1–T6.1.9 done, but the subgoal's positive half (`template-configs.sh` renders every `*_CIDR`/`*_BASE_URL` placeholder to the same value pre- and post-move, on staging AND prod) was never captured on either environment — only the negative half (render aborts non-zero, writes nothing, when OpenBao is unreachable) was live-verified, on staging only (T6.1.8). Verify before closing: capture a `.templated-before/` snapshot is no longer possible retroactively (the plaintext defaults are already deleted on both envs) — instead, on each of staging and prod, confirm live: `bash common/scripts/template-configs.sh` exits 0 and the rendered `*_CIDR`/`*_BASE_URL` values in `.templated/<env>/` match what's actually running today (compare against the live APISIX route config via the Admin API, not the deleted `.env.config.sh` defaults).

### G6.2: `ip-restriction` denies by default and is always present

- [x] T6.2.1 [deploy]: Add `KC_HOSTNAME_ADMIN` to the Keycloak service (depends on T6.1.3) (2026-08-10) — added `KC_HOSTNAME_ADMIN=${KC_HOSTNAME_ADMIN}` to the keycloak service environment in `common/docker-compose.yml`, adjacent to `KC_HOSTNAME`/`KC_HOSTNAME_STRICT`. Value comes from the `secret/haisir/infra` KV key T6.1.3 already seeded on staging and prod — `render-deploy-secrets.sh` emits it automatically, no `.env` edit needed on those two envs. Verified live: `docker compose --env-file staging/.env config` (with an override value) renders `KC_HOSTNAME_ADMIN` distinct from `KC_HOSTNAME` in the keycloak service block, matching the task's own `yq` test. Dev has no render-wrapper pipeline for this compose file — if dev ever runs `common/docker-compose.yml`'s keycloak service, it needs `KC_HOSTNAME_ADMIN` added to `dev/.env` separately (out of scope here, not currently a gap since dev doesn't run this path).
- [ ] T6.2.2 [deploy]: Live gate: prove tailnet admin login works on staging (depends on T6.2.1)
- [ ] T6.2.3 [deploy]: Replace plugin-stripping with a deny-all whitelist (depends on T6.2.2)
- [ ] T6.2.4 [deploy]: Regression test that fails if fail-open returns (depends on T6.2.3)
- [ ] T6.2.5 [deploy]: Apply the deny-all on staging (depends on T6.2.4)
- [ ] T6.2.6 [deploy]: Apply the deny-all on prod (depends on T6.2.5)
- [ ] T6.2.7 [deploy]: Re-verify tailnet admin login on prod after the deny-all (depends on T6.2.6)
- [ ] **G6.2: `ip-restriction` denies by default and is always present** — integration test

### G6.3: The seven paths are committed, scanned, and sourced from the release

- [ ] T6.3.1 [deploy]: Precondition: scan the seven paths for secret and topology residue (depends on T6.1.9)
- [ ] T6.3.2 [deploy]: Land the seven paths, the scanner and the sync in one commit (depends on T6.1.2, T6.1.9, T6.2.7, T6.3.1)
- [ ] T6.3.3 [deploy]: Verify no committed config path carries a `REMOTE_*` assignment (depends on T6.3.2)
- [ ] T6.3.4 [deploy]: Supply all three `REMOTE_*` vars as Jenkins credentials (depends on T6.3.2)
- [ ] T6.3.5 [deploy]: Make `deploy.sh` fail closed on a missing SSH target (depends on T6.3.4)
- [ ] **G6.3: The seven paths are committed, scanned, and sourced from the release** — integration test

### G6.4: The release artifact is the only source of the three files on any host

- [ ] T6.4.1 [deploy]: Tighten the three synced files to mode 600, by name (depends on T6.3.2)
- [ ] T6.4.2 [deploy]: Verify the files arrive at mode 600 from an empty start (depends on T6.4.1, T6.3.4)
- [ ] T6.4.3 [deploy]: Verify the remote content matches the committed copy (depends on T6.4.2)
- [ ] **G6.4: The release artifact is the only source of the three files on any host** — integration test

### G6.5: Version reconciliation is deleted, not repaired

- [ ] T6.5.1 [deploy]: Delete the remote VERSION rewrite and the tag-reconciliation block (depends on T6.4.3)
- [ ] T6.5.2 [deploy]: Delete the override reads and every consumer of them (depends on T6.5.1)
- [ ] T6.5.3 [deploy]: Remove the dead manifest fields (depends on T6.5.2)
- [ ] T6.5.4 [deploy]: Assert manifest version equals the committed `VERSION=` (depends on T6.5.3)
- [ ] **G6.5: Version reconciliation is deleted, not repaired** — integration test

### G6.6: One deploy path, not two

- [ ] T6.6.1 [deploy]: Delete the manual deploy scripts (depends on T6.4.3)
- [ ] T6.6.2 [deploy]: Take `REMOTE_HOST` from the environment in the test runner (depends on T6.3.2, T6.6.1)
- [ ] T6.6.3 [deploy]: Document the single deploy path (depends on T6.6.2)
- [ ] **G6.6: One deploy path, not two** — integration test

## G7: Specs and phase record reflect what shipped

- [ ] T7.1 [specs]: Record the delivered image inventory (depends on T4.11)
- [ ] T7.2 [specs]: Report CVE reduction per component from published counts (depends on T7.1)
- [ ] T7.3 [specs]: Mark BR-SEC-022 and BR-SEC-023 shipped (depends on T6.6.3, T6.5.4)
- [ ] T7.4 [specs]: Record the committed paths and KV keys in the layout section (depends on T7.3)
- [ ] T7.5 [specs]: Retire the render-hook follow-up item (depends on T6.1.1, T7.3)
- [ ] T7.6 [specs]: Two independent security review passes (depends on T7.2, T7.4, T7.5, T3.6, T5.3, T5.4, T5.6, T5.7, T5.8, T5.9, T5.11, T5.12, T6.5.4)
- [ ] T7.7 [specs]: Fill the Phase 7.5 Outcome column (depends on T7.6)
- [ ] T7.8 [specs]: Record the phase close-out decisions (depends on T7.6)
- [ ] T7.9 [specs]: Clear the closed backlog items (depends on T7.6)
- [ ] T7.10 [specs]: Add the new load-bearing constraints (depends on T7.6)
- [ ] T7.11 [specs]: Re-snapshot `current/` (depends on T7.9)
- [ ] **G7: Specs and phase record reflect what shipped** — E2E test

---

## Ready now

Tasks with no pending dependencies — can be started immediately:

- T1.7 [deploy]: Boot the full application stack on the migrated bases
- T5.12 [deploy]: Confirm the pollers hold no transaction on staging — note: also needs the T5.3 operator step (re-run `bootstrap.sh db-engine` on staging) done first, not just a deploy
- T6.2.2 [deploy]: Live gate: prove tailnet admin login works on staging
- T6.3.1 [deploy]: Precondition: scan the seven paths for secret and topology residue
