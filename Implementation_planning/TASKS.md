# Progress

> Auto-generated from PLAN.md. Updated by `/implement` in each code repo.
> Last baselined: backend:3c53b1a frontend:816194d deploy:0c7f77f (2026-07-14)

## G1 [deploy]: Deploy-side reconciliation

### G1.1 [deploy]: Branch file reconciliation
- [x] T1.1.1 [deploy]: Land net-new common/openbao/ directory files (2026-07-14)
- [x] T1.1.2 [deploy]: Land net-new generate-certs-openbao.sh (2026-07-14)
- [x] T1.1.3 [deploy]: Reconcile common/docker-compose.yml (depends on T1.1.1) (2026-07-14)
- [x] T1.1.4 [deploy]: Reconcile common/scripts/template-configs.sh (2026-07-14)
- [x] T1.1.5 [deploy]: Reconcile common/scripts/env-setup.sh (2026-07-14)
- [x] T1.1.6 [deploy]: Reconcile other/env_templates/.env.template (2026-07-14)
- [x] T1.1.7 [deploy]: Regenerate .secrets.baseline (depends on T1.1.1-T1.1.6) (2026-07-14)
- [x] **G1.1: Branch file reconciliation** — integration test (2026-07-14)

### G1.2 [deploy]: Static seal replaces transit-unseal (design change A)
- [x] T1.2.1 [deploy]: Remove openbao-unseal service/config/volumes, add seal "static" block (2026-07-14)
- [x] T1.2.2 [deploy]: Rework bootstrap.sh cmd_unseal_init() + `all` composite (2026-07-14)
- [x] T1.2.3 [deploy]: Remove --with-unseal flag from backup.sh (2026-07-14)
- [x] T1.2.4 [deploy]: Remove openbao-unseal cert generation (2026-07-14)
- [x] **G1.2: Static seal replaces transit-unseal** — integration test (2026-07-14)

### G1.3 [deploy]: Version pin to 2.6.0 (design change B)
- [x] T1.3.1 [deploy]: Bump surviving OpenBao image ref in docker-compose.openbao.yml (2026-07-14)
- [x] T1.3.2 [deploy]: Bump OPENBAO_IMAGE in .env.config.sh.template (2026-07-14)
- [x] **G1.3: Version pin to 2.6.0** — integration test (2026-07-14)

### G1.4 [deploy]: Ollama API key secret-gap closure
- [x] T1.4.1 [deploy]: Add backend Ollama keys to secret/haisir/backend (depends on T1.1.1) (2026-07-14)
- [x] T1.4.2 [deploy]: Add worker Ollama keys to secret/haisir/worker (depends on T1.1.1) (2026-07-14)
- [x] T1.4.3 [deploy]: Add backend keys to backend.env(.dynamic).ctmpl (depends on T1.1.1, T1.4.1) (2026-07-14)
- [x] T1.4.4 [deploy]: Add worker keys to worker.env(.dynamic).ctmpl (depends on T1.1.1, T1.4.2) (2026-07-14)
- [x] T1.4.5 [deploy]: Remove plaintext Ollama key lines from docker-compose.yml (depends on T1.4.1, T1.4.2, T1.4.3, T1.4.4, T1.1.3) (2026-07-14)
- [x] **G1.4: Ollama API key secret-gap closure** — integration test (2026-07-14) <!-- UNRESOLVED: subgoal test calls for a live dry-run template render, which needs a running OpenBao server; accepted as a documented static substitute — all 3 keys (EMBEDDING__OLLAMA_API_KEY, HAITU__OLLAMA_API_KEY, GRADING__OLLAMA_API_KEY) appear as guarded render-ready stanzas in all 4 ctmpl, and grep -c OLLAMA_API_KEY common/docker-compose.yml == 0. Live proof deferred to T2.6.1. -->

### G1.5 [deploy]: Stack bring-up ordering
- [x] T1.5.1 [deploy]: Add OpenBao-ready gate before main stack start (depends on T1.1.3, T1.1.4, T1.1.5, T1.2.1, T1.3.1) (2026-07-14)
- [x] T1.5.2 [deploy]: Make deploy.sh partial redeploy sidecar-aware (depends on T1.1.3) (2026-07-14)
- [x] **G1.5: Stack bring-up ordering** — integration test (2026-07-14)

### G1.6 [deploy]: Dynamic Postgres engine compatibility
- [x] T1.6.1 [deploy]: Verify bootstrap.sh db-engine SQL against haisir-postgres image (depends on T1.1.1) (2026-07-14)
- [x] **G1.6: Dynamic Postgres engine compatibility** — integration test (2026-07-14) <!-- UNRESOLVED: subgoal test requires a live OpenBao+Postgres instance, unlike G1.2-G1.5's static checks; accepted as a documented limitation, see PLAN.md G1.6. Passed via the documented policy-lint substitute (database/creds/haisir-{backend,worker} read granted; creation SQL statically pgvector/public-compatible); live proof deferred to G2.5. -->

- [x] **G1: Deploy-side reconciliation** — end-to-end test (2026-07-14) <!-- Goal test passed: `docker compose -f common/docker-compose.yml -f common/openbao/docker-compose.openbao.yml config` validates (exit 0) and grep -c OLLAMA_API_KEY common/docker-compose.yml == 0. All 21 G1 tasks done (G1.4 + G1.6 live-proof caveats documented, deferred to G2.5/T2.6.1). -->

## G2 [deploy]: HARD GATE — first-ever live verification
*(depends on ALL 21 tasks of G1: T1.1.1-T1.1.7, T1.2.1-T1.2.4, T1.3.1-T1.3.2, T1.4.1-T1.4.5, T1.5.1-T1.5.2, T1.6.1)*

### G2.1 [deploy]: Stack bring-up with static seal + pinned version
- [x] T2.1.1 [deploy]: Bring up OpenBao stack, confirm healthy (depends on all 21 G1 tasks) (2026-07-14) <!-- LIVE PASS: bao status => Sealed: false, Version: 2.6.0, Seal Type: static, Initialized: true; container healthy; NO manual operator unseal run (static seal auto-unsealed — the novel claim is proven). bootstrap.sh all completed (init + audit + KV + policies + cert-auth roles). Three never-live-tested config bugs surfaced and fixed in-repo as root cause: (1) docker-compose.openbao.yml — openbao image runs uid=100 but named volumes mount root-owned => added openbao-init container chowning data/audit/seal to 100:1000 (mirrors etcd-init); (2) same file — host cert keys are 600/uid-1000, unreadable by uid-100 => added openbao-certs named volume populated by openbao-init (copy+chown+chmod), switched openbao cert mount off the host bind-mount; preserves G1.5 bring-up ordering (no env-setup.sh dependency); also fixed the healthcheck to present the deploy client cert (mTLS listener requires it — bare `bao status -tls-skip-verify` could never pass); (3) openbao-server.hcl — v2.6.0 gates API audit creation behind unsafe_allow_api_audit_creation (CVE-2025-54997) => added declarative `audit "file" "file"` stanza, bootstrap's idempotent check skips the legacy enable; (4) bootstrap.sh — `docker cp` of policy files fails on rootless Docker ("remount-ro ... operation not permitted"; staging/prod are rootless too) => svr() now uses -i and policies write via stdin (`bao policy write <name> -`). Side effects: root CA + openbao mTLS certs generated at $HOME/certs/localhost; recovery keys + root token in common/openbao/.bootstrap-out/dev/server-init.json (gitignored, must be moved offline + deleted). OIDC admin path intentionally skipped in dev (env unset). -->
- [x] **G2.1: Stack bring-up** — integration test (2026-07-14) <!-- Subgoal test: `docker compose -f common/openbao/docker-compose.openbao.yml up -d` then `bao status` reports Sealed: false, Version: 2.6.0 — PASSED live on feature/secrets-openbao-v2 (local dev, APP_ENV=dev). -->

### G2.2 [deploy]: mTLS-bound client authentication
- [x] T2.2.1 [deploy]: Request without client cert is denied (depends on T2.1.1) (2026-07-14) <!-- LIVE PASS: against healthy openbao-dev (127.0.0.1:8200). No-client-cert probe: `curl --cacert ca.pem https://127.0.0.1:8200/v1/sys/health` => curl exit 56, OpenSSL "tlsv13 alert certificate required" — TLS handshake rejected before any HTTP response (tls_require_and_verify_client_cert enforced). Valid-cert probe: same URL with --cert openbao-client-backend.pem --key ...-key.pem => HTTP 200 `{"initialized":true,"sealed":false,"version":"2.6.0",...}`. A stolen token without the matching CA-signed client cert cannot complete the handshake. No file changes. -->
- [x] **G2.2: mTLS-bound client authentication** — integration test (2026-07-14) <!-- Subgoal test: curl without --cert/--key denied at TLS layer; same with valid agent cert succeeds — PASSED live. -->

### G2.3 [deploy]: Audit logging proof
- [x] T2.3.1 [deploy]: Audit device logs a KV secret read (depends on T2.1.1) (2026-07-14) <!-- LIVE PASS: against healthy openbao-dev. Seeded a test marker into secret/haisir/backend (root token + deploy cert for mTLS), then read it as the `backend` MACHINE identity via cert auth (`bao login -method=cert` with the backend client cert). The declarative `audit "file" "file"` device (added in T2.1.1) captured the read: two entries (request+response) with `request.operation: "read"`, `request.path: "secret/data/haisir/backend"`, `auth.display_name: "cert-backend"`, `auth.policies: ["backend","default"]`, `auth.metadata.common_name: "openbao-client-backend"`, and `time: 2026-07-15T00:58:28Z`. Secret VALUES are HMAC'd (log_raw=false) — no plaintext leaked. The requesting identity is the cert-bound machine identity, not root — a stronger proof than root-token reads. No file changes. -->
- [x] **G2.3: Audit logging proof** — integration test (2026-07-14) <!-- Subgoal test: an authenticated KV read produces a same-second audit entry with actor identity, exact KV path, and timestamp — PASSED live. -->

### G2.4 [deploy]: Static-seal auto-unseal after restart
- [x] T2.4.1 [deploy]: Restart container, confirm auto-unseal with no manual step (depends on T2.1.1) (2026-07-14) <!-- LIVE PASS — the single riskiest untested claim of the whole effort, now formally proven. Against healthy openbao-dev (pre-restart: sealed=false, version=2.6.0). `docker restart openbao-dev` then polled `bao status -format=json` every 3s for up to 60s. At t=3s: sealed=false, version=2.6.0, initialized=true — auto-unsealed by the `seal "static"` stanza reading /openbao/seal/unseal.key. The poll loop ran ONLY read-only `bao status` — no `bao operator unseal` was ever issued (zero manual intervention). Container returned to (healthy). The never-live-tested static-seal auto-unseal design change A is proven correct. No file changes. -->
- [x] **G2.4: Static-seal auto-unseal after restart** — integration test (2026-07-14) <!-- Subgoal test: `docker restart openbao` -> poll bao status shows Sealed: false on its own within 60s with zero manual unseal calls — PASSED live (auto-unsealed at ~3s). -->

### G2.5 [deploy]: Dynamic Postgres credentials proof (Phase 3)
- [x] T2.5.1 [deploy]: Issue dynamic Postgres creds via database/creds/*, connect (depends on T2.1.1, T1.6.1) (2026-07-14) <!-- LIVE PASS against the real haisir-postgres image. Prereqs filled in: brought up `registry.haisir.in/haisir-postgres:18.4` as `haisir-db-dev` on haisir-net (PG 18.4 + pgvector, the genuine image — pulled from registry.haisir.in, the real registry; the .env template's haisir-registry:5000 value is stale and MagicDNS is down); seeded secret/haisir/db with the db admin creds; ran `APP_ENV=dev OPENBAO_DB_HOST=haisir-db-dev bootstrap.sh db-engine` (enabled database/ engine, wrote database/config/haisir-postgres + roles haisir-backend/haisir-worker with the T1.6.1 creation SQL). Issued `bao read database/creds/haisir-backend` => username `v-root-haisir-b-...`, password (20 chars, redacted), lease_id `database/creds/haisir-backend/b7Bly1VS1Xk5K7rCnZT6iuQQ`, lease_ttl 3600s. Connected using ONLY the dynamic credential: `psql -h haisir-db-dev -U <dyn> -d haisir_backend -c 'SELECT 1 AS ok'` => `1`, exit 0. db admin password read from the running container env into a var (never printed); KV seeded with a throwaway test admin password. No file changes. haisir-db-dev kept up — T2.5.2 (lease revoke) reuses it. -->
- [x] T2.5.2 [deploy]: Verify lease expiry/rotation revokes access (depends on T2.5.1) (2026-07-14) <!-- LIVE PASS against the same healthy openbao-dev + haisir-db-dev from T2.5.1. Proved the full dynamic-credential lifecycle on a freshly issued credential (T2.5.1's password was only ever in a shell var, gone across calls — so re-issued to be rigorous): (1) `bao read database/creds/haisir-backend` => new user `v-root-haisir-b-kQPvxsBhGruzRyrMgJb4-1784078135` + 20-char password (redacted) + lease `database/creds/haisir-backend/fOD5GYNKpsyDrakfFtovp8m0`; (2) connect BEFORE revoke via `psql -h haisir-db-dev -U <dyn> -d haisir_backend -c 'SELECT 1 AS ok'` => `1`, exit 0; (3) `bao lease revoke database/creds/haisir-backend/fOD5GYNKpsyDrakfFtovp8m0` => "All revocation operations queued successfully!"; (4) connect AFTER revoke with the SAME credential => `psql: error: ... FATAL: password authentication failed for user "v-root-haisir-b-kQPvxsBhGruzRyrMgJb4-1784078135"`, exit 2. Revoked credential is immediately rejected at the DB — the database engine's revocation_statements dropped the role's password, so the lease lifecycle (issue → use → revoke → deny) is end-to-end proven. Cleanup: also revoked the leftover T2.5.1 lease `database/creds/haisir-backend/b7Bly1VS1Xk5K7rCnZT6iuQQ`. No file changes. -->
- [x] **G2.5: Dynamic Postgres credentials proof** — integration test (2026-07-14) <!-- Subgoal test: "credential issued connects, same credential rejected after revoke" — PASSED live. T2.5.1 issued a dynamic cred and connected successfully (SELECT 1 => 1); T2.5.2 revoked that credential's lease and confirmed the SAME connection then fails (FATAL: password authentication failed, exit 2). The database secrets engine's dynamic-credential lifecycle — issue, use, revoke, deny — is end-to-end proven against the real haisir-postgres image. This also closes the G1.6 + G1.4 live-proof deferrals (the dynamic DB engine works live; the Ollama-key static-secret live proof remains T2.6.1). -->

### G2.6 [deploy]: Ollama secrets served via OpenBao templating
- [x] T2.6.1 [deploy]: Verify backend/worker receive Ollama API keys via template, not plaintext (depends on T2.1.1, T1.4.5) (2026-07-14) <!-- LIVE PASS. Seeded secret/haisir/backend (DATABASE_URL + EMBEDDING__/HAITU__OLLAMA_API_KEY), created secret/haisir/worker (DATABASE_URL + EXTRACTION__/EMBEDDING__/HAITU__/GRADING__OLLAMA_API_KEY), and secret/haisir/shared (all throwaway dev values, never printed). Templates/agent HCL/compose wiring needed no logic changes, but three previously-untested live bugs surfaced and were fixed as root cause: (1) vault-agent-backend/worker mounted client certs from the raw host dir (${OPENBAO_CERT_DIR}), which is 600/uid-1000 — unreadable by the agent's non-root image user; switched both to mount the openbao-certs named volume (already correctly chowned by the openbao stack's openbao-init, external:true reference added to common/docker-compose.yml) instead of a host bind. (2) consul-template's `{{ with secret "secret/data/haisir/shared" }}` on a NOT-YET-SEEDED path is a hard error (infinite exponential-backoff retry), not a silently-skipped optional block as assumed — confirms the friction the task brief predicted; fixed by seeding a placeholder key at secret/haisir/shared (same pattern as the audit probe), not a template change. (3) rendered secret files (perms 0640, owner openbao:openbao) were unreadable by the backend/worker containers' fixed non-root UID (65532, chainguard image) — changed both templates' `perms` to 0644 in backend-agent.hcl/worker-agent.hcl (single-purpose per-service tmpfs volume, no cross-service exposure). PROOF: `docker exec haisir-backend-dev python -c "from shared.config import settings; ..."` and an equivalent one-off worker container run (full worker loop needs live Ollama connectivity, out of scope) both show all keys non-empty; SHA-256[:10] of each rendered value matches the independently-fetched KV value for all 6 keys (EMBEDDING/HAITU on backend; EXTRACTION/EMBEDDING/HAITU/GRADING on worker) — byte-for-byte proof of template delivery without printing any secret. `grep -c OLLAMA_API_KEY common/docker-compose.yml` stayed 0 (plaintext never reintroduced). NOTE: PLAN.md's literal test wording (`docker exec backend printenv KEY`) cannot pass as written — pydantic-settings' `env_file` loads the file into the Settings model only, never into the container's OS env (by design, so secrets never surface via `docker inspect`); the image is also shell-less (no `sh`), so even a manual source-then-printenv workaround isn't executable. The hash-comparison method above is the actual live-provable equivalent; flagged to the user, PLAN.md not edited (deploy-side task, cross-repo test-wording change left for a spec-owner decision). Files changed: common/docker-compose.yml (openbao-certs external volume + vault-agent mount), common/openbao/agent/backend-agent.hcl, common/openbao/agent/worker-agent.hcl (perms 0640->0644). No plaintext secrets or file changes to templates themselves. -->
- [x] **G2.6: Ollama secrets served via OpenBao templating** — integration test (2026-07-14) <!-- Subgoal test: keys land in the vault-agent-rendered env files and match the KV-seeded values, not plaintext in compose — PASSED live (see T2.6.1). -->

- [x] **G2: HARD GATE — first-ever live verification** — end-to-end test (2026-07-14) <!-- All 7 G2 tasks (T2.1.1, T2.2.1, T2.3.1, T2.4.1, T2.5.1, T2.5.2, T2.6.1) PASSED live in one continuous run against feature/secrets-openbao-v2 (local dev, APP_ENV=dev) — zero manual seal intervention at any point (static-seal auto-unseal proven in T2.4.1; T2.6.1 required no unseal step either). G2 clears; G3 backend fail-fast tasks unlock. -->

## G3 [backend]: Backend fails fast instead of running with dummy secrets
*(depends on ALL 7 tasks of G2: T2.1.1, T2.2.1, T2.3.1, T2.4.1, T2.5.1, T2.5.2, T2.6.1)*

### G3.1 [backend]: CSRF secret and database_url require real values
- [x] T3.1.1 [backend]: Remove default="dummy" from CSRFSettings.secret (depends on all 7 G2 tasks) (2026-07-15) <!-- secret: str = Field(min_length=1), now required. Also fixed a real bug: csrf: CSRFSettings = CSRFSettings() was a bare instantiation evaluated at class-body/import time -- once secret had no default, that call would raise ValidationError on EVERY import regardless of env state. Changed to csrf: CSRFSettings = Field(default_factory=CSRFSettings) (deferred call, only invoked when no CSRF__* env data present) with a scoped mypy ignore. Verified empirically against this repo's pydantic 2.13.4/pydantic-settings 2.14.2: no env -> ValidationError; CSRF__SECRET set -> succeeds; partial CSRF__* env (secret still missing) -> ValidationError. Non-pytest callers of Settings() also needed wiring: Jenkinsfile's bare `alembic upgrade head` CI stage (outside pytest/conftest) now passes CSRF__SECRET inline; tests/conftest.py sets CSRF__SECRET via os.environ.setdefault mirroring the existing DATABASE_URL pattern. Local src/.env (gitignored, untracked) also given a dev placeholder for uvicorn/alembic scripts -- not part of this diff. OAuthSettings/KeycloakSettings have the identical bare-instantiation defect, currently latent (no required fields yet) -- T3.2.1 will need the same default_factory treatment. -->
- [x] T3.1.2 [backend]: Remove default="dummy" from Settings.database_url (depends on T3.1.1) (2026-07-15) <!-- database_url: str = Field(min_length=1), now required. Plain top-level BaseSettings field, no nested-model bare-instantiation hazard like csrf -- one-line change. mypy strict flagged the module-level `settings = Settings()` singleton call once database_url had no default (no pydantic mypy plugin configured); added `# type: ignore[call-arg]`, verified necessary by removing it and confirming mypy fails without it. Test (tests/unit/shared/test_config.py::TestSettings::test_database_url_required) clears both DATABASE_URL and CSRF__SECRET and uses Settings(_env_file=None) -- verified src/.env (gitignored, untracked in this devcontainer) has a real DATABASE_URL that would otherwise silently mask the missing-env-var case locally (passes in CI, no-ops locally) without _env_file=None. Clearing both fields in one test also directly proves G3.1's subgoal test. -->
- [x] **G3.1: CSRF secret and database_url require real values** — integration test (2026-07-15) <!-- Subgoal test: Settings() with CSRF__SECRET and DATABASE_URL both unset raises ValidationError. Verified by direct import-time reproduction: `import shared.config` with both env vars absent and dotenv disabled raises pydantic_core.ValidationError (CSRFSettings.secret Field required) at the module-level `settings = Settings()` singleton line -- fail-fast holds even before any test runs. Also covered by the new regression test above. Both G3.1 children (T3.1.1, T3.1.2) done. -->

### G3.2 [backend]: Keycloak admin credentials require real values
- [x] T3.2.1 [backend]: Remove default="" from admin_client_id (depends on all 7 G2 tasks) (2026-07-15) <!-- admin_client_id: str = Field(min_length=1), now required. Same eager-instantiation defect as T3.1.1's csrf field but one level deeper: OAuthSettings.keycloak: KeycloakSettings = KeycloakSettings() (bare) breaks at OAuthSettings class-definition time, which happens BEFORE Settings is even defined in the file -- fixed with keycloak: KeycloakSettings = Field(default_factory=KeycloakSettings) (needs # type: ignore[arg-type], mypy strict) and Settings.oauth: OAuthSettings = Field(default_factory=OAuthSettings) (verified NO type:ignore needed here -- mypy accepts it once the nested field carries the factory). Verified end-to-end with a runtime repro: env present -> factory bypassed, value flows through nesting; env absent -> factory invoked, raises; only a sibling var set (e.g. OAUTH__KEYCLOAK__REALM) with ADMIN_CLIENT_ID still unset -> still raises. tests/conftest.py and Jenkinsfile's bare alembic CI stage both updated with OAUTH__KEYCLOAK__ADMIN_CLIENT_ID (this non-pytest-CI gap was caught proactively this time, having been missed and fixed reactively in T3.1.1). admin_client_secret intentionally untouched (T3.2.2). -->
- [x] T3.2.2 [backend]: Remove default="" from admin_client_secret (depends on T3.2.1) (2026-07-15) <!-- admin_client_secret: str = Field(min_length=1), now required. Unlike its 3 predecessors in this series, needed NO default_factory/nesting fix -- the eager-instantiation defect at OAuthSettings.keycloak/Settings.oauth was already fixed in T3.2.1, and a second required leaf field on KeycloakSettings doesn't change when/how those wrappers get called. Pure one-line change, verified. tests/conftest.py + Jenkinsfile's bare alembic CI stage updated with OAUTH__KEYCLOAK__ADMIN_CLIENT_SECRET (4th var in this pattern, every task in the series has needed it). Test additions used `# noqa: S106` (ruff hardcoded-password rule on the `admin_client_secret=` kwarg, matching this file's existing `CSRFSettings(secret=...)` precedent) -- explicitly did NOT add `# pragma: allowlist secret` despite a reviewing agent claiming it was needed; verified directly via `detect-secrets scan` that this kwarg-in-constructor-call pattern doesn't trigger a finding (confirmed the existing CSRFSettings(secret=...) line has no such pragma either), so that claim was rejected before implementation. As the last task in G3.2, also added tests/unit/shared/test_config.py::TestSettings::test_oauth_keycloak_admin_credentials_required (Settings(_env_file=None) with both OAUTH__KEYCLOAK__ADMIN_CLIENT_ID and _SECRET unset) to directly prove G3.2's subgoal test, following the T3.1.2 precedent of committing the bubble-up proof permanently rather than deferring it. -->
- [x] **G3.2: Keycloak admin credentials require real values** — integration test (2026-07-15) <!-- Subgoal test: Settings() with KEYCLOAK_ADMIN_CLIENT_ID/KEYCLOAK_ADMIN_CLIENT_SECRET unset raises ValidationError. Verified by direct reproduction: with DATABASE_URL, CSRF__SECRET, OAUTH__KEYCLOAK__ADMIN_CLIENT_ID, and OAUTH__KEYCLOAK__ADMIN_CLIENT_SECRET all unset and dotenv disabled, `Settings()` raises pydantic ValidationError. Also covered permanently by the new regression test above. Both G3.2 children (T3.2.1, T3.2.2) done. -->

### G3.3 [backend]: Fail-fast behavior proven and documented
- [x] T3.3.1 [backend]: Add test asserting Settings() raises when required secrets unset (depends on T3.1.2, T3.2.2) (2026-07-15) <!-- Pure test-authoring task, no production code changes (all 4 fields already required from T3.1.1/T3.1.2/T3.2.1/T3.2.2). Added tests/unit/shared/test_config.py::test_settings_fail_fast_on_missing_secrets as a MODULE-LEVEL function (deliberate exception to this file's otherwise 100%-class-based convention) so the task's literal "Done when" pytest node-id (`test_config.py::test_settings_fail_fast_on_missing_secrets`, no class prefix) resolves exactly as specified. Clears all 4 REAL env var names (DATABASE_URL, CSRF__SECRET, OAUTH__KEYCLOAK__ADMIN_CLIENT_ID, OAUTH__KEYCLOAK__ADMIN_CLIENT_SECRET) -- PLAN.md's own Build line uses stale single-underscore shorthand (CSRF_SECRET, KEYCLOAK_ADMIN_CLIENT_ID) that doesn't match any real var, caught and corrected. Uses Settings(_env_file=None) per this file's established idiom (src/.env sets all 4 locally). Verified: literal Done-when command shows "1 passed" (exits non-zero only due to a pre-existing, repo-wide --cov=src + fail_under=100 addopts artifact on any narrowly-scoped run -- confirmed this is NOT specific to this test, judge by "1 passed" not shell exit code). `-k fail_fast` (G3.3's own subgoal test) also shows "1 passed, 37 deselected". G3.3 NOT yet fully done -- its subgoal test also requires the header comment at config.py:8-10 to stop contradicting the code, which is T3.3.2's job, still pending. -->
- [x] T3.3.2 [backend]: Correct header comment to match enforced behavior (depends on T3.3.1) (2026-07-15) <!-- Pure comment edit, zero runtime impact. Updated src/shared/config.py:8-10's "REQUIRED Environment Variables (no defaults)" list to add OAUTH__KEYCLOAK__ADMIN_CLIENT_ID and OAUTH__KEYCLOAK__ADMIN_CLIENT_SECRET (previously only listed DATABASE_URL/CSRF__SECRET, stale since T3.2.1/T3.2.2). Scope deliberately narrow (lines 8-10 only, per literal Build line) -- both planner and challenger checked the rest of the file (inline field comments, other docs) for similar contradictions and found none needing fixes. No new tests (task's own Test line is a manual read-and-confirm check). NOTE: a planning subagent this round triggered a security-classifier warning by printing raw src/.env secret values into its own transcript while checking key presence -- user confirmed low risk (local gitignored dev-placeholder file) but set a standing rule: always do key-name-only checks going forward, saved to memory. -->
- [x] **G3.3: Fail-fast behavior proven and documented** — integration test (2026-07-15) <!-- Subgoal test: `pytest tests/unit/shared/test_config.py -k fail_fast` passes (1 passed, 37 deselected) AND the header comment at config.py:8-10 accurately describes enforced behavior (now lists all 4 required fields) -- both verified. Both G3.3 children (T3.3.1, T3.3.2) done. -->

- [x] **G3: Backend fails fast** — end-to-end test (2026-07-15) <!-- Goal test: "Starting the backend with CSRF_SECRET/DATABASE_URL/Keycloak admin env vars unset causes Settings() instantiation to raise immediately at import time; starting it with vault-agent-rendered values present succeeds." Verified BOTH directions by direct reproduction: (1) all 4 vars unset + dotenv disabled -> Settings() raises pydantic.ValidationError; (2) all 4 vars set to valid values -> Settings() constructs successfully. All 6 G3 tasks done (G3.1: T3.1.1+T3.1.2; G3.2: T3.2.1+T3.2.2; G3.3: T3.3.1+T3.3.2). BR-SEC-019 closed: haisir-backend no longer runs with dummy/empty-string secret defaults for CSRF, database, or Keycloak admin credentials. -->

- [ ] **G3: Backend fails fast** — end-to-end test

## G4: Close-out and merge
*(entry point T4.1.1 depends on all 7 G2 tasks + all 6 G3 tasks)*

### G4.1 [deploy]: Full-stack gate test
- [x] T4.1.1 [deploy]: Combined smoke test — reconciled stack + fail-fast backend together (depends on all 7 G2 + all 6 G3 tasks) (2026-07-15) <!-- LIVE PASS, targeted (not literal full-cold) restart: built registry.haisir.in/haisir-backend:openbao-failfast from haisir-backend feature/secrets-openbao-v2 (commit 216d0f4, the G3 fail-fast changes), then recreated only backend+worker+vault-agent-backend/worker against the already-live openbao-dev stack (apisix/keycloak/postgres/etcd/pgadmin/dockhand left untouched — full-stack cold restart would have been needlessly disruptive to other running dev work; user-approved scope narrowing). Surfaced and fixed 2 real, previously-untested cross-repo integration gaps as root cause: (1) G1.4's vault-agent templates (backend.env.ctmpl, worker.env.ctmpl, + both .dynamic.ctmpl variants) never covered G3's 3 new required fields — CSRF__SECRET and OAUTH__KEYCLOAK__ADMIN_CLIENT_SECRET had template stanzas but were never seeded into KV, and OAUTH__KEYCLOAK__ADMIN_CLIENT_ID had no template stanza at all; also newly discovered that worker imports the same shared.config.settings singleton as backend, so worker needs all 3 fields too, not just backend — fixed by adding the missing template stanzas (all 4 .ctmpl files) and bao kv patch-ing throwaway dev values into secret/haisir/{backend,worker} (user-approved use of the dev root token, the same mechanism bootstrap.sh itself uses for all KV seeding). (2) worker's DATABASE_URL (host `db`, the compose service name) failed DNS resolution because the live Postgres (haisir-db-dev, manually started in T2.5.1, never brought up through this compose project) only had the `haisir-db-dev` container-name alias, not the `db` service alias backend/worker's rendered secrets expect — fixed via `docker network disconnect`+`connect --alias db` on the already-running container (no restart, no data risk). Also ran Alembic migrations (user-approved) against haisir-db-dev via `alembic.config.Config`/`command.upgrade` inside the healthy backend container, since that ad-hoc DB had never been migrated (separate pre-existing gap, unrelated to secrets). PROOF: `docker ps` shows haisir-backend-dev AND haisir-worker-dev both (healthy) on the openbao-failfast image, worker's poll loops (extraction/essay-grading/RAG-outbox) running cleanly against the real DB, zero ValidationError/restart-loop from config. Files changed: common/openbao/agent/templates/{backend,worker}.env.ctmpl + both .dynamic.ctmpl (added missing stanzas); dev/.env (added EXTRACTION__MODEL_SPEC placeholder — worker's own non-secret startup check, unrelated to OpenBao, no local Ollama wired up in this dev stack). KV/network/migration changes are live-only, not file diffs. -->
- [x] **G4.1: Full-stack gate test** — integration test (2026-07-15) <!-- Subgoal test: docker compose ps shows backend/worker healthy with no restart loop from a config ValidationError — PASSED live (see T4.1.1). Both G1-G2's deploy work and G3's backend fail-fast work now proven compatible together, closing 2 real integration gaps neither G2 nor G3 individually could have caught. -->

### G4.2 [deploy]: Ops runbooks
- [x] T4.2.1 [deploy]: Reduce per-env .env files to non-secret config only (depends on T4.1.1) (2026-07-15) <!-- Runbook executed by the user directly against the real staging/.env and prod/.env (2026-07-15): removed all 9 leftover plaintext lines now sourced exclusively from OpenBao (DATABASE_URL, CSRF__SECRET, OAUTH__KEYCLOAK__ADMIN_CLIENT_ID/_SECRET, OLLAMA_API_KEY [bare, confirmed dead -- grepped haisir-backend + haisir-deploy, nothing reads it], EXTRACTION__/GRADING__/EMBEDDING__/HAITU__OLLAMA_API_KEY). First pass only commented the lines out -- caught during verification that commented lines still carried the real plaintext values (checked value-length only, never printed actual secrets, per standing key-safe-check rule) -- user then fully deleted all 9 lines from both files on request. Verified (key-name-only checks, both active and commented forms): zero occurrences of all 9 target keys in either file. Remaining secret-shaped vars (POSTGRES_PASSWORD, KEYCLOAK_POSTGRES_PASSWORD, KEYCLOAK_ADMIN_PASSWORD, KC_DB_PASSWORD) correctly untouched -- Phase 5.6 scope, not this task. Live functional verification used dev/.env as an explicit, user-approved PROXY for the unreachable staging environment ("we can't bring up staging, need a proper release pipeline first" -- not yet built): removed the one overlapping leftover (OAUTH__KEYCLOAK__ADMIN_CLIENT_ID/_SECRET, the only 2 of the 9 keys dev/.env had to begin with) from dev/.env, recreated haisir-backend-dev/haisir-worker-dev in the live local stack -- both came up/stayed (healthy), confirming they now source these values purely from OpenBao. A real staging bring-up with the edited staging/.env remains untested (no release pipeline yet) -- same documented-limitation pattern as G1.4/G1.6's live-proof deferrals earlier in this phase. -->
- [x] T4.2.2 [deploy]: Rotate every existing secret value (depends on T4.2.1, T2.5.2) (2026-07-15) <!-- Runbook executed by the user against the live local dev stack (staging/prod deferred, same "no release pipeline yet" limitation as T4.2.1): rotated all 9 backend/worker KV fields. CSRF__SECRET: new shared random value patched into secret/haisir/{backend,worker}. OAUTH__KEYCLOAK__ADMIN_CLIENT_SECRET: regenerated at the source (Keycloak admin console "Regenerate Secret"), old value invalidated by Keycloak itself, new value patched into KV. 4x *_OLLAMA_API_KEY: rotated to new throwaway dev placeholders (not real-auth-backed in this dev stack -- user's own call, consistent with how they were originally seeded). DATABASE_URL / Postgres role password: user ran ALTER ROLE haisir WITH PASSWORD on the live haisir-db-dev, then patched the new connection string into both secret/haisir/backend and secret/haisir/worker. LIVE PROOF of the DB rotation specifically (the one category with a real, testable reject-old/accept-new check): pre-rotation baseline confirmed old password worked (asyncpg connect from haisir-backend-dev over TCP to db:5432); post-rotation the SAME old password was confirmed REJECTED over the same TCP path (the user's own first attempt used `docker exec haisir-db-dev psql` with no -h flag, which connects over the container's local Unix socket -- Postgres's default trust auth for local socket connections ignores PGPASSWORD entirely, giving a false "still works" result; corrected by testing from a DIFFERENT container over the real TCP path, matching how backend/worker actually connect -- same asyncpg-from-haisir-backend-dev method already proven in T2.5.2). CAUGHT DURING VERIFICATION: `docker compose up -d --no-deps backend worker` after a vault-agent restart did NOT actually restart backend/worker -- compose only recreates a container when its own config changes, and the secret lives in a vault-agent-rendered FILE (not a compose-level env var), so compose saw no diff and left the already-running (stale-secret) processes alone. Required an explicit `docker restart haisir-backend-dev haisir-worker-dev` to force a real process restart and pick up the re-rendered file -- a real operational gotcha for any future rotation, not just this one. POST-ROTATION PROOF: both containers came up (healthy) fresh; worker's own poll loops (visible in logs) successfully INSERTed a heartbeat row and ran extraction_jobs SELECTs against the real DB immediately after restart -- since the old DB password is independently confirmed rejected, this is live proof worker is running on the new rotated credential, not just "didn't crash." Key-safe check (names only, no values) confirmed all 6 backend / 8 worker fields still render correctly post-rotation, nothing broken. No values were ever printed to the conversation transcript at any point (user retrieved/verified secrets themselves via commands run in their own terminal; I only ever compared lengths/names or used values directly inside single non-echoing command chains). -->
- [x] **G4.2: Ops runbooks** — integration test (2026-07-15) <!-- Both G4.2 children done. T4.2.1: real per-env .env files (staging/prod) reduced to non-secret config, verified secret-shaped values fully removed (not just commented). T4.2.2: all 9 backend/worker secrets rotated in OpenBao KV, DB-credential rotation live-proven end-to-end (old rejected, new accepted, real app processes running on the new credential post-restart). Both runbooks executed by the user directly; verification done via key-safe checks and live connection tests, never exposing secret values in this conversation. Staging/prod live verification remains deferred pending a real release pipeline -- documented limitation, not a gap in the dev-stack proof. -->

### G4.3: Security review pass 1 — automated skill
- [x] T4.3.1 [deploy]: Run security-review skill against haisir-deploy diff (depends on T4.1.1) (2026-07-15) <!-- Ran the security-review methodology (skill invocation itself only operates on the session's fixed cwd repo, haisir-specs — replicated its exact 3-phase methodology via a general-purpose sub-agent scoped correctly to `git diff main...feature/secrets-openbao-v2` in haisir-deploy, 2456 lines/28 files). One confidence-8 finding: static-seal auto-unseal key is colocated with the encrypted raft data it protects (openbao-seal + openbao-data volumes, same host) — anyone with raw host/volume access bypasses the entire mTLS/policy ACL model. Verified backup.sh does NOT compound this (API-driven encrypted raft snapshot only, not a raw volume copy). Evaluated and accepted as an inherent, already-considered trade-off of static seal vs. external KMS/transit-unseal (not a new bug from this diff) rather than reopening G1.2/G2.4 — see decisions.md 2026-07-15 entry for full reasoning + the G4.2 host-hardening follow-up this creates. -->
- [x] T4.3.2 [backend]: Run security-review skill against haisir-backend diff (depends on T4.1.1) (2026-07-15) <!-- Same replicated methodology, scoped to haisir-backend's `git diff main...feature/secrets-openbao-v2` (299 lines/4 files: Jenkinsfile, src/shared/config.py, tests/conftest.py, tests/unit/shared/test_config.py). No findings at confidence >=8. Verified: all 4 hardened fields (csrf.secret, database_url, oauth.keycloak.admin_client_id/_secret) are read exclusively through the settings singleton in every consumer (no os.getenv bypass); no try/except swallows the ValidationError anywhere Settings() is constructed; default_factory nesting is atomic (no partially-validated Settings object can leak through); pydantic's missing/string_too_short error paths never surface the actual secret value. -->
- [x] **G4.3: Security review pass 1** — integration test (2026-07-15) <!-- Subgoal test: zero unresolved high/critical findings against either diff. Backend: 0 findings. Deploy: 1 finding, evaluated and formally accepted as documented risk (not "unresolved" — a considered decision with a recorded follow-up), not a code defect to fix. Both G4.3 children done. -->

### G4.4: Security review pass 2 — independent adversarial review
- [x] T4.4.1 [deploy]: Adversarial review of trust boundaries/secret-zero/policy/HCL (depends on T4.1.1) (2026-07-15) <!-- Independent manual review (fresh sub-agent, not briefed on T4.3.1's finding, per decisions.md's "run without reference to each other's findings"). All 4 required focus areas reached explicit verdicts: (1) secret-zero bootstrap/storage — no issue, root token/recovery keys never touch stdout, land in a chmod-700-dir/chmod-600-file that's gitignored, BAO_TOKEN always forwarded by name to docker exec (never in argv/ps); (2) policy least-privilege — no issue, backend/worker/deploy are each scoped read-only to exactly their own paths, admin's broader grants are gated by Keycloak-realm-role bound_claims; (3) HCL/config misconfiguration — no issue, strict mTLS (tls_require_and_verify_client_cert, tls_min_version 1.2), static-seal key is /dev/urandom-sourced/chmod-600/written-once, audit device declared statically specifically to avoid CVE-2025-54997; (4) trust-boundary/escalation — no issue, per-identity cert volumes (commit b5e3c2d) + auth/cert role pinning to exact registered leaf cert prevents a compromised worker sidecar from presenting its cert and getting backend/admin policy. One Low-severity finding: TOCTOU on common/scripts/deploy-lib.sh's remote `.env.runtime` render (`cat .env >file && chmod 600 file` left a brief window where the file existed at the process umask's default mode, readable by another local account on a multi-tenant deploy host, before being locked to 600) — FIXED as root cause: file is now created at mode 600 via `(umask 077 && :>file)` before any content is written, so it's never briefly wider-than-600 (matches the mktemp-based pattern env-setup.sh's local-host equivalent already uses). File changed: common/scripts/deploy-lib.sh (`bash -n` syntax-checked). -->
- [x] T4.4.2 [backend]: Adversarial review of fail-fast correctness and log hygiene (depends on T4.1.1) (2026-07-15) <!-- Independent manual review (fresh sub-agent, not briefed on T4.3.2's "no findings" result). Conclusion (a) fail-fast-bypass: no issue — grepped all of src/ for every consumer of the 4 required fields, confirmed exclusively through the settings singleton (no os.getenv bypass, no bypassing Settings()/CSRFSettings()/KeycloakSettings() construction, no exception-swallowing), worker/__main__.py imports the singleton before _validate_startup() runs too. Conclusion (b) secret-leakage: ONE HIGH-severity finding, independently reproduced (not just taken on the sub-agent's word) — pydantic's ValidationError for a "missing field" error embeds the raw `input` dict of EVERY SIBLING field's actual value at that nesting level (e.g. DATABASE_URL missing but CSRF__SECRET/OAUTH__KEYCLOAK__ADMIN_CLIENT_ID/_SECRET set -> e.json() contains all 3 real secret values in cleartext; str(e) leaks a truncated tail of one). Since the module-level `settings = Settings()` singleton had no try/except, this was an uncaught exception -> Python's default excepthook prints str(e) to stderr -> lands in docker logs/CI console output. Verified this is a genuine diff-introduced regression (before this diff all 4 fields had defaults, so ValidationError could never be raised for them here) and verified the app's OWN JSON logging (logutil.py's setup_logging()) can't even be active at this failure point (it itself imports shared.config.settings), so the practical exposure is the default Python traceback, not this app's structured logger -- still a real, confirmed leak (verified the truncated secret substring appears in str(e) directly). FIXED as root cause, not deferred: wrapped the singleton construction in try/except ValidationError, re-raising RuntimeError with only e["loc"] field paths joined, `from None` to suppress the original secret-laden exception from ever appearing in the traceback chain. Verified via 3 direct reproductions (partial-missing leaks nothing + reports correct field, all-4-missing lists exactly the true gaps, all-4-set still succeeds) plus a new subprocess-based regression test (test_module_import_failure_does_not_leak_sibling_secrets) asserting the secret substrings never appear in combined stdout+stderr. Full suite: 43 passed (was 42); ruff + mypy clean. Files changed: src/shared/config.py (ValidationError import + try/except wrap), tests/unit/shared/test_config.py (+1 test, +4 imports). -->
- [x] **G4.4: Security review pass 2** — integration test (2026-07-15) <!-- Subgoal test: no unresolved finding in any focus area, either repo. Deploy: 1 Low finding, fixed as root cause (TOCTOU). Backend: 1 High finding, fixed as root cause (secret leak via uncaught ValidationError) + regression test added. Both reviews ran independently of T4.3's findings per decisions.md. Both G4.4 children done, zero unresolved findings remain. -->

### G4.5 [specs]: Close-out documentation
- [ ] T4.5.1 [specs]: Add progress.md entry for OpenBao cutover (depends on T4.6.1, T4.6.2)
- [ ] **G4.5: Close-out documentation** — integration test

### G4.6: Merge to main
- [ ] T4.6.1 [deploy]: Merge feature/secrets-openbao-v2 → haisir-deploy main (depends on T4.2.1, T4.2.2, T4.3.1, T4.4.1)
- [ ] T4.6.2 [backend]: Merge backend fail-fast branch → haisir-backend main (depends on T4.3.2, T4.4.2, T4.6.1 [deploy] — backend cannot merge ahead of deploy)
- [ ] **G4.6: Merge to main** — integration test

- [ ] **G4: Close-out and merge** — end-to-end test

- [ ] **ROOT: OpenBao live as hAIsir's secrets authority** — acceptance test

## Ready now
Tasks with no pending dependencies — can be started immediately:
- T4.2.1 [deploy]: Reduce per-env `.env` files to non-secret config only (deps: T4.1.1 ✓ — done 2026-07-15). Scope note: an inventory pass (2026-07-15, during the T4.1.1 mid-course scope discussion) found this phase's OpenBao migration only ever covered `secret/haisir/{backend,worker,db}` — `staging/.env`/`prod/.env` still carry plaintext duplicates of the now-OpenBao-sourced backend/worker keys (`CSRF__SECRET`, `OAUTH__KEYCLOAK__ADMIN_CLIENT_ID`/`_SECRET`, the 4 `*_OLLAMA_API_KEY` vars) — that cleanup is T4.2.1's real scope. apisix/keycloak/postgres-server/pgadmin/session/google-oauth secrets were **never in this phase's scope at all** (no KV paths, no templates exist for them) — carved out to a new Phase 5.6, see `phases.md`. **Still needs your explicit go-ahead before touching real per-env files, per our earlier agreement** — I'll show the diff first.

All of G1, G2, G3 are fully complete. **G4.1 done** (2026-07-15) — combined smoke test found and fixed 2 cross-repo integration gaps (missing vault-agent template coverage for G3's 3 new required fields; a DNS-alias mismatch on the `db` compose service name). **G4.3 done** (2026-07-15) — automated security review: 0 findings backend, 1 finding deploy (static-seal host-compromise risk) formally accepted as documented risk, not a blocker — see decisions.md. **G4.4 done** (2026-07-15) — independent adversarial review, run without reference to G4.3's findings: deploy got 1 Low finding (TOCTOU on `deploy-lib.sh`'s remote `.env.runtime` creation), backend got 1 HIGH finding (pydantic `ValidationError` on a *partial* missing-secret misconfiguration leaks sibling secrets' plaintext values via `e.json()`/into the default uncaught-exception traceback — a genuine diff-introduced regression, since before this diff none of the 4 fields could ever raise `ValidationError`). Both fixed as root cause, not deferred: `deploy-lib.sh` now creates the runtime env file at mode 600 atomically before writing; `haisir-backend`'s `src/shared/config.py` now wraps the module-level `Settings()` singleton in try/except, re-raising with only field names via `RuntimeError(...) from None` (suppresses the secret-laden original exception from the traceback chain) — verified via 3 direct reproductions + a new subprocess-based regression test, full suite 43 passed, ruff+mypy clean. **Both G4.3 and G4.4 ran independently of each other, satisfying the "symmetric depth, not anchored" requirement.** Zero unresolved findings remain across both review passes.

**G4.2 done** (2026-07-15) — see its note above: real `staging`/`prod` `.env` files cleaned, all 9 backend/worker secrets rotated with a live-proven DB-credential rotation (old rejected, new accepted, real app traffic running on the new credential).

**G4.1, G4.2, G4.3, G4.4 are ALL now done.** Only `G4.6` (merge to main) and `G4.5` (close-out doc, depends on `G4.6`) remain. `T4.6.1` [deploy] deps (`T4.2.1`✓, `T4.2.2`✓, `T4.3.1`✓, `T4.4.1`✓) are all satisfied — **ready now**, blocked only on committing the uncommitted work below and getting explicit user go-ahead to merge (standing policy, not autonomous). `T4.6.2` [backend] then follows once `T4.6.1` lands.

**Scope decision (2026-07-15):** the apisix/keycloak/postgres-server/session/google-oauth secrets gap is deliberately NOT part of this phase — scoped out to **Phase 5.6** (full `.env` elimination, all remaining services) per user direction, inserted between this phase and the existing Phase 6 backlog. See `phases.md` Phase 5.6 entry and the `/plan` prompt saved for that cycle.

**Uncommitted changes, both repos** (not yet committed — this session did live fixes as findings surfaced, per this phase's established root-cause-fix pattern; T4.6.1/T4.6.2 need these committed first):
- `haisir-deploy` (on `feature/secrets-openbao-v2`): `common/openbao/agent/templates/{backend,worker}.env.ctmpl` + both `.dynamic.ctmpl` (T4.1.1's missing-template-stanza fix), `common/scripts/deploy-lib.sh` (T4.4.1's TOCTOU fix). `dev/.env` and real `staging/.env`/`prod/.env` also have uncommitted T4.2.1/T4.2.2 changes (dev-only `EXTRACTION__MODEL_SPEC` placeholder + Keycloak-admin-creds removal; staging/prod's 9-line secret cleanup) — note per-env `.env` files are typically gitignored in this repo, confirm before assuming these need a commit at all vs. being purely runtime state.
- `haisir-backend`: the real target is the **`backend` devcontainer** (`/workspaces/haisir-backend`, not the host clone at `/home/gulzar/Workspace/haisir-backend` — skills/tools must run there). `src/shared/config.py` + `tests/unit/shared/test_config.py` are uncommitted there on top of `216d0f4` (T4.4.2's secret-leak fix + regression test, verified passing in-container: 43 tests, ruff+mypy clean).
- Live dev stack (openbao-dev, haisir-db-dev, vault-agent-backend/worker, haisir-backend-dev, haisir-worker-dev) is running the `openbao-failfast` image built from host-clone commit `216d0f4` — does NOT include the T4.4.2 config.py fix (that fix only exists in the `backend` devcontainer so far, never rebuilt into a running image). Not blocking — T4.4.2 only changes failure-path log hygiene, not anything T4.1.1/T4.2.2 already proved live. Also now running on rotated secrets per T4.2.2.
