# Progress

> Auto-generated from PLAN.md. Updated by `/implement` in each code repo.
> Last baselined: backend:a6cba00 frontend:afacb33 deploy:ee39f9c (2026-07-10 — frontend T5.4 HaituDoubtPanel enabled for Home Study topics (enrollment_id omitted from topic-doubt body for parent source, revocation-aware gating copy, green accent) + T6.2 source-aware Home Study empty state + T6.3 Home Study content-viewing verification (markdown rendering in ContentViewer, dashboard→Home-Study-tab deep link fix, green tab/topic-title theme accent); backend T5.1+T5.2 parent-link hAITU gate)

## G1 [backend/frontend/specs]: Parent–child linking lifecycle
- [x] T1.1 [backend]: Student link-code generation + current-code endpoints (2026-07-09)
- [x] T1.2 [backend]: Student parent-link list + revoke endpoints (2026-07-09)
- [x] T1.3 [backend]: Parent children list + parent-side revoke endpoints (2026-07-09)
- [x] T1.4 [backend]: Max-10-children cap on redemption (BR-PAR-016) (2026-07-09)
- [x] T1.5 [frontend]: /profile page (new) — link-code + linked-parents sections (2026-07-09)
- [x] T1.6 [specs]: Link-code semantics + physical endpoint paths (2026-07-09)
- [x] T1.7 [backend]: integration test: full link round-trip (depends on T1.1, T1.2, T1.3, T1.4) (2026-07-09) — `tests/integration/routes/test_parent_link_round_trip_integration.py`; collects + self-skips clean, PASS confirmed via CI (V39 DB env)
- [x] **G1: Parent–child linking lifecycle** — all tasks complete (2026-07-09)

## G2 [frontend/specs]: Parent workspace shell + route guard
- [x] T2.1 [frontend]: ParentRouteGuard component (2026-07-09)
- [x] T2.2 [frontend]: /parent layout + P-home dashboard (depends on T2.1, T1.3) (2026-07-10)
- [x] T2.3 [frontend]: /parent/link-child page (P-link) (depends on T2.1, T1.4) (2026-07-10)
- [x] T2.4 [frontend]: Parent header nav + redirect-matrix verification (depends on T2.2) (2026-07-10)
- [x] T2.5 [specs]: /parent route-guard slice into target 05_parent.md (2026-07-09)
- [x] T2.6 [frontend]: Onboarding parent-ready CTA dead-link fix (depends on T2.3) (2026-07-10)
- [ ] **G2: Parent workspace shell** — redirect-matrix component tests (in T2.4) + browser pass recorded in T7.2 walkthrough (2026-07-10: all G2 leaf tasks T2.1-T2.6 done; T2.4 redirect-matrix + header-nav component tests green; in-browser walkthrough still pending T7.2)

## G3 [backend/frontend/specs]: Parent curriculum builder

### G3.1 Curriculum structure API
- [x] T3.1 [backend]: V40 migration — source_node_id lineage column + partial unique index (2026-07-09)
- [x] T3.2a [backend]: Parent node CRUD endpoints (owner-scoped) (depends on T3.1) (2026-07-10)
- [x] T3.2b [backend]: Node hierarchy validation for parent trees (depends on T3.2a) (2026-07-10)
- [x] T3.2c [backend]: Node cascade delete + exam-session guard (depends on T3.2b) (2026-07-10)
- [x] T3.3 [backend]: Parent read access to platform tree (adopt browse) (2026-07-10)
- [x] T3.4 [backend]: Adopt/clone service + endpoint (409 idempotent) (depends on T3.1, T3.2a, T3.3) (2026-07-10)
- [x] T3.5 [backend]: Parent topic CRUD + draft/live publish (depends on T3.2a) (2026-07-10)
- [x] **G3.1: Curriculum structure API** — T3.13 [backend] integration test: cross-owner 404 sweep (2026-07-10) — `tests/integration/routes/test_g3_1_cross_owner_404_sweep_integration.py`; collects + self-skips clean locally (no INTEGRATION_DB_URL), full real-DB run pending CI

### G3.2 Content authoring API
- [x] T3.6 [backend]: Parent instant content create + owner-scoped PATCH/DELETE (depends on T3.5) (2026-07-10)
- [x] **G3.2: Content authoring API** — round-trip covered by T3.6 Done-when (2026-07-10)

### G3.3 Builder UI
- [x] T3.7 [frontend]: features/parent curriculum API client + types (depends on T3.2a, T3.5, T3.6) (2026-07-10)
- [x] T3.8 [frontend]: /parent/curriculum builder page (depends on T3.7, T2.1) (2026-07-10)
- [x] T3.9 [frontend]: Adopt modal (depends on T3.7, T3.3, T3.4) (2026-07-10)
- [x] T3.10 [frontend]: Parameterize shared content components (admin↔parent) (2026-07-10)
- [x] T3.11 [frontend]: P-topic Topic Content Manager page (depends on T3.10, T3.7, T3.5, T3.6) (2026-07-10)
- [x] T3.12 [specs]: 05_parent.md + 01_data_model.md builder/V40 updates (depends on T3.4, T3.6) (2026-07-09)
- [ ] **G3.3: Builder UI** — component tests per task + browser journey recorded in T7.2 walkthrough (2026-07-10: all G3.3 leaf tasks T3.7–T3.11 + T3.12 done; component tests green — 210 files / 3548 tests, 100% coverage; in-browser walkthrough still pending T7.2)

## G4 [backend/frontend/specs]: RAG ingestion + re-ingestion lifecycle
- [x] T4.1 [backend]: Outbox enqueue on instant text-content create (depends on T4.2, T3.6) (2026-07-10)
- [x] T4.2 [backend]: Outbox upsert-with-reset repository helper (2026-07-10)
- [x] T4.3 [backend]: Re-enqueue on content update (depends on T4.2, T3.6) (2026-07-10)
- [x] T4.4 [backend]: Chunk + outbox cleanup on content delete (depends on T3.6) (2026-07-10)
- [x] T4.5 [backend]: Worker delete-stale-chunks-before-insert (depends on T4.2) (2026-07-10)
- [x] T4.6 [backend]: Cascade RAG cleanup on topic/node delete (depends on T4.4, T3.2c, T3.5) (2026-07-10)
- [x] T4.7a [frontend]: "No notes yet" hint in parent builder (depends on T3.11) (2026-07-10)
- [x] T4.7b [frontend]: "No notes yet" placeholder in student content viewer (2026-07-10)
- [x] T4.8 [specs]: Re-ingestion contract into 12_content_extraction.md §5 + 01_data_model.md (depends on T4.5) (2026-07-10)
- [x] T4.9 [backend]: G4 integration test — content→chunk lifecycle (ollama-gated + non-gated companion) (depends on T4.1, T4.3, T4.4, T4.5) (2026-07-10) — `tests/integration/worker/test_content_chunk_lifecycle_integration.py`; gated create→drain→patch→drain→delete lifecycle + extraction-materialized-row drain, non-gated outbox state-transition companion (BR-DATA-020 reset); collects clean (6 items via anyio param), self-skips without INTEGRATION_DB_URL, full real-DB run pending CI; full unit suite 4605 passed/42 skipped/100% coverage
- [x] **G4: RAG ingestion lifecycle** — all tasks complete (2026-07-10)

## G5 [backend/frontend/specs]: hAITU answers on parent-owned topics
- [x] T5.1 [backend]: Optional enrollment_id in topic-doubt schema (2026-07-10)
- [x] T5.2 [backend]: Parent-link authorization gate in HaituDoubtService (depends on T5.1) (2026-07-10)
- [x] T5.4 [frontend]: HaituDoubtPanel enabled for Home Study topics (depends on T5.1, T5.2) (2026-07-10)
- [x] T5.5 [specs]: 11_haitu_ai_layer.md §9 — parent-topic access contract (depends on T5.2) (2026-07-10)
- [x] T5.3 [backend]: Severance + cross-family 403 tests (depends on T5.2) (2026-07-10) — `tests/integration/routes/test_g5_severance_cross_family_403.py`; 4 scenarios (mid-session revoke, cross-family, draft-topic, platform-topic null-enrollment regression) against the real `POST /api/haitu/topic-doubt` route + real `doubts` row-count assertions, only `HaituService.stream_answer` stubbed; collects clean, self-skips without `INTEGRATION_DB_URL`, full real-DB run pending CI
- [x] **G5: hAITU on parent topics** — T5.3 [backend] severance + cross-family 403 tests (depends on T5.2) (2026-07-10: all G5 leaf tasks T5.1–T5.5 done — parent-link gate live in HaituDoubtService, HaituDoubtPanel enabled for Home Study topics, access contract documented, severance/cross-family goal test passes locally; full real-DB confirmation pending CI)

## G6 [backend/frontend]: Student Home Study surface complete
- [x] T6.1 [backend]: Live-only + visibility enforcement tests (fixture-driven, no deps) (2026-07-10) — `tests/integration/routes/test_g6_visibility_student_read_paths.py` + `tests/unit/domain/test_services/test_student_dashboard_service_parent_gate.py`; found and fixed a production bug in `StudentDashboardService._assert_node_in_enrolled_subtree`/`get_topic_content` (`src/domain/services/student_dashboard_service.py`) where parent-owned nodes/topics always 403'd for every student because the platform-enrollment-subtree check ran unconditionally regardless of `owner_type` — DI always injects a real `EnrollmentRepository`, so this blocked Home Study entirely in production; fix mirrors the already-shipped `HaituDoubtService` owner_type-branch pattern; integration suite collects clean/self-skips without `INTEGRATION_DB_URL`, full real-DB run pending CI; unit suite green, 100% coverage
- [x] T6.2 [frontend]: Source-aware empty state in existing Home Study tree (2026-07-10)
- [x] T6.3 [frontend]: Home Study content viewing verification (depends on T6.2, T3.5, T4.7b) (2026-07-10)
- [x] **G6: Student Home Study surface** — T6.1 [backend] live-only + visibility enforcement tests (fixture-driven, no deps) (2026-07-10: all G6 leaf tasks T6.1–T6.3 done; goal test T6.1 passes locally — unit gate green, integration suite collects clean and self-skips without INTEGRATION_DB_URL, full real-DB confirmation pending CI)

## G7 [backend/frontend]: Phase acceptance
- [x] T7.1 [backend]: E2E journey test — contract-level (CI-safe) + ollama-gated grounded variant (depends on T1.7, T3.13, T4.9, T5.3, T6.1) (2026-07-11) — part (a) non-gated journey `tests/integration/routes/test_g7_1_e2e_journey_integration.py` (link→adopt→content→outbox pending→draft-hidden→publish→child dashboard Home Study→topic-doubt SSE doubt_id frame + doubt row persisted→revoke→Home Study empty SQL-filtered 200+[] + topic-doubt 403 + 0 new rows, LLM stubbed) + part (b) ollama-gated grounded variant `tests/integration/phase5_ollama_gated/test_g7_1_ollama_gated_grounded_journey.py` (real bge-m3 outbox drain→chunks exist→child `POST /api/haitu/topic-doubt` answer grounded in parent note marker `ZORBLAX-912`→PATCH content via real parent route→re-drain→answer reflects `ZORBLAX-913`; chunk-level deterministic backstop + LLM grounding assertion; through real route + real pipeline, `HaituService.answer` never called directly); both collect clean and self-skip without `INTEGRATION_DB_URL`/Ollama (part b counted in "Ollama-gated: N skipped" summary), full real-DB run pending CI; full unit suite 4625 passed/53 skipped/100% coverage
- [x] T7.2 [frontend]: Frontend closure — suites green + manual browser walkthrough record (depends on T7.1 + all frontend leaf tasks of G1–G6) (2026-07-13: manual walkthrough record complete — `Implementation_planning/phase5_manual_walkthrough_record.md` Parts A–I all pass, sign-off checklist complete; found and fixed G7-patch-18/19/20 during Parts H/A3 along the way — see entries above. 2026-07-14: code changes committed — backend `9e42130`/`3c53b1a`, frontend `816194d`; `progress.md` "Completed Phases" entry filed)
- [x] **G7: Phase acceptance** — all leaf tasks (T7.1, T7.2) complete; Phase 5 acceptance walkthrough green (2026-07-13)

### G7-patch — defects found during Part A of the manual walkthrough (2026-07-11)
- [x] G7-patch-1 [backend]: `GET /api/student/parent-links` returned `id`/`parent_sub`/`created_at` while the frontend contract expected `link_id`/`parent_display_name`/`linked_at` — student `/profile` Linked Parents rows showed a blank name and "Invalid Date". Fixed: `ParentLinkWithProfile` schema now aliases to the expected field names.
- [x] G7-patch-2 [backend]: `parent_display_name` fallback leaked the parent's raw `idp_sub` UUID because no parent ever had a `parent_profiles` row (the `POST /api/parents/me/profile` route has no frontend caller). Fixed: name now resolved from Keycloak instead of the unpopulated profile table.
- [x] G7-patch-3 [frontend]: No UI path to link a second child once a parent already has one linked — `parent-dashboard.tsx`'s "Link your child" card only rendered when `children.length === 0`, no persistent add-another-child affordance existed elsewhere. Contradicted `BR-PAR-015`/`BR-PAR-016` (multi-child, 10-child cap). Fixed: "+ Add another child" affordance added to the child strip (gated on `MAX_ACTIVE_CHILDREN`); re-tested, second child links successfully.
- [ ] G7-patch-4 [backend/frontend]: Second linked child renders as a blank, unlabeled circle in the parent dashboard's child-selector strip instead of a name chip. Same root cause class as G7-patch-2 but mirrored on the child side: `list_children_for_parent` (`user_metadata_service.py:346-362`) sources the name only from `student_profiles`, populated via `POST /api/students/me/profile` — which, like the parent-profile route, has no frontend caller, so most students never get a row. `parent-dashboard.tsx`'s pill also renders `{child.firstName}` with no fallback text and no `aria-label`, so an empty name collapses into an unlabeled circular button (`border-radius: 9999px`, no min-width) — an accessibility gap too. Needs: backend Keycloak-lookup fallback mirroring the G7-patch-2 fix (reuse `KeycloakAdminClient.get_user()`), plus a frontend defensive fallback/`aria-label` on the pill regardless. Not yet fixed.

### G7-patch — defects found during Part B of the manual walkthrough (2026-07-13)
- [x] G7-patch-5 [frontend]: Adding a child node (e.g. subject "Science" under grade root "7") in the parent curriculum builder succeeded (201) but never appeared in the tree, on refresh, or after logout/login. Root cause: `ParentNodeRow` (`parent-node-tree.tsx`) gated the expand toggle's `disabled`/`aria-expanded`/icon and the auto-expand-on-select handler on `children.length > 0`, where `children` falls back to `node.children` pre-expansion — but `node.children` for root nodes always comes from `GET /api/parent/curriculum/nodes`, which never populates children (only the per-node `GET /nodes/{id}` does), so the toggle was permanently disabled with no way to ever trigger the fetch that would reveal the new child. Fixed: added a `mayHaveChildren` value (`isExpanded ? children.length > 0 : !hasTopics` — a node with topics can never have child nodes per BR, the only pre-expansion case safe to treat as childless) used in place of the stale check in all three spots.
- [x] G7-patch-6 [backend]: Adding text content to a topic (B6) appeared to silently fail — no confirmation, content missing after reload/navigating away and back, with a `405 Method Not Allowed` on `GET /api/parent/curriculum/topics/{topic_id}/content` in devtools. Root cause: only `POST` was ever implemented at that path in `parent_curriculum.py` — the frontend's own `getTopicContents` had a `contract-assumption` comment flagging the GET path as unverified, and it was wrong; no route, service method, or repository method existed to list a parent-owned topic's content. Fixed: added `get_owner_content_by_topic` (abstract repo interface + SQLAlchemy impl, same owner-scoped join pattern as `get_owner_content_by_id`), `TopicContentService.get_own_content_by_topic`, and the new `GET /topics/{topic_id}/content` route (`require_parent()`, no CSRF, matching the file's other GETs).
- [x] G7-patch-7 [frontend]: "Back to curriculum" link from a topic's content page 404'd — `parent-topic-content-page.tsx` linked to `/parent/curriculum/${nodeId}`, a route that was never built (only `/parent/curriculum` and `/parent/curriculum/[node_id]/topics/[topic_id]` exist). Fixing the href to the real `/parent/curriculum` page then exposed a second gap: node selection (`selectedNodeId`/`selectedBreadcrumb`) and tree-expand state are local `useState` with no persistence, so returning always landed on the empty "Select a node" view instead of the grade/subject you came from. Fixed: link now carries `?nodeId=<id>`; `parent-curriculum-page.tsx` reads it via `useSearchParams`, walks `parent_id` up through `parentCurriculumApi.getNode` to rebuild the breadcrumb + ancestor-id set, and passes the ancestor ids to `ParentNodeTree` via a new `initialExpandedIds` prop (merged into its internal `expandedIds` state via `useEffect` since it arrives async). This also surfaced and fixed a latent bug in `getNode()`'s return type: it was typed as `ParentNodeDetail` (`{node, children}`) but the backend's `GET /nodes/{id}` actually returns the node flat with `children` as one of its own fields — the tree's lazy-load code only ever read `.children` (present on both shapes, masking the bug), but the new ancestor-walk needed `.parent_id`, which only exists on the real flat shape. Removed the incorrect `ParentNodeDetail` type entirely (dead beyond this one call site) and retyped `getNode()` as `Promise<ParentNode>`.
- [x] G7-patch-8 [deploy/frontend]: The `?nodeId=` restore link (G7-patch-7) 403'd at the gateway when the param was still named `?node=`. Not an application bug — the OWASP CRS ruleset behind this environment's Coraza WAF (rule `932236`, Remote Command Execution/Unix Command Injection) flags common shell/interpreter binary names (`node`, `python`, `bash`, `perl`, …) appearing as a bare query **parameter name** and blocks the whole request (`Inbound Anomaly Score Exceeded`, no route change needed in `haisir-deploy` — the frontend catch-all route already covers this page). Fixed: renamed the param to `nodeId` everywhere it's used.

### G7-patch — defects found during Part C of the manual walkthrough (2026-07-13)
- [x] G7-patch-9 [frontend]: The Adopt-from-Platform modal (and, sharing the same CSS, the "Build from scratch" add-node modal) opened functional but rendered pinned near the top-left of the viewport instead of centered. Root cause: both render `<dialog open>` directly rather than calling the native `.showModal()` API (a deliberate choice — they implement their own backdrop/focus-trap instead of relying on the browser's built-in modal), so the dialog never gets the `:modal` pseudo-class. The UA stylesheet's non-modal default (`position: absolute; inset-block-start: 0`, no `left`/`right`) pulls the dialog out of `.overlay`'s flex layout, so `.overlay`'s `align-items: center; justify-content: center` never applied to it — it just sat at its static top-left position. Fixed: added `position: static` to the shared `.modal` class (`parent-modals.module.css`) so it participates in the flex layout again and centers correctly.

### G7-patch — defects found during Part D of the manual walkthrough (2026-07-13)
- [x] G7-patch-10 [deploy, env-only]: D2's RAG drain check failed — `rag_indexing_outbox` row stuck at `status='failed', retry_count=3, last_error='Connection error.'`. Not a code bug: this dev environment's `EMBEDDING__MODEL_SPEC` was never set, so `build_embed_model()` defaulted to `OllamaEmbedding` against `localhost:11434` inside the backend container, which had zero models pulled. The real embedding host for this environment is an LM Studio instance on the LAN serving `text-embedding-bge-m3`. Fixed by setting `EMBEDDING__MODEL_SPEC=lmstudio://text-embedding-bge-m3@<lmstudio-host>:<port>/v1` in `src/.env` and restarting the worker; re-verified outbox `done` + a chunk row in `data_topic_content_chunks`. Surfaced a real product gap while diagnosing this — see G7-patch-11.
- [ ] G7-patch-11 [backend/frontend, spec gap]: No parent-facing visibility into RAG indexing status, and no way to retry a permanently failed embed. `_claim_batch` (`rag_outbox_loop.py`) excludes any row with `retry_count >= _MAX_RETRY (3)` from ever being picked up again — a transient embedding-host outage permanently and silently degrades that content to text-only hAITU grounding. The student-facing hAITU path (`vision/requirements/08_haitu_ai_layer.md`) already treats `status='failed'` identically to `status='done'` with no chunks (both fall back to raw `topic_contents.text`, no retry hint) — by design, so students aren't blocked, but that also means "permanently failed" and "successfully has nothing to embed" are indistinguishable to everyone, including the parent. `target/requirements/05_parent.md` and `11_haitu_ai_layer.md` have zero mention of indexing-status visibility or a retry affordance (unlike `ExtractionJob`'s explicit `POST .../extraction-jobs/{id}/retry`). Not yet fixed — needs a real design decision (status chip on `ParentTopicRow`? explicit retry endpoint? auto-retry with backoff?) before implementation; tracked for `/update-target-state`. Now speced: `target/requirements/05_parent.md` BR-PAR-020 + `target/requirements/01_data_model.md` BR-DATA-023 (2026-07-13, `/update-target-state`) — spec only, implementation still not started.

### G7-patch — defects found during Part E of the manual walkthrough (2026-07-13)
- [x] G7-patch-12 [backend]: E1 — Student A's `/home` "Home Study" section showed the heading but zero cards, despite Parent A having root nodes and an active link. Root cause: `GET /api/student/nodes?owner_type=parent` 400'd without an `owner_id` query param, but the frontend (`useStudentDashboard`) never passes one — and has no way to: a parent's raw `idp_sub` is deliberately excluded from `GET /api/student/parent-links`'s response (`parent_sub: str = Field(exclude=True)`). The 400 threw inside the hook's try/catch and was silently swallowed; `hasParentLink` was already `true` so the section rendered with an empty node list. The service (`get_node_tree`) already independently loads the student's active parent links just to *validate* a client-supplied `owner_id` — it never actually needed the client to supply it. Fixed: `owner_id` is now optional end-to-end; the route no longer 400s when it's missing, and the service auto-resolves to the student's (first) actively-linked parent when omitted, reusing the link list it already fetches.
- [x] G7-patch-13 [frontend]: E5 — selecting a Home Study node with zero topics (e.g. "Maths") still showed `TopicListPanel`'s "Select a node to view topics" message, identical to the true no-selection state, instead of a "this node has no topics" message. Root cause: the component's only empty-state branch was `topics.length === 0`, with no check on whether `selectedNodeId` was actually set. Fixed: added a `selectedNodeId === null` check first, and a source-aware message for the "selected but empty" case (`"No Home Study content yet — ask your parent to add topics"` for Home Study, `"No topics in this course yet"` for Platform), plumbed via a new `source` prop from `student-courses-page.tsx`. E4's analogous logic in `content-viewer.tsx` was already correct (source-aware "Your parent hasn't added notes to this topic yet") — just untested since no live, contentless topic existed in the walkthrough's test data yet.
- [x] G7-patch-14 [frontend, unrelated to G6 but caught during Part E]: Grade nodes displayed as bare numbers ("7", "8") instead of "Grade 7"/"Grade 8" on the student `/home` Platform Board and Home Study cards and the `/courses` tree sidebar — inconsistent with the existing Browse Courses catalog page (`catalog-card.tsx`), which already had the correct `node.node_type === "grade" && /^\d+$/.test(node.name) ? \`Grade ${node.name}\` : node.name` formatting. That exact pattern (already used in 2 other places in the codebase too: `use-exam-authoring.ts`, `grade-options.ts`) was just never applied in these 3 spots. Fixed by applying the same inline pattern in `home-study-section.tsx`, `platform-board-section.tsx`, and `node-tree-sidebar.tsx` (added `node_type` to `home-study-section.tsx`'s local `NodeCard` type, which was missing it).

### G7-patch — defects found during Part F of the manual walkthrough (2026-07-13)
- [x] G7-patch-15 [backend/frontend, security-relevant]: "Ask Teacher Help" ("Ask your teacher" button in `haitu-doubt-panel.tsx`) rendered and functioned identically on Home Study (parent-owned) topics as on platform topics, with no ownership check anywhere in the chain. Escalating set `status='escalated'` and notified `recipient_role="instructor"` — but there is no instructor overseeing parent-owned content in this increment (BR-SEC-005; `05_parent.md`'s "no instructor oversight" scope), so the notification went nowhere meaningful, and it never notified the parent either (`fan_out_to_parents` exists in `doubt_service.py` but is only wired into a later "teacher replied" event, not the initial escalation). Worse: `list_for_teacher_queue` (`doubt_repository.py`) also had no `owner_type` filter — an actual instructor account could see the topic title, student name, and doubt content from a parent's private Home Study topic, a real cross-boundary exposure inconsistent with BR-SEC-005. Root cause: `target/requirements/11_haitu_ai_layer.md` §3.4's "shared instructor queue" escalation design was speced entirely around platform topics; Home Study was never considered. Fixed (immediate block, not a full redesign — see below): `AbstractDoubtRepository` gained `get_topic_owner_type(topic_id)`; `DoubtService.escalate_doubt` now raises the existing `DoubtConflictError` (409, generic message, no ownership details leaked) when the doubt's topic is `owner_type='parent'`; `haitu-doubt-panel.tsx` hides the button entirely when `source === "parent"` (defense in depth — UI hide alone wouldn't have stopped a direct API call). Re-verified: button no longer appears on Home Study topics.
- [ ] G7-patch-16 [spec gap, open]: Should Home Study doubts have their own escalation path at all (e.g. notify the parent instead of an instructor), or is "no escalation for Home Study" the permanent intended behavior? Not designed — G7-patch-15 only blocks the broken path, it does not decide what (if anything) should replace it. Tracked for a future `/update-target-state` pass, same treatment as G7-patch-11's RAG-indexing-visibility gap.
- [x] G7-patch-17 [backend/frontend]: G7-patch-15's fix only covered the live in-topic hAITU panel (`haitu-doubt-panel.tsx`) — a second, separate UI location, the persisted doubt-thread page (`/doubts/{id}`, `doubt-thread-page.tsx`), had its own independent "Request teacher help" button gated only on `canEscalate(thread.status)` with no topic-ownership awareness, and its data source (`GET /api/students/me/doubts/{id}`) never carried that information at all. The backend-side block from G7-patch-15 (`escalate_doubt` service check) already covered this path too — clicking it would have 409'd safely — but the button stayed visibly present, a broken-looking affordance. Fixed by threading topic ownership through the single-thread fetch: `get_with_messages` (`doubt_repository.py`) now also selects `topics.owner_type`; `DoubtSummary`/`DoubtRead` gained a `topic_owner_type` field (optional 4th param on `_build_summary`, other 2 call sites unaffected); frontend `Doubt`/`DoubtBackendDTO` types and `mapDoubt` carry it through; `doubt-thread-page.tsx` now hides the button when `topic_owner_type === "parent"`. Re-verified: button gone on the Home Study ("Ratio") doubt thread.

### G7-patch — defects found during Part H of the manual walkthrough (2026-07-13)
- [x] G7-patch-18 [frontend]: H1 — `/parent` had no way to revoke a child link at all. The backend endpoint (`DELETE /api/parent/children/{child_sub}/link`, T1.3) has existed since G1, but `parent-dashboard.tsx`'s child strip only ever rendered a selectable pill per child (`selectChild`/`aria-pressed`) — no revoke button, confirm dialog, or API caller existed anywhere in the `features/parent` slice; `parentApi` had no `revokeChild` method and `useParentChildren` never exposed a mutation. Root cause: the student-side mirror of this flow (`/profile` → Linked Parents → confirm-then-remove, `use-student-profile.ts`'s `removeParent`) was built in G1 T1.5, but the parent-side equivalent was never built alongside T1.3 — T1.3's Done-when only covered the endpoint, not a UI consumer. Fixed: `parentApi.revokeChild` (DELETE, expects 204), `useParentChildren` gained a `revokeChild`/`revokingSubs`/`revokeError` mutation (mirrors `use-student-profile.ts`'s `removeParent` pattern, invalidates the children query on success), and `parent-dashboard.tsx` adds a "Revoke" control per child pill with an inline confirm/cancel prompt (mirrors the `/profile` Linked Parents row pattern). Re-verified: unit tests green (`parent-api.test.ts`, `use-parent-children.test.ts`, `parent-dashboard.test.tsx`), `pnpm typecheck` clean for the changed files.
- [x] G7-patch-19 [backend/frontend]: H6 — re-linking a previously-revoked parent-child pair failed at the confirm-dialog step with a generic "Something went wrong" error, even though the code-redemption itself (`POST /api/parent-child-links`) was never reached. Root cause: `GET /api/parent-link-codes/{code}` (the parent's validate-before-link lookup) has, since it was first built in `718e692`, only ever returned `{code, child_sub, expires_at}` — its domain model (`ParentLinkCode`) carries no name data at all. The frontend's `LinkCodeValidationDtoSchema` (added later, unrelated commit) required `{first_name, last_name}` on that same response, which never existed — so `parentApi.validateLinkCode`'s Zod parse threw on every call (502, generic message), meaning the parent-side link confirm dialog has never actually been reachable via this path, for a first link or a re-link (Part A3's "Confirmed" walkthrough note for this exact dialog predates this level of scrutiny and was not an accurate check). Fixed: added `UserMetadataService.get_link_code_preview` (resolves the child's name via the existing Keycloak-fallback pattern also used by `list_children_for_parent`/`list_parent_links_for_child` — `_resolve_identity`/`build_display_name`), new domain model `ParentLinkCodePreview`, `ParentLinkCodeRead` schema gained a computed `child_display_name` field (mirrors `ChildLinkWithProfile`'s pattern exactly), route now calls the new service method. Frontend: `LinkCodeValidationDtoSchema`/`LinkCodeValidationDto`/`LinkCodeValidation` changed from `{first_name/last_name}` to `{child_display_name}` → `{displayName}`; `link-child-page.tsx`'s confirm text updated. Re-verified: backend unit tests green (81 passed, `mypy` clean), frontend `pnpm typecheck` clean; re-link completed successfully in-browser.
- [x] G7-patch-20 [backend]: After G7-patch-19 unblocked H6, the re-linked student's Home Study still showed no content. Root cause: the student had a *second* active parent link (from an earlier, unrelated test pass against a different parent account that was never revoked) alongside the fresh Parent-A link. `StudentDashboardService.get_node_tree`'s owner-resolution (added by G7-patch-12) picked `links[0]` — the single *oldest* active parent link — whenever the frontend omits `owner_id` (which it always does, per G7-patch-12's own rationale: the frontend has no source for a parent's raw `idp_sub`). The stray older link belonged to a parent with zero owned `course_path_nodes`, so Home Study correctly detected "a parent is linked" but silently showed the wrong (empty) one, with no signal to the student that a second link even existed. This surfaces a real, previously-undiscovered case: a student can have more than one active parent link (e.g. both parents), which G7-patch-12's "just pick one" heuristic never accounted for. Fixed: `get_node_tree` now aggregates nodes from **every** actively-linked parent instead of just the first, consistent with how Home Study cards already render as one flat list with no per-parent grouping. Verified against real DB data (one parent with 9 nodes, one with 0) that the merge now surfaces the non-empty parent's content; existing `test_student_dashboard.py`/`test_student_dashboard_service.py` suites pass except one pre-existing stale test (`test_get_nodes_400_parent_type_without_owner_id`) that still asserts G7-patch-12's since-removed 400 contract — unrelated to this fix, not yet updated (tests deferred per user direction during this session).

## Ready now
Phase 5 (G1–G7) is closed (2026-07-14). G7-patch-18/19/20 committed at backend `9e42130`/`3c53b1a`, frontend `816194d`; `progress.md`'s "Completed Phases" entry is filed. Open backlog (not phase blockers, tracked for a future cycle): G7-patch-4 (blank second-child pill), G7-patch-11 (RAG-indexing status/retry UI — spec'd via BR-PAR-020/BR-DATA-023, not implemented), G7-patch-16 (Home Study escalation design question, undecided). Next: `/plan` for Phase 6 — `PLAN.md`'s baseline watermark predates all of Phase 5 and needs to be re-cut.
